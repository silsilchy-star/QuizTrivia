# 랭킹 기반 다주제 학습 퀴즈 게임 — 설계 계획서

> **버전** v1.5 · **작성일** 2026-08-12
> **상태** **Day 1 진행 중.** 남은 미결정은 Day 5~6 항목 7개(Q5는 Q5'로 갱신)뿐이며 전부 추천안 있음 (11장)
> **기간** 7일 (1일 8시간 기준, 총 56시간)
>
> **v1.5 변경** — **백엔드를 Firebase에서 Cloudflare(Workers + D1)로 전환**. Firebase는 EP-5 뒤에 격리한 채 가능성만 열어두고 지금은 쓰지 않습니다. 자세한 내용은 부록 A와 1.2·7장 참고.

---

## 0. 이 문서를 읽는 법

모든 항목에 아래 표시 중 하나가 붙습니다. **사용자가 지시하지 않은 것을 확정된 것처럼 쓰지 않기 위한 장치**입니다.

| 표시 | 의미 |
|---|---|
| **[확정]** | 사용자가 직접 지시했거나 승인한 내용. 이 문서의 전제입니다 |
| **[제안]** | 설계자가 근거를 들어 제안하는 내용. **승인 전까지 확정이 아닙니다** |
| **[미결정]** | 아직 정보가 부족한 항목. 11장에 모아두었습니다 |

**빠르게 보려면** → 3장(확정 결정 목록)과 10장(한 장 요약)만 보십시오.
**설계가 왜 이렇게 됐는지** 알고 싶으면 → 부록 A(변경 이력).

> **용어** — 지식 영역의 분류 단위는 **주제(topic)**입니다. 단일 층이며 상위 분류는 없습니다. 코드·DB에서도 `topic`을 씁니다.

---

## 1. 목적과 성공 기준

### 1.1 목적 [확정]

목적이 **두 층**입니다. 둘을 구분하는 것이 중요합니다.

**층 1 — 제품의 목적**
랭킹으로 학습 동기를 부여하고, 플레이어가 자신의 지식 영역을 키울 수 있는 지식 체계를 구축하게 하는 퀴즈 게임.

**층 2 — 콘텐츠 축**
**생성형 AI로 여러 주제를 학습한다.** 한 사람이 여러 분야를 경험하며 학습하는 것, 그 자체가 이 게임의 콘텐츠다.

> **층 2가 설계에 미치는 영향**
> 생성형 AI는 실습 소재가 아니라 **콘텐츠 폭을 만드는 엔진**입니다. 한 사람이 혼자서는 만들 수 없는 분야 수의 문제를 확보하는 수단이고, 그렇게 확보한 **폭 자체가 플레이어에게 제공되는 가치**입니다.
>
> 따라서 **주제 등록 파이프라인(6.6절)이 게임의 핵심 제작 시스템**입니다. 부수 도구가 아니라 이 게임이 존재하는 이유입니다.

### 1.2 기술적 방향성 [확정 · v1.5 변경]

- 백엔드는 **Cloudflare** 기반 (Workers + D1)
- **Firebase는 가능성만 열어둔다 — 지금 쓰지 않는다.** EP-5 저장소 계층 격리를 유지해 나중에 필요하면 부분/전면 전환 가능해야 함
- 실시간 멀티가 필요해지면 Durable Objects로 확장 → 7.4절 / 확장점 EP-5

> v1.4까지는 반대 방향(Firebase 메인 · Cloudflare는 실시간 전용)이었으나, 사용자가 v1.5에서 뒤집었습니다. 이 절 아래 7장의 Firebase 관련 서술은 전부 Cloudflare 기준으로 다시 읽으십시오 — 표는 7.1절에서 갱신됨.

### 1.3 성공 기준 [제안]

7일 후 아래가 전부 참이면 성공입니다.

1. 낯선 사람이 링크를 받아 **로그인 없이** 주제를 골라 1판을 끝까지 플레이할 수 있다
2. 점수를 랭킹에 올리려 하면 로그인이 요구되고, **익명으로 쌓은 기록이 그대로 승계**된다
3. 통합 랭킹과 주제별 랭킹에서 상위 30위와 자기 위치를 볼 수 있다
4. **새 주제를 코드 수정 없이** 등록·적재할 수 있고, 문항 하한에 미달한 주제는 자동으로 게임에 노출되지 않는다
5. 통합 랭킹에서 위로 가려면 **여러 주제를 해야만 한다** — 기획 의도가 점수 공식에 박혀 있다
6. 실시간 배틀을 나중에 붙일 때 기존 코드를 갈아엎지 않아도 되는 확장점이 존재한다

---

## 2. 게임 개요

### 2.1 개요 [확정]

| 항목 | 내용 |
|---|---|
| 장르 태그 | 퀴즈, 퍼즐, 암기, 연산, 교육 |
| 핵심 재미 | 랭킹으로 동기 부여 → 여러 주제를 오가며 자기 지식 체계 구축 |
| 핵심 시스템 | 퀴즈 / **주제** / 랭킹 / 스테이지 / 수리 퍼즐 / 수학 연산 |
| 코어 루프 | 주제 선택 → 퀴즈 → 스테이지 증가(난이도 상승) → 랭킹 |
| 타겟층 | **15~20세** (중3~대학 저학년) |
| 콘텐츠 성격 | **넓고 얕게.** MVP는 단순 사실 확인 수준 |

> **타겟층이 톤의 기준선입니다.**
> 15~20세는 학교에서 이미 시험 공부를 하고 있습니다. 게임에서까지 교과 시험처럼 느껴지면 거부감이 생깁니다. **문제는 쉽고 짧게, 대신 "어? 아니었어?"가 남도록** 만듭니다.

### 2.2 콘텐츠 단계 로드맵 [확정]

퀴즈의 주제와 수준을 **한꺼번에 올리지 않고 단계적으로** 올립니다. 전체를 10단계로 볼 때 **MVP는 1~3단계**입니다.

| 단계 | 문제 성격 | 적재 대상 | 상태 |
|---|---|---|---|
| **1~3** | **사실 확인** — "안다" | 과학 · 지리 · 스포츠 | **MVP 범위** |
| 4~7 | 원리·유래 — "왜 그런지 안다" | 역사, 세계 문화, IT 기초, 맞춤법 등 주제 추가 | 로드맵 |
| 8~10 | 통찰·판별 — "적용하고 가려낸다" | 사람의 착각, 돈이 움직이는 법 등 주제 추가 | 로드맵 |

> **이 로드맵은 D-5(주제 등록 구조) 덕분에 실현 가능합니다.**
> 단계를 올릴 때 코드를 고치는 게 아니라 **주제를 새로 등록하기만 하면 됩니다.** 만약 주제를 코드에 상수로 박아뒀다면 4단계로 갈 때 전면 수정이었습니다. "기둥을 먼저 세운다"는 제약이 왜 옳았는지가 여기서 드러납니다.

---

## 3. 확정 결정 목록 (Decision Log)

아키텍처 전체가 여기에 매달려 있습니다. **바뀌면 일정을 다시 짜야 합니다.**

| ID | 결정 | 핵심 이유 | 따라오는 책임 |
|---|---|---|---|
| **D-1** | 플랫폼: **React + TypeScript (웹)** | 링크 공유만으로 플레이 · 백엔드 교체 자유 (EP-5) | 화려한 게임 연출은 포기 |
| **D-2** | 문제: **사전 배치 생성 + 사람 검수** | 런타임 비용 0, 지연 0 · 환각을 사용자 도달 전 차단 | 검수 시간이 주제 수에 비례 (Day 4) |
| **D-3** | 실시간 배틀: **MVP 제외** | 최소 2~3일 소요 → "기둥 우선" 제약과 충돌 | 들어올 자리를 미리 비워둘 것 (EP-4) |
| **D-4** | 인증: **익명 플레이 + 랭킹 등록 시에만 로그인** | 진입 장벽 최소화 | **계정 승계 로직 필수** (Day 6) |
| **D-5** | 주제: **언제든 등록 가능한 1급 엔티티** | 층 2 목적의 직접 구현 · 로드맵의 전제 | 구조는 무제한, 초기 활성은 3개 |
| **D-6** | 한 판의 구성: **주제 1개 선택** | 구현 단순 · 주제별 랭킹이 자연스러움 · 학습자의 선택권 유지 | "여러 분야 경험"은 강제가 아닌 **랭킹 보상으로 유도** |
| **D-7** | 통합 랭킹: **폭(breadth) 보상형** | 기획 의도를 점수 공식에 직접 박아 넣음 | 주제 간 난이도 형평성 관리 필요 |
| **D-8** | 시드 적재: **3개 × 80문항 = 240문항** | 검수 3시간 → Day 4 하루에 들어감 | 하한 미달 시 주제 자동 비활성 |
| **D-9** | 랭킹 노출: **상위 30위** | 한 화면에 들어감 · 조회 비용 절감 | 30위 밖 정확한 등수는 표시하지 않음 |
| **D-10** | 타겟: **15~20세 · 넓고 얕게** | 학습 동기가 확실하되 교과 피로도가 높은 층 | 교과 시험 냄새가 나는 주제 배제 |
| **D-11** | 시드 주제: **과학 / 지리 / 스포츠** | 결이 달라(자연/공간/규칙) 폭이 체감됨 · 정답이 공식 자료 한 곳에서 확인됨 | 주제별 고유 함정 존재 (6.4절) |
| **D-12** | 수학: **주제가 아니라 문제 유형** | 15~20세에게 "수학"이라는 이름표는 진입 장벽 | 모든 주제에 `NUMERIC_INPUT`을 일정 비율 배치해야 "수리 퍼즐·수학 연산" 시스템이 유지됨 |
| **D-13** | 콘텐츠를 **10단계로 나누고 MVP는 1~3단계** | 주제와 난이도를 한꺼번에 올리면 검수 부담과 실패 위험이 동시에 커짐 | 난이도 축을 **인지도 기준**으로 정의 (6.2절) |
| **D-14** | 문제 생성은 **수동** — 채팅 UI에 요청 후 붙여넣기 | API 키 없음 | 프롬프트를 **재사용 템플릿 파일**로 만들어야 일관성 유지. 생성 전략도 "선생성"에서 "필요한 만큼 추가"로 |
| **D-15** | 분류는 **주제 단일 층** — 상위 대분류(장르) 없음 | 2층 구조는 무겁고, 확장성·유연성이 목적 | 주제가 늘면 랭킹 보드도 늘어남 → **UI 층에서 억제**해야 함 (5.2절) |
| **D-16** | 주제는 **잘게 쪼갠 태그**이며 문항 하나가 **여러 주제에 속함** (`topicIds` 배열) | 끄투코리아 방식. 잘게 쪼개면 확장성이 오르지만 주제당 문항이 모자라는데, **복수 부착이 그 문제를 해소**함 | 태그 부착이 파이프라인의 정식 단계가 됨 · 태그가 심하게 겹치는 주제는 합쳐야 함 (6.4절 ⑤) |

> **D-6과 D-7의 조합이 이 설계의 핵심 장치입니다.**
> 플레이어는 원하는 주제만 골라 플레이할 수 있습니다(자유). 그런데 통합 랭킹은 **주제별 최고점의 합**이므로, 한 분야만 파면 점수가 천장에 막히고 새 분야를 건드리면 즉시 오릅니다. **강제 없이 "여러 분야 경험"이 이득이 되도록** 만든 것입니다. 층 2 목적이 UI 문구가 아니라 산술식에 들어가 있습니다.

---

## 4. 코어 루프

### 4.1 루프 구조

```
  [시작]
    ↓
  주제 선택  ← 활성(active) 주제만 노출
    ↓
  스테이지 N 진입  ──→  문제 5개 출제 (난이도 = f(N), 해당 주제 풀에서)
    ↓
  문제 풀이 (정답/오답 즉시 피드백 + 해설)
    ↓
  스테이지 클리어 판정
    ├─ 성공 → 점수 획득 → 스테이지 N+1  ──┐
    └─ 실패 → 판 종료                      │
    ↓                                      │
  판 종료 → 점수 산출                    ←─┘
    ↓
  주제 최고 기록 갱신 여부 판정
    ↓
  [랭킹 등록?] ─ 예 → 로그인 필요 → 통합·주제 랭킹 반영
                └ 아니오 → 기록만 보관
```

### 4.2 스테이지와 난이도 [제안]

| 항목 | 값 | 근거 |
|---|---|---|
| 스테이지당 문항 | **5문항** | 3문항은 운이 지배하고, 10문항은 스테이지가 오르는 리듬감이 죽음 |
| 클리어 조건 | **4문항 이상 정답** | |
| 최대 스테이지 | **12** | 아래 계산 |
| 난이도 구간 | 1–3: 난이도1 / 4–6: 난이도2 / 7–9: 난이도3 / 10–12: 난이도4 | |

**최대 스테이지를 12로 잡은 계산**

12스테이지를 완주하면 난이도별로 15문항씩(3스테이지 × 5문항) 소비합니다. 주제당 난이도별 보유량이 20문항이므로 **판마다 20개 중 다른 15개가 뽑혀** 반복감이 줄고 문제 고갈도 없습니다.

- 무한 스테이지: 난이도 4 문항이 즉시 고갈되어 같은 문제가 반복됩니다 ✗
- 12스테이지: 문항 수급과 정확히 맞물립니다 ✓

> **나중에 최대 스테이지를 올릴 수 있습니다.** 주제에 문항이 쌓이면(난이도당 40문항) 스테이지를 24까지 늘릴 수 있고 만점도 올라갑니다. **즉 "주제를 깊게 만드는 것"도 랭킹의 새 상한을 여는 방법입니다.** MVP에서는 12로 고정합니다.

**유한 스테이지의 부작용과 대응**
만점자가 여럿 생기면 동점이 발생합니다. → **동점 시 먼저 달성한 사람이 상위** (`updatedAt` 오름차순) [제안]

### 4.3 문제 재사용 정책 [제안]

- **한 판 안에서는 같은 문제가 두 번 나오지 않습니다** (필수)
- **판을 넘어선 반복은 허용합니다**
  *근거: 이건 학습 게임이고 장르 태그에 "암기"가 있습니다. 반복 노출은 결함이 아니라 학습 기능입니다.* 다만 매번 같은 순서면 지루하므로 난이도 풀에서 무작위 추출합니다.

### 4.4 문제 유형 [확정]

| 유형 코드 | 설명 | 커버하는 시스템 |
|---|---|---|
| `MULTIPLE_CHOICE` | 4지선다 | 퀴즈, 암기 |
| `NUMERIC_INPUT` | 숫자 직접 입력 | **수학 연산, 수리 퍼즐** |

MVP는 이 둘만. 서술형·순서배열·매칭은 확장점 EP-1.

**단답 입력(`SHORT_TEXT`)은 MVP 제외** [제안]

끄투식 "정의 → 단어" 문항("배추를 양념에 절여 숙성시킨 한국의 대표 반찬은?" → 김치)은 단답 입력이 가장 자연스럽습니다. 구현 비용도 크지 않습니다 — `NUMERIC_INPUT`의 입력 UI를 재사용하므로 문자 정규화와 허용 답안 비교만 추가하면 1시간 남짓이고, 4지선다 오답 검수가 빠지는 만큼 Day 4에서 일부 회수됩니다.

**그럼에도 제외하는 이유는 비용이 아니라 두 가지 감각 문제입니다.**

1. **모바일 타이핑이 퀴즈 리듬을 깹니다.** 타겟이 15~20세면 대부분 폰입니다. 한글 자판으로 단어를 치는 건 버튼 누르기보다 3~5배 느립니다. 숫자는 키패드라 빠르지만 한글은 다릅니다. **구현 문제가 아니라 게임 감각 문제라 나중에 고치기도 어렵습니다.**
2. **억울한 오답이 이탈 사유가 됩니다.** "이산화탄소/이산화 탄소", "오타와/오타와시", "산소/O" — 허용 답안을 다 잡았는지는 플레이 전엔 모릅니다. 학습 게임에서 이건 치명적입니다.

> **미루는 비용이 거의 0입니다.** `answer` 필드가 이미 있으므로 단답 전환은 **문항 재작성이 아니라 `acceptedAnswers` 필드 추가**입니다. 240문항을 다시 볼 필요가 없습니다.
>
> **대신 지금 공짜로 해둘 것** — 생성 프롬프트에 **"정답은 완전한 문장이 아니라 단어 형태로만"**을 지시합니다. `"김치입니다"` ❌ → `"김치"` ⭕. 비용 0, 나중 재작업 0. (6.6절 [2] 원칙에 포함)
>
> Day 7 버퍼가 남으면 몇 문항으로 단답을 실험해보고, 감각이 좋으면 다음 단계에 넣습니다.

**`NUMERIC_INPUT` 비율: 주제별 문항의 20~25% (80문항 중 16~20문항)** [확정]

D-12에 따라 수학은 별도 주제가 아니라 이 유형으로 존재합니다. **비율 하한을 정해두지 않으면 AI 생성 시 계산 문항이 거의 안 나오고, 핵심 시스템에 명시된 "수리 퍼즐·수학 연산"이 조용히 사라집니다.** 생성 프롬프트에 주제별 목표 개수를 명시합니다.

부수 이득이 큽니다 — `NUMERIC_INPUT`은 **기계가 정답을 직접 계산해 대조할 수 있어**(6.6절 [3]) 검수 시간이 문항당 1분에서 15초로 줄어듭니다.

| 주제 | 수리 연산이 들어가는 지점 | 예시 |
|---|---|---|
| 과학 | 단위 환산, 온도, 원자번호 | "물의 끓는점은 섭씨 몇 도?" → 100 |
| 지리 | 시차, 면적·인구 비교 | "한국과 영국의 시차는 몇 시간?" → 9 |
| 스포츠 | 인원·시간·점수·거리 — **소재가 가장 풍부** | "마라톤 정식 거리는 몇 km?" → 42.195 |

> **1~3단계의 `NUMERIC_INPUT`은 계산이 아니라 "수치를 정확히 아는가 + 한 단계 환산"입니다.**
> 확률 직관의 오류(기저율 무시, 생일 역설) 같은 문항은 훨씬 강력하지만 8단계 소재입니다 → 2.2절 로드맵.

---

## 5. 점수와 랭킹

### 5.1 점수 시스템 [제안]

사용자 지시는 **"문제 해결 횟수 당 점수 부여"**입니다. 그대로 하면 `점수 = 맞힌 문제 수`인데, 이러면 **쉬운 문제만 반복하는 것이 최적 전략**이 되어 난이도 상승이라는 코어 루프와 충돌합니다.

**제안: 난이도 가중치를 곱합니다.**

```
판 점수 = Σ (맞힌 문제의 난이도 등급 × 10)
```

**만점 계산** (12스테이지 완주)

| 스테이지 | 난이도 | 문항 | 점수 |
|---|---|---|---|
| 1–3 | 1 | 15 | 150 |
| 4–6 | 2 | 15 | 300 |
| 7–9 | 3 | 15 | 450 |
| 10–12 | 4 | 15 | 600 |
| | | | **1,500점** |

→ 주제 3개 전부 만점 시 통합 **4,500점**.

- 시간 보너스, 콤보 보너스는 **MVP 제외** [제안]
  *근거: 밸런싱 난이도를 크게 올리고, 없어도 코어 루프는 완전히 작동합니다.*

### 5.2 랭킹 시스템

**보드 구성** [제안]

| 보드 ID | 내용 | 산출 방식 |
|---|---|---|
| `global_all_time` | 통합 · 전체 기간 | **Σ(주제별 최고 판 점수)** ← 폭 보상 |
| `global_weekly` | 통합 · 주간 (매주 리셋) | 그 주 안에서의 Σ(주제별 최고 판 점수) |
| `topic_{topicId}` | 주제별 · 전체 기간 | 해당 주제 최고 판 점수 |

**폭 보상형 공식이 만드는 성질**

- 한 주제만 파면 그 주제 만점(1,500)이 천장 → 상한이 막힘
- 새 주제를 건드리면 즉시 점수가 오름 → **폭이 곧 이득**
- 나중에 주제가 추가되면 **모두에게 동시에 새 상한이 열림** → 후발 주자에게도 따라잡을 여지가 생김. 랭킹이 굳는 것을 막는 부수 효과

**⚠ D-15·D-16이 남긴 문제와 대응 — 주제가 늘면 보드도 늘어납니다**

주제 단일 층이고 태그처럼 잘게 쪼개므로, 주제가 20~30개가 되면 `topic_*` 보드도 그만큼 생깁니다. **D-16으로 이 시나리오는 가정이 아니라 예정된 결과입니다.** 랭킹 메뉴에 보드 30개를 나열하면 아무도 안 봅니다.

> **[제안] 데이터가 아니라 UI 층에서 막습니다.**
> - **랭킹 메뉴에는 통합 보드만 둡니다** — `global_all_time`, `global_weekly` 2개
> - **주제별 순위는 그 주제의 플레이 화면 안에서** 보여줍니다 (선택 → 진입 → "이 주제 내 순위 7위")
>
> 데이터 구조는 그대로 두고 노출 위치만 나눕니다. 주제가 몇 개로 늘어도 랭킹 메뉴는 항상 2개입니다.

**폭 보상형 통합 점수의 상한은 계속 오릅니다.** 주제 20개면 만점이 30,000점이 되어 "전 주제 만점"은 사실상 불가능해집니다. **이건 결함이 아니라 이득입니다** — 랭킹이 늘 열려 있고 굳지 않습니다.

**주간 보드가 필요한 이유**
전체 기간 보드만 있으면 초기 상위권이 굳어 신규 플레이어의 동기가 죽습니다. 주간 보드는 매주 모두를 0에서 다시 시작시킵니다.

### 5.3 ⚠ 랭킹 조회 비용 — 반드시 주의할 함정

Cloudflare D1 무료 등급도 **일일 읽기·쓰기 행 수로 제한**됩니다. 랭킹을 매번 `topic_best`에서 정렬 조회하면 보드마다 다수의 행을 읽고, 보드가 여러 개면 배가 됩니다. 랭킹 페이지 하나로 무료 한도를 태울 수 있습니다.

**대응** [제안]: 각 보드의 상위 30명을 **`ranking_cache` 테이블 한 행에 미리 합쳐 저장**합니다. 1회 조회 = 1행 읽기가 됩니다.

> 위 UI 분리(5.2절)가 이 비용도 같이 줄입니다. 랭킹 메뉴에서 읽는 캐시 행이 항상 2개로 고정되기 때문입니다.

**30위 밖 플레이어에게 보여줄 것** [제안]

정확한 등수를 계산하려면 "나보다 점수가 높은 사람 수"를 세야 하는데 이게 비쌉니다. → **"30위권 밖 · 내 점수 1,240점 · 30위 커트라인 1,580점"** 형태로 표시합니다. 커트라인은 캐시 문서에 이미 있어 공짜이고, "얼마나 더 하면 드는지"라는 실질 정보는 등수보다 오히려 유용합니다.

### 5.4 인증 및 계정 승계

```
  최초 방문 → Worker가 자체 익명 uid 발급 (사용자는 인지 못 함)
    ↓  uid를 클라이언트에 저장(쿠키/localStorage) → 이 uid로 플레이 기록 저장
  ...플레이...
    ↓  [랭킹 등록] 클릭 → 소셜 로그인 요구
    ↓  소셜 계정 ↔ 익명 uid 연결 (구현 방식은 **미결정 — Q5'**)
    →  uid가 유지되므로 기존 기록이 그대로 승계됨
    ↓  닉네임 입력 → 랭킹 반영
```

> **⚠ Firebase Auth의 `linkWithCredential`처럼 "익명 계정 ↔ 소셜 계정 연결"을 공짜로 해주는 기능이 Cloudflare엔 없습니다.** OAuth 자체 구현(Google 등 제공자의 OAuth 2.0을 Worker에서 직접 처리) 또는 Cloudflare 호환 인증 라이브러리(Lucia, Auth.js 등) 중 하나를 Day 6 시점에 골라야 합니다 → **Q5'로 이관**.
>
> **⚠ 실패 케이스가 하나 있습니다** (라이브러리를 쓰든 자체 구현이든 동일).
> 연결하려는 소셜 계정이 **이미 다른 uid로 가입되어 있으면** 연결이 실패합니다 (다른 기기에서 로그인한 적이 있는 경우). 선택지는 "익명 기록 폐기" 또는 "두 기록 병합"인데 병합은 구현이 무겁습니다.
> **[제안] MVP는 "기존 계정 기록 유지 · 익명 기록 폐기"로 처리하되, 폐기 전 사용자에게 명확히 경고합니다.**

---

## 6. 주제와 콘텐츠

### 6.1 주제 체계 — 끄투식 태그 [제안 · D-16]

**참고 자료: 끄투코리아의 주제 분류.** 끄투의 주제는 표준국어대사전의 「전문 분야」 라벨에서 따온 것으로, 경제·공업·교육·교통·농업·문학·물리·미술·동물·생물·수학·식물·언어·역사·운동·음악·지리·천문·컴퓨터·화학·음식 등 **수십 개로 잘게 쪼개져** 있습니다. 여기에 고유명사 위주의 어인정 주제가 별도 계열로 붙습니다.

즉 **주제는 큰 카테고리가 아니라 태그**입니다. 이것이 확장성·유연성의 실체입니다.

**문제 하나 — 잘게 쪼개면 주제당 문항이 모자랍니다.** 하한 게이트가 주제당 80문항(난이도당 20)이므로 주제 30개면 2,400문항이 필요하고, 수동 생성(D-14)으로는 불가능합니다.

**복수 부착이 이 문제를 해소합니다.** `questions.topicIds`는 배열입니다.

| 문항 | 붙는 주제 |
|---|---|
| "배추를 양념에 절여 숙성시킨 한국의 대표 반찬은?" → 김치 | `음식`, `한국` |
| "원소기호 O는?" → 산소 | `과학`, `화학` |
| "축구 한 팀의 선수는 몇 명?" → 11 | `스포츠`, `축구` |

**효과 세 가지**

1. **문항 하나가 여러 주제 풀에 동시에 들어갑니다.** 240문항에 태그를 평균 2.5개씩 붙이면 주제-문항 연결이 600개가 되어 **3개가 아니라 4~7개 주제를 활성화**할 수 있습니다
2. **넓은 태그와 좁은 태그를 함께 붙일 수 있습니다.** `과학`은 문항이 빨리 모여 먼저 활성되고 `화학`은 나중에 쌓여 활성됩니다. **계층 구조 없이 성장 경로가 생깁니다**
3. **하한 게이트(6.6절)가 확장성을 안전하게 받습니다.** 주제를 마음껏 추가해도 문항이 부족하면 자동으로 노출되지 않습니다 — 이미 만들어둔 장치가 그대로 작동합니다

**MVP 시드 태그**

| 구분 | 태그 | 목표 |
|---|---|---|
| **넓은 태그** | **과학 · 지리 · 스포츠** | 3개 모두 활성 (난이도당 20문항 이상) |
| 좁은 태그 | 화학 · 천문 · 인체 · 국가 · 수도 · 축구 · 야구 · 올림픽 등 | **목표 없음.** 문항이 쌓이는 대로 자동 활성 |

> **좁은 태그에 목표를 두지 않는 이유** — 억지로 채우려 하면 문항이 이상해집니다. **넓은 태그 3개만 확실히 채우고 좁은 태그는 부산물로 얻습니다.** 활성되면 좋고, 안 되면 다음 적재에서 쌓입니다.

**로드맵 태그** (2.2절)
- 4~7단계: 역사 · 세계 문화 · 컴퓨터 · 언어 · 음식 · 동물 · 식물 …
- 8~10단계: 사람의 착각 · 경제 …

> **⚠ 새 주제를 추가할 때 통과시킬 기준 — 정의서로 범위를 좁힐 수 있는가?**
>
> 이전 설계에서 **"생활 상식"이라는 주제가 실패**했습니다(부록 A). 이름이 "나머지 전부"를 뜻해서 정의서를 써도 경계가 그려지지 않았고, 난이도 축도 서지 않았습니다.
>
> **기준은 이름의 길이가 아닙니다.** 정의서에 "다루는 범위"와 "다루지 않는 범위"를 적었을 때 **경계가 그려지는가**입니다. `음식`·`화학`·`축구`는 그려집니다. `생활 상식`·`잡학`·`기타`는 안 그려집니다.

### 6.2 난이도 축 — 인지도 기준 [확정]

MVP는 1~3단계(사실 확인)이므로 문제가 전부 "안다"의 영역에 있습니다. 그러면 난이도를 무엇으로 나누느냐가 문제입니다. **"몇 명이 아는가"로 나눕니다.**

| 난이도 | 기준 | 판정 질문 |
|---|---|---|
| **1** | 거의 모두 아는 사실 | 중학생도 아는가? |
| **2** | 학교에서 배운 사실 | 배운 사람은 아는가? |
| **3** | **착각하기 쉬운 사실** | **많은 사람이 틀리게 알고 있는가?** |
| **4** | 덜 알려진 사실 | 관심 있는 사람만 아는가? |

> **난이도 3을 "착각하기 쉬운 것"으로 잡은 것이 이 설계의 핵심입니다.**
> 캐나다 수도를 토론토로, 호주 수도를 시드니로 아는 사람이 많습니다. 틀렸을 때 **"어? 아니었어?"가 남아서 1~3단계에서도 인사이트가 생깁니다.** 단순 암기 퀴즈와 갈리는 지점이고, 나중에 8단계 통찰형 문항으로 이어지는 씨앗입니다.
>
> 세 주제가 **동일한 축**을 쓰기 때문에, 통합 랭킹(D-7)이 주제 점수를 합산할 때 생기는 **형평성 문제도 함께 완화**됩니다.

### 6.3 주제 정의서 [확정]

> **승인 완료. 이 절이 그대로 Day 3의 생성 프롬프트와 Day 4의 검수 기준이 됩니다.**
>
> **예시문이 왜 결정적인가** — 말로 "난이도 3은 착각하기 쉬운 것"이라고 써도 AI는 감을 못 잡습니다. **예시 3개를 보여주면 패턴을 잡습니다.** 특히 난이도 3은 AI가 스스로 만들지 못하므로(6.5절), 아래 예시문이 **유일하게 확실한 씨앗**입니다. 프롬프트에서 절대 빼지 마십시오.

#### 주제 1 — 과학

| 항목 | 내용 |
|---|---|
| **한 줄** | 학교에서 배운 것 중 기억에 남는 것 |
| **다루는 범위** | 원소기호와 화학식 · 태양계와 천체 · 인체의 기관과 기능 · 물질의 상태 · 기본 단위와 상수 · 생물 분류 |
| **다루지 않는 범위** | **이미 폐기된 교과서 통설** · 최신 연구의 논쟁 지점 · **의학적 조언** · 수식 유도 |
| **허용 태그** | 넓은 `과학` / 좁은 `화학` `물리` `천문` `인체` `생물` `단위` |

| 난이도 | 예시문 | 정답 |
|---|---|---|
| **1** 거의 모두 안다 | 원소기호 `O`는? | 산소 |
| | 물의 화학식은? | H₂O |
| | 태양계에서 지구는 몇 번째 행성? | 세 번째 |
| **2** 학교에서 배웠다 | 원소기호 `Fe`는? | 철 |
| | 태양계에서 가장 큰 행성은? | 목성 |
| | 식물이 빛으로 양분을 만드는 과정은? | 광합성 |
| **3 착각한다 ★** | 사람 몸에서 가장 큰 장기는? | **피부** — "장기"를 내장으로만 떠올림 |
| | 고래는 어떤 동물 분류에 속하는가? | **포유류** — 물에 살고 생김새가 물고기 같음 |
| | 다이아몬드를 이루는 원소는? | **탄소** — 값비싼 보석이라 특별한 물질일 거라 짐작 |
| **4** 관심 있으면 안다 | 원소기호 `Sn`은? | 주석 |
| | 원소기호 `K`는? | 칼륨 |
| | 성인의 뼈는 모두 몇 개? | 206 |
| **숫자 입력** | 물의 끓는점은 섭씨 몇 도? | 100 |
| | 산소의 원자번호는? | 8 |

#### 주제 2 — 지리

| 항목 | 내용 |
|---|---|
| **한 줄** | 세계가 어떻게 생겼는지 |
| **다루는 범위** | 국가와 수도 · 대륙과 해양 · 지형(강·산·사막) · 국기와 국명 · 시차 |
| **다루지 않는 범위** | **영유권 분쟁 지역의 귀속** · **측정 기준에 따라 답이 바뀌는 순위**(가장 긴 강 등) · 수시로 변하는 통계(정확한 인구수) |
| **허용 태그** | 넓은 `지리` / 좁은 `국가` `수도` `지형` `국기` `시차` |

| 난이도 | 예시문 | 정답 |
|---|---|---|
| **1** 거의 모두 안다 | 한국의 수도는? | 서울 |
| | 일본의 수도는? | 도쿄 |
| | 프랑스의 수도는? | 파리 |
| **2** 학교에서 배웠다 | 세계에서 가장 큰 대륙은? | 아시아 |
| | 이집트를 흐르는 큰 강의 이름은? | 나일강 |
| | 대한민국에서 가장 큰 섬은? | 제주도 |
| **3 착각한다 ★** | 캐나다의 수도는? | **오타와** — 토론토·밴쿠버가 더 유명함 |
| | 호주의 수도는? | **캔버라** — 시드니가 압도적으로 유명함 |
| | 튀르키예의 수도는? | **앙카라** — 이스탄불이 더 유명함 |
| **4** 관심 있으면 안다 | 세계에서 가장 작은 나라는? | 바티칸 |
| | 아시아와 유럽 두 대륙에 걸친 나라는? | 튀르키예 |
| | 국기에 나무가 그려진 나라는? | 레바논 |
| **숫자 입력** | 한국과 영국의 시차는 몇 시간? | 9 |
| | 한국의 표준시는 UTC+몇? | 9 |

> **난이도 3이 셋 다 같은 패턴인 것은 의도입니다.**
> 지리에서 난이도 3은 **"더 유명한 도시가 수도라고 착각"**이 압도적으로 강한 유형입니다. 이 패턴을 씨앗으로 주면 AI가 유사 문항(스위스→베른, 미국→워싱턴 D.C., 브라질→브라질리아)을 잘 찾아옵니다.

#### 주제 3 — 스포츠

| 항목 | 내용 |
|---|---|
| **한 줄** | 규칙을 알면 보는 재미가 달라진다 |
| **다루는 범위** | 종목별 **공식 규칙**(인원 · 경기 시간 · 점수 체계 · 경기장과 용구 규격) · 올림픽·월드컵의 기본 사실 · 종목의 기본 용어 |
| **다루지 않는 범위** | **선수 개인 기록·이적·연봉** · 최신 시즌 성적과 순위 · "역대 최고" 같은 주관적 평가 · 도핑·논란 사안 |
| **허용 태그** | 넓은 `스포츠` / 좁은 `축구` `야구` `구기` `올림픽` `기록` |

| 난이도 | 예시문 | 정답 |
|---|---|---|
| **1** 거의 모두 안다 | 축구 한 팀의 선수는 몇 명? | 11 |
| | 농구 한 팀이 코트에 세우는 인원은? | 5 |
| | 올림픽은 몇 년마다 열리나? | 4 |
| **2** 학교에서 배웠다 | 야구 정규 경기는 몇 이닝? | 9 |
| | 축구 전반전은 몇 분? | 45 |
| | 마라톤 정식 거리는? | 42.195km |
| **3 착각한다 ★** | 배드민턴 한 게임은 몇 점 내기? | **21** — 15점제로 배웠음 (2006년 개정) |
| | 탁구 한 게임은 몇 점 내기? | **11** — 21점제로 배웠음 (2001년 개정) |
| | 배구 한 세트는 몇 점 내기? | **25** — 15점 서브권제로 배웠음 |
| **4** 관심 있으면 안다 | 컬링 한 팀은 몇 명? | 4 |
| | 럭비(15인제) 한 팀은 몇 명? | 15 |
| | 야구에서 투수판과 홈플레이트 사이 거리는 약 몇 m? | 18.44 |
| **숫자 입력** | 마라톤 정식 거리는 몇 km? | 42.195 |
| | 탁구 한 게임은 몇 점 내기? | 11 |

> **난이도 3이 전부 "규칙 개정" 유형입니다.** 6.4절 ④에서 지적한 대로 스포츠에서 가장 강한 난이도 3 소재입니다. 옛 규칙으로 배운 세대가 많아 정답률이 확실히 떨어집니다.

> **"선수 기록을 배제한 것"이 두 문제를 동시에 해결합니다**
> 1. **문항이 안 썩습니다.** 기록·순위·이적은 시즌마다 바뀌어 반년 뒤 틀린 문항이 됩니다. 규칙은 안 바뀝니다.
> 2. **스포츠 팬이 아니어도 풀 수 있습니다.** 이 주제의 유일한 약점이 관심 편차인데, "축구는 11명"은 팬이 아니어도 압니다. 반면 "○○ 선수의 통산 홈런"은 팬만 압니다. 규칙 위주로 한정한 것이 곧 완화책입니다.

#### ⚠ 승인 시 함께 검토했고 **그대로 두기로 한** 세 가지

정의서를 승인할 때 아래 세 지점이 지적됐고, **씨앗을 바꾸지 않고 진행**하기로 했습니다. 대응은 이미 파이프라인에 있으므로 새로 만들 것은 없습니다. **Day 4 검수 때 이 세 가지를 특히 보십시오.**

| # | 지적된 지점 | 그대로 두는 근거 / 어디서 잡히는가 |
|---|---|---|
| ① | 과학 난이도 4에 원소기호가 둘(`Sn`·`K`) — AI가 원소기호만 잔뜩 만들 수 있음 | 검수 단계에서 유형 편중이 보이면 반려하고 재요청. **문항 다양성은 프롬프트 반복 시 조정 가능**하므로 씨앗을 바꿀 필요 없음 |
| ② | "가장 ~한 것" 문항이 여럿 — AI가 흉내내면 "가장 긴 강"처럼 답이 갈리는 문항이 나옴 | **6.4절 ③의 자동 검수 플래그가 이미 잡습니다.** "가장/최고/제일"이 들어간 문항은 전부 사람 검수로 넘어가므로 통과할 수 없음 |
| ③ | 스포츠 난이도 3이 전부 "몇 점 내기" — 4지선다로 내면 선택지(11/15/21/25)가 서로 답을 알려줌 | **이 세 문항은 `NUMERIC_INPUT`으로 배정하면 선택지가 없어 문제가 사라집니다.** 정답이 숫자라 유형 전환도 자연스러움 |

### 6.4 검수 기준 — 반드시 반영할 함정 4개

**① 난이도 3과 "반려 대상"을 혼동하지 말 것 — 가장 중요**

난이도 3은 **"정답은 명확한데 많은 사람이 틀리게 아는 것"**입니다. 정답 자체가 논쟁적이거나 이미 폐기된 것은 난이도 3이 아니라 **무조건 반려**입니다. 이 둘은 겉보기에 비슷해서 검수 중 가장 헷갈립니다.

| 주제 | 난이도 3 (채택) | 반려 |
|---|---|---|
| 과학 | 가장 큰 장기 = 피부 | 혀의 부위별 미각 지도 — 이미 폐기된 통설 |
| 지리 | 캐나다의 수도 = 오타와 | "세계에서 가장 긴 강" — 나일/아마존, 측정 기준에 따라 답이 다름 |
| 스포츠 | 배드민턴 = 21점제 (많은 사람이 15점으로 안다) | "역대 최고의 선수는?" — 주관적 / "○○의 통산 기록" — 시즌마다 변함 |

> **판정 기준 한 줄: "공식 자료 한 곳을 펴서 확인할 수 있는가?"** 안 되면 반려입니다.

**② AI가 이 세 주제에서 특히 잘 틀리는 것**

| 주제 | 전형적 오류 |
|---|---|
| 과학 | **폐기된 교과서 통설을 사실로 서술** — 뇌 10% 설, 혀 미각 지도, 명왕성의 행성 지위 |
| 지리 | 변경된 국명을 반영 못 함(터키 → **튀르키예**), 분쟁 지역의 귀속을 단정, 오래된 인구·GDP 수치 |
| 스포츠 | **개정 전 규칙을 정답으로 내놓음** (배드민턴 15점제, 배구 서브권제), 리그마다 다른 규정을 하나로 단정 |

**③ "가장 ~한 것" 문항은 기준을 명시하거나 배제**

최상급 표현은 측정 기준에 따라 답이 바뀝니다. "가장 큰 나라"는 면적인지 인구인지, "가장 높은 산"은 해수면 기준인지에 따라 답이 다릅니다. **기준을 문항 안에 써넣거나, 못 쓰면 버립니다.**

> 자동 검증기가 부분적으로 도울 수 있습니다 [제안] — 문항에 "가장", "최고", "제일"이 들어가면 **자동으로 사람 검수 플래그**를 세워 놓치지 않게 합니다.

**④ 스포츠 — 리그·대회마다 규정이 다름**

같은 종목도 리그에 따라 규정이 갈립니다. 야구 연장 이닝(KBO vs MLB), 축구 교체 인원, 배구 세트 수 등입니다. AI는 이걸 하나로 단정합니다.

> **대응**: 문항에 **"국제 공식 규정 기준"**을 명시하거나, **리그 간 차이가 없는 항목만** 씁니다. "축구 한 팀 11명"은 어디서나 같지만 "연장은 몇 분"은 대회마다 다릅니다.
>
> **한편 규칙 개정 이력은 난이도 3의 최고 소재입니다.** "규칙이 개정됐다 = 많은 사람이 옛 정보로 안다 = 착각하기 쉬운 것"입니다. ①의 반려 기준과 헷갈리지 마십시오 — **현재 공식 규정이 명확하면 채택**이고, 현재 규정 자체가 리그마다 갈리면 반려입니다.

**⑤ 태그가 심하게 겹치는 주제는 합칠 것 — D-16이 만든 새 함정**

문항이 여러 주제에 속하므로(D-16), 두 주제의 문항 집합이 거의 같아질 수 있습니다. 예를 들어 `음식`과 `요리`, `스포츠`와 `체육`을 둘 다 만들면 같은 문항이 양쪽에 들어갑니다.

**왜 문제인가** — 폭 보상형 랭킹(D-7)은 "여러 분야를 경험하면 점수가 오른다"는 전제로 작동합니다. 그런데 겹치는 주제 두 개는 **같은 문항을 두 번 풀고 두 주제의 점수를 얻는** 것이 되어, 폭이 아니라 중복이 보상됩니다. 기획 의도가 깨집니다.

> **대응** [제안]: 자동 검증기가 **주제 쌍의 문항 겹침 비율**을 계산해, 90%를 넘으면 경고합니다. 그때는 두 주제를 하나로 합칩니다. 계산은 `topicIds` 배열만 훑으면 되므로 비용이 거의 없습니다.
>
> 겹침 자체는 정상입니다 — `과학`과 `화학`은 당연히 겹치고, 그게 넓은 태그·좁은 태그 구조의 의도입니다. **문제는 90%를 넘는 경우**, 즉 사실상 같은 주제를 이름만 둘로 쓴 경우입니다.

### 6.5 ⚠ 난이도 3은 AI가 만들지 못합니다

**AI는 "사람들이 무엇을 착각하는지" 모릅니다.** 착각은 통계적 사실이 아니라 문화적 현상이고, AI의 학습 데이터에 "이걸 많이 틀린다"는 라벨이 붙어 있지 않습니다.

따라서 **난이도 3 문항은 생성물에서 기대하면 안 됩니다.** 사람이 직접 골라 배치해야 합니다.

> **이것이 11장 Q1에서 난이도 3 예시문을 특히 강조하는 이유입니다.** 사람이 준 예시 3개가 씨앗이 되면 AI가 유사 패턴을 찾을 수 있습니다. 씨앗이 없으면 이 난이도가 통째로 비고, **게임에서 가장 재미있는 구간이 사라집니다.**
>
> Day 4의 여유 시간 1순위 용도로도 배정했습니다(8장).

### 6.6 주제 등록 파이프라인 ★ 핵심 제작 시스템

**주제의 생애주기**

```
  draft   →  문제 적재 중. 게임에 노출되지 않음
    ↓  (난이도 1~4 각각 20문항 이상 확보)
  active  →  게임에 노출. 플레이 가능
    ↓
  archived → 노출 중단. 기존 기록은 보존
```

> **⚠ 하한선 자동 게이트 — "무너지지 않는 기둥"의 실체**
> 난이도 등급 중 하나라도 20문항 미만이면 주제는 `active`가 될 수 없습니다.
> 이게 없으면 문항이 부족한 주제에서 스테이지 7에 진입했을 때 **출제할 문제가 없어 게임이 멈춥니다.** 주제를 채우다 말아도 게임이 깨지지 않게 하는 장치입니다.

**파이프라인**

```
  [1] 주제 정의서 작성 (사람)
        - 이름 / 한 줄 설명 / 범위 / 비범위 / 난이도 1~4 정의 / 난이도별 예시문
        ↓
  [2] AI 배치 생성 — 채팅 UI에 수동 요청 (D-14)
        - 프롬프트 템플릿에 {주제, 난이도, 개수, 예시문, 허용 태그 목록}을 채워 붙여넣기
        - JSON으로 받아 파일에 저장 → 1회당 20~30문항
        - 문항마다 topicIds 를 함께 받음 (넓은 태그 1개 + 좁은 태그 1~2개)
        ↓
  [3] 자동 검증 (기계가 잡을 수 있는 것)
        - JSON 형식 / 선택지 4개 / 정답이 선택지 안에 존재
        - 수식 문제라면 계산 결과가 실제로 정답과 일치하는가
        - 같은 주제 내 중복
        - "가장/최고/제일" 포함 시 검수 플래그
        - topicIds 가 허용 태그 목록 안에 있는가 (오타·신조 태그 차단)
        - 주제 쌍의 문항 겹침이 90%를 넘는가 (6.4절 ⑤)
        ↓
  [4] 사람 검수 (기계가 못 잡는 것)
        - 정답이 진짜 정답인가 → 공식 자료 한 곳으로 확인
        - 오답 선택지가 그럴듯한가 (너무 허술하면 정답이 보임)
        - 난이도가 정의서 기준과 맞는가
        - 6.4절 함정 4개에 걸리지 않는가
        ↓
  [5] 승인 → Firestore 적재 · questionCount 갱신
      반려 → rejectReason 기록
        ↓
  [6] 하한 충족 판정 → 충족 시 status: draft → active
        ↓
      게임에 노출
```

**[2] 생성 단계의 원칙** [제안]

- **프롬프트를 텍스트 파일로 만들어 재사용합니다.** 매번 손으로 쓰면 주제·난이도별 지시가 달라져 품질이 들쭉날쭉해집니다. `{주제}`·`{난이도}`·`{개수}`만 바꿔 끼우는 템플릿 하나를 Day 3에 만듭니다.
- **주제 정의서(6.3절)를 프롬프트에 그대로 붙여넣습니다.** 특히 **난이도 정의와 예시문**을 빼면 AI는 난이도를 임의로 해석합니다.
- **출력은 반드시 JSON으로 지시합니다.** 형식이 흔들리면 붙여넣기 자동 처리가 깨집니다.
- **태그는 허용 목록에서만 고르게 합니다.** 자유롭게 붙이라고 하면 AI가 `자연과학`·`화학기초`·`케미스트리` 같은 변종을 만들어 주제가 파편화됩니다. 프롬프트에 **쓸 수 있는 태그 목록을 명시**하고, 자동 검증에서 목록 밖 태그를 거부합니다.
- **정답은 완전한 문장이 아니라 단어 형태로만 쓰게 합니다.** `"김치입니다"` ❌ → `"김치"` ⭕. 지금 비용 0이고, 나중에 단답 유형(4.4절)으로 전환할 때 **240문항을 다시 보지 않아도 됩니다.**
- **"1.3배 선생성" 전략을 쓰지 않습니다.** 240문항을 먼저 받고 **반려된 만큼만 추가 요청**합니다.
  *이유: 자동 생성이면 한 번에 많이 뽑는 게 싸지만, 수동에서는 **반복 횟수가 비용**입니다. 쓰지도 않을 70문항을 미리 받으면 붙여넣기 3~4회를 헛수고합니다.*

**[5]의 `rejectReason`을 계속 기록하는 이유** [제안]
필드 하나 값이고, **"어떤 주제에서 AI 생성 품질이 떨어지는가"**를 알려주므로 다음 주제 적재 시 프롬프트 개선에 직접 쓰입니다.

**시드 적재 목표** [확정]

| 넓은 태그 | 목표 문항 | 붙여넣기 | 검수 추정 |
|---|---|---|---|
| 과학 | 80 | 3~4회 | **~50분** |
| 지리 | 80 | 3~4회 | **~50분** |
| 스포츠 | 80 | 3~4회 | **~60분** |
| **합계** | **240** | **~10회 + 보충** | **약 3시간** |

> **문항 수는 240 그대로지만 활성 주제는 3개보다 많아집니다.** D-16으로 문항마다 좁은 태그가 1~2개 더 붙으므로, 운이 좋으면 `화학`·`수도`·`축구` 같은 주제가 하한을 넘겨 함께 활성됩니다. **목표로 삼지는 않습니다**(6.1절) — 부산물입니다.

> **수동 생성 작업량**: 1회당 20~30문항이므로 240문항이면 **약 10회 반복**, 여기에 반려분 보충이 붙습니다. 붙여넣기 자체는 **1~2시간** 정도이고 Day 3 후반과 Day 4 앞부분에 나눠 들어갑니다.
>
> 검수가 3시간으로 짧은 이유는 세 가지입니다. **1~3단계 사실 확인은 공식 자료 한 곳으로 확인이 끝나고**, `NUMERIC_INPUT`(주제당 16~20개)은 자동 검증되어 문항당 15초이며, 세 주제 모두 정답이 시간에 따라 변하지 않습니다.

> **API 키가 없어서 생기는 이득이 하나 있습니다**
> 자동 생성이면 문항이 몇 분 만에 쏟아지고 사람은 **결과만** 봅니다. 수동이면 20~30문항씩 나오는 것을 **눈으로 보면서** 진행하므로, 프롬프트가 잘못됐다는 걸 3번째 요청에서 알아채고 바로 고칠 수 있습니다. 자동화라면 다 뽑은 뒤에 알게 됩니다.
>
> 이건 목적 층 2(1.1절)와도 맞물립니다 — AI가 무엇을 잘 만들고 무엇을 못 만드는지가 생성 과정에서 바로 보입니다.

---

## 7. 기술 아키텍처

### 7.1 구성 요소 [v1.5 변경 — Firebase → Cloudflare]

| 계층 | 선택 | 역할 |
|---|---|---|
| 화면 | React + TypeScript (Vite) | 게임 UI |
| 인증 | **Cloudflare Workers + D1 (자체 구현)** | 익명 uid 발급. 소셜 로그인·계정 승계 방식은 **미결정 — Q5' 로 이관** |
| 데이터 | **Cloudflare D1 (SQLite)** | 주제·문제·기록·랭킹 |
| 배포 | **Cloudflare Workers Static Assets** | 링크 공유 |
| 문제 생성 | **채팅 UI + 프롬프트 템플릿 파일** (수동) | D-14 |
| 취합·검증 | 로컬 Node.js 스크립트 | 붙여넣은 JSON을 읽어 기계 검증 |
| 검수 도구 | 로컬 웹 페이지 (간이) | 승인/반려 클릭 |

> **Firebase는 EP-5 저장소 계층 뒤에 격리된 채로 가능성만 열어둡니다.** 지금은 전혀 쓰지 않습니다. 아래 7.2~7.6절은 v1.4의 Firestore/Firebase 서술을 Cloudflare D1/Workers 기준으로 재작성한 것입니다.

### 7.2 데이터 모델 (Cloudflare D1 — SQLite)

**여기가 잘못되면 나중에 전부 갈아엎어야 합니다.**

```sql
-- ★ D-5 · D-15 · D-16
CREATE TABLE topics (
  id            TEXT PRIMARY KEY,
  no            INTEGER,                   -- 목록 정렬 키
  name          TEXT NOT NULL,             -- "과학" | "화학" | "지리" | "축구" ...
  kind          TEXT CHECK(kind IN ('broad','narrow')),  -- 넓은/좁은 태그 (6.1절)
  tagline       TEXT,
  scope_json         TEXT,                 -- 다루는 범위 (JSON 배열 문자열)
  out_of_scope_json  TEXT,                 -- 다루지 않는 범위
  difficulty_spec_json TEXT,               -- 난이도 정의 + 예시문 (검수·생성 기준)
  status        TEXT CHECK(status IN ('draft','active','archived')) DEFAULT 'draft',
  q_count_1 INTEGER DEFAULT 0, q_count_2 INTEGER DEFAULT 0,
  q_count_3 INTEGER DEFAULT 0, q_count_4 INTEGER DEFAULT 0,
  created_at TEXT, updated_at TEXT
);

CREATE TABLE questions (
  id            TEXT PRIMARY KEY,
  type          TEXT CHECK(type IN ('MULTIPLE_CHOICE','NUMERIC_INPUT')),  -- ★ EP-1
  difficulty    INTEGER CHECK(difficulty BETWEEN 1 AND 4),
  body          TEXT NOT NULL,
  choices_json  TEXT,                      -- 선택지 배열 (NUMERIC_INPUT이면 NULL)
  answer        TEXT NOT NULL,
  explanation   TEXT NOT NULL,
  status        TEXT CHECK(status IN ('pending','approved','rejected')) DEFAULT 'pending',
  source        TEXT CHECK(source IN ('ai_generated','manual')),
  generated_by  TEXT,
  reject_reason TEXT,
  created_at    TEXT
);

-- ★ EP-2 · D-16 — 문항 하나가 여러 주제에 속함 (넓은 1개 + 좁은 1~2개)
CREATE TABLE question_topics (
  question_id TEXT REFERENCES questions(id),
  topic_id    TEXT REFERENCES topics(id),
  PRIMARY KEY (question_id, topic_id)
);

CREATE TABLE users (
  uid           TEXT PRIMARY KEY,
  nickname      TEXT,                      -- 랭킹 등록 전엔 NULL
  is_anonymous  INTEGER NOT NULL DEFAULT 1,
  global_score  INTEGER DEFAULT 0,         -- = Σ topic_best.score (트리거 또는 재계산으로 유지)
  play_count    INTEGER DEFAULT 0,
  created_at TEXT, updated_at TEXT
);

-- ★ 폭 보상형 랭킹의 원천 (Firestore 맵 → 관계형 테이블로 분해)
CREATE TABLE topic_best (
  uid       TEXT REFERENCES users(uid),
  topic_id  TEXT REFERENCES topics(id),
  score     INTEGER NOT NULL,
  stage     INTEGER NOT NULL,
  updated_at TEXT,
  PRIMARY KEY (uid, topic_id)
);

-- 주간 보드용 (weekId 바뀌면 별도 행으로 누적, 5.2절 EP-3)
CREATE TABLE topic_best_weekly (
  uid       TEXT REFERENCES users(uid),
  topic_id  TEXT REFERENCES topics(id),
  week_id   TEXT NOT NULL,
  score     INTEGER NOT NULL,
  updated_at TEXT,
  PRIMARY KEY (uid, topic_id, week_id)
);

CREATE TABLE runs (                         -- 한 판의 기록
  id             TEXT PRIMARY KEY,
  uid            TEXT REFERENCES users(uid),
  topic_id       TEXT REFERENCES topics(id),  -- 단수 — 주제 1개 선택 (D-6)
  mode           TEXT DEFAULT 'solo',       -- ★ EP-4: "battle"이 들어올 자리
  stage_reached  INTEGER,
  score          INTEGER,
  answers_json   TEXT,                      -- [{questionId, given, correct, elapsedMs}]
  started_at TEXT, ended_at TEXT,
  status TEXT CHECK(status IN ('completed','abandoned'))
);

-- 5.3절 비용 대응 · 상위 30 (D-9). D1은 읽기 자체가 저렴하므로
-- Firestore만큼 절박한 문제는 아니지만, 정렬 쿼리 비용 절감을 위해 유지.
CREATE TABLE ranking_cache (
  board_id      TEXT PRIMARY KEY,
  top_json      TEXT,                      -- [{rank, uid, nickname, score}]
  cutoff_score  INTEGER,
  generated_at  TEXT
);
```

**설계 의도 세 가지**

1. **`topic_best`를 별도 테이블로 분해한 이유** — Firestore의 `topicBest` 맵을 그대로 옮기면 SQLite에 JSON 컬럼으로 두는 방법도 있었지만, **관계형으로 쪼개면 "주제별 최고점 정렬" 같은 쿼리를 SQL이 직접 처리**합니다. `SUM(score) GROUP BY uid`가 곧 통합 점수 계산입니다.
2. **`ranking_cache` 테이블이 남아있는 이유** — D1도 요청당 과금(무료 등급은 일일 읽기 한도)이므로, 랭킹 페이지 하나가 매번 `topic_best`를 정렬 조회하는 것보다 **미리 합쳐 캐싱**하는 게 여전히 유리합니다 (7.6절).
3. **`questions`↔`topics`가 다대다, `runs.topic_id`는 단수인 이유** — 문항은 여러 주제에 속하지만(D-16) **한 판은 주제 하나로 플레이**합니다(D-6). 출제 쿼리는 `question_topics`를 조인해 `topic_id`로 필터링하고, 문항을 승인할 때는 **연결된 모든 주제의 `q_count_*`를 함께 올립니다.**

### 7.3 확장점 (Extension Points)

"기둥을 강하게" 요구에 대한 직접적인 답입니다. **지금 구현하지 않되, 나중에 붙일 때 기존 것을 부수지 않도록 자리만 만듭니다.**

| ID | 확장점 | 지금 하는 일 | 나중에 붙일 것 |
|---|---|---|---|
| **EP-1** | 문제 유형 | `type` 필드 + 유형별 렌더링 분기 | 서술형, 순서배열, 매칭 |
| **EP-2** | 주제 | `topics` 테이블 + 생애주기 + 하한 게이트 | 주제 무제한 추가 (**이미 열려 있음**) |
| **EP-3** | 랭킹 보드 | 보드를 `boardId` 문자열로 일반화 | 일간·친구·주제별 주간 보드 |
| **EP-4** | 게임 모드 | `runs.mode` (지금은 `"solo"` 고정) | 실시간 배틀, 혼합 주제 모드 |
| **EP-5** | 백엔드 교체 | 데이터 접근을 **저장소 계층 한 곳에 격리** | Firebase 등 다른 백엔드로 전환 |

> **EP-5가 가장 중요합니다.** 화면 코드가 Worker API를 직접 fetch하지 않고, `getTopics()`·`getQuestions()`·`saveRun()` 같은 함수를 모은 파일 하나를 두고 화면은 그것만 부르게 하면, **그 파일 하나만 바꾸면 백엔드가 교체**됩니다. Firebase는 지금 쓰지 않지만, 이 격리 덕분에 나중에 필요하면(예: 특정 기능만 Firebase로) 화면을 뜯지 않고 붙일 수 있습니다.

### 7.4 실시간 확장 경로 — Durable Objects (지금은 실행 안 함)

실시간 배틀(EP-4)이 필요해지는 시점에 D1에서 부족한 것은 **실시간 상태 동기화**이며, **Cloudflare Durable Objects**가 이 용도에 적합합니다. 이미 백엔드가 Cloudflare이므로 v1.4 때처럼 "전면 이전이냐 부분 추가냐"를 고민할 필요가 없습니다 — **같은 플랫폼 안에서 Durable Object 하나를 추가**하면 됩니다.

→ EP-5 저장소 계층에 배틀 함수만 추가하면 되고 기존 코드는 손대지 않습니다. 실시간 기능 착수 시점에 별도 검토합니다. **지금 결정할 사항이 아닙니다.**

### 7.5 부정행위(치팅) 대응 — ⚠ 중요

**정직하게 말씀드립니다: 웹 클라이언트에서 완전한 방지는 불가능합니다.** 구멍이 둘입니다.

1. 정답이 클라이언트에 내려갑니다. 브라우저 개발자 도구로 볼 수 있습니다
2. 점수를 클라이언트가 계산해 DB에 씁니다. 조작한 값을 보낼 수 있습니다

| 안 | 방법 | 비용 | 방어 |
|---|---|---|---|
| **A** | **Cloudflare Workers가 채점·점수 계산** | 0원 (무료 등급 넉넉) — **카드 등록 불필요** | 높음 |
| **B** | 클라이언트 신뢰 + Worker에서 최소 검증만 | 0원 | 낮음 |

> **[제안] MVP는 A.**
> *근거: Firebase였다면 A(Cloud Functions)는 Blaze 요금제(카드 등록)가 필요해 B로 타협했지만, **Cloudflare Workers는 애초에 API 요청이 서버(Worker)를 거치므로 채점 로직을 서버에 두는 것이 오히려 자연스럽고 추가 비용도 없습니다.** 클라이언트는 답만 보내고, Worker가 정답을 대조해 점수를 계산합니다.*
> 다만 만점이 1,500점으로 확정되어 있으므로, **Worker 쪽에 "판 점수 ≤ 1,500" 상한 검증**은 유지합니다 → 11장 Q3

### 7.6 Cloudflare 무료 등급

플레이어 수십~수백 명 규모에서는 Workers/D1 무료 등급으로 넉넉합니다. 단 5.3절 캐싱을 하지 않으면 랭킹 페이지 하나로 D1 일일 읽기 한도를 태울 수 있습니다.

> **[할 일] Day 1에 Cloudflare 공식 요금 페이지에서 Workers/D1 현재 한도 수치를 직접 확인할 것.** 한도는 시기에 따라 바뀌며, **이 문서의 추정을 신뢰하지 마십시오.**

---

## 8. 7일 개발 일정

**설계 원칙 — Day 2에 "수직 슬라이스"를 관통시킵니다.**
수직 슬라이스란 화면 → DB → 화면을 **끝에서 끝까지 한 번 뚫는 것**입니다. 초보자의 가장 흔한 실패는 각 부분을 따로 잘 만들어놓고 6일차에 합치려다 안 붙는 것입니다. 이틀차에 뚫어두면 이후엔 통로를 넓히는 작업만 남습니다.

| Day | 목표 | 하는 일 | 완료 판정 |
|---|---|---|---|
| **1** | 토대 | 개발환경, React 프로젝트, **Cloudflare 계정·Workers·D1 생성·연결**, 자체 익명 uid 발급. **+ 7.6절 요금 한도 확인** | 화면에 내 `uid`가 뜬다 |
| **2** | **수직 슬라이스** ★ | 손으로 만든 주제 1개 + 문제 10개를 **D1**에 넣고, 불러와 1판 플레이 → 결과 저장 | 문제를 풀고 결과가 DB에 남는다 |
| **3** | **등록 파이프라인** ★ | 주제 정의서 적재, **프롬프트 템플릿 파일**, 붙여넣은 JSON 취합·검증기, 검수 화면, 하한 게이트. **주제 1개로 [1]~[6] 전 과정 관통 검증** | 주제 1개가 `draft`→`active`로 자동 전환된다 |
| **4** | 콘텐츠 | 나머지 2개 주제 생성 + 240문항 검수·적재 + **난이도 3 문항 직접 배치** | 활성 주제 3개 확보 |
| **5** | 게임성 | 주제 선택 화면, 스테이지 진행, 난이도 매핑, 점수, 해설, 결과 화면 | 주제를 골라 12스테이지까지 갈 수 있다 |
| **6** | 랭킹 | 소셜 로그인, **계정 승계**, 닉네임, 통합 보드 2개 + 주제별 순위, 캐시 | 로그인해도 기록이 살아있고 랭킹에 뜬다 |
| **7** | 마감 | 배포, 다른 기기 테스트, 버그 수정, **버퍼** | 남에게 링크를 보낼 수 있다 |

> **Day 3의 성격** — "문제 만드는 도구"를 만드는 날이 아니라 **게임의 핵심 제작 시스템을 관통 검증하는 날**입니다. 여기서 주제 1개가 끝까지 흐르는 것을 확인해야 Day 4에 나머지 2개를 안심하고 돌릴 수 있습니다.
>
> **Day 3 착수에는 6.3절 주제 정의서 상세가 필요합니다** → 11장 Q1. Day 1~2는 막히지 않습니다.

### 일정 리스크와 대응

| 리스크 | 확률 | 대응 |
|---|---|---|
| Day 1이 환경 설정만으로 다 감 (초보자에게 흔함) | 중 | Day 7 버퍼를 당겨 씀. **Day 2 수직 슬라이스는 절대 미루지 않음** |
| **난이도 3 문항이 안 모임** | **높음** | **AI는 "사람들이 무엇을 착각하는지" 모릅니다**(6.5절). 정의서에 예시문 3개를 씨앗으로 넣고, Day 4 여유 시간에 사람이 직접 골라 배치 |
| 수동 생성(D-14) 반복이 지겨워 대충 넘어감 | 중 | 붙여넣기 10여 회는 단조롭습니다. **프롬프트 템플릿을 Day 3에 확실히 만들어** 매번 다시 쓰지 않게 하고, 주제별로 나눠 하루에 몰지 않음 |
| Day 4 검수가 8시간 초과 | 낮음 (추정 3시간) | 초과 시 "스포츠"을 먼저 줄임 |
| Day 6 계정 승계에서 막힘 | 중 | 최악의 경우 "랭킹 등록 시 익명 기록 폐기"로 축소 (경고 표시 필수) |
| 범위 확대 | **높음** | 9장을 방패로 씀. 새 아이디어는 구현하지 않고 목록에만 적음 |

> **가장 큰 리스크는 기술이 아니라 범위 확대입니다.** 만들다 보면 "이것도 있으면 좋겠다"가 계속 생깁니다. 7일 제약을 지키는 유일한 방법은 **적어만 두고 넘어가는 것**입니다.

---

## 9. 범위 밖 (Non-Goals)

"안 좋은 아이디어"가 아니라 **"7일 후에 할 좋은 아이디어"**입니다. 적혀 있지 않으면 슬금슬금 들어옵니다.

- **콘텐츠 4~10단계** (2.2절 로드맵). 역사·세계 문화·IT·맞춤법·사람의 착각·돈 등 **주제 추가는 전부 MVP 이후**
- 실시간 퀴즈 배틀 (D-3 → EP-4)
- 혼합 주제 모드 (D-6에서 단일 선택으로 확정 → EP-4)
- 최대 스테이지 확장 (4.2절 — 문항이 쌓인 뒤)
- 런타임 AI 문제 생성 (D-2)
- 서버 사이드 채점 (7.5절 B안 → 나중에 A/C안)
- 시간·콤보 보너스 (5.1절)
- 30위 밖 정확한 등수 표시 (5.3절)
- 주제별 주간 보드 (→ EP-3)
- 랭킹 메뉴에 주제별 보드 나열 (5.2절 — 주제별은 플레이 화면 안에서만)
- 오답 노트, 취약 영역 진단 리포트
- 재화, 아이템, 상점, 뽑기
- 친구, 길드, 채팅
- 업적, 배지, 일일 미션
- 사용자 제보로 문제 신고
- 다국어, 앱스토어 배포

---

## 10. 한 장 요약

> 구현 중 헷갈릴 때 이 표만 보면 됩니다.

| 질문 | 답 |
|---|---|
| 누구를 위해? | **15~20세**, 넓고 얕게 |
| 무슨 기술로? | React + TypeScript + Cloudflare (Workers + D1) |
| 분류 단위는? | **주제 단일 층.** 끄투식으로 잘게 쪼갠 태그 |
| 주제 부착은? | **문항 하나에 여러 주제** (`topicIds` 배열) — 넓은 1개 + 좁은 1~2개 |
| 시드 넓은 태그는? | **과학 · 지리 · 스포츠** (각 80문항, 총 240) |
| 주제 추가 기준은? | **정의서에 범위/비범위를 적었을 때 경계가 그려지는가.** `음식`⭕ `잡학`❌ |
| 한 판은? | 주제 1개 선택 → 스테이지 1~12 → 각 5문항 |
| 클리어는? | 5문항 중 4문항 이상 |
| 점수는? | 난이도 등급 × 10점, 판 만점 1,500점 |
| 통합 랭킹은? | Σ(주제별 최고 판 점수) — 폭 보상형 |
| 랭킹 메뉴는? | **통합 2개만** (전체·주간). 주제별 순위는 플레이 화면 안에서 |
| 랭킹 노출은? | 상위 30위 + 커트라인 |
| 로그인은? | 익명 자동 → 랭킹 등록 시에만 소셜 연결 |
| 주제 활성 조건은? | 난이도당 20문항 이상. 미달이면 게임에 안 나옴 |
| 문제 수준은? | **1~3단계 = 사실 확인.** "한국의 수도는?" 수준 |
| 난이도 1~4는? | **인지도 축.** 1 모두 안다 / 2 배웠다 / **3 착각한다** / 4 덜 알려짐 |
| 수학은? | 주제 아님. 모든 주제에 `NUMERIC_INPUT` 20~25% |
| 문제는 어디서? | **채팅 UI 수동 요청** → 붙여넣기 → 자동 검증 → 사람 검수 |
| 단답 입력은? | **MVP 제외.** 단 정답은 단어 형태로 써서 나중 전환 대비 |
| 검수 판정 기준은? | **"공식 자료 한 곳으로 확인되는가?"** 안 되면 반려 |

---

## 11. 미결정 항목

**Day 1~4를 막는 것은 없습니다.** 아래는 전부 Day 5~6 항목이고 추천안이 있으므로 "추천대로" 한마디로 넘길 수 있습니다.

| # | 질문 | 왜 필요한가 | 막는 시점 |
|---|---|---|---|
| **Q1** | 스테이지 실패 시 **처음부터 다시**입니까, 이어하기입니까? <br>*추천: 처음부터* | 이어하기면 누구나 결국 12스테이지에 도달해 점수가 다 같아지고 **랭킹이 의미를 잃습니다** | Day 5 |
| **Q2** | 4.2·5.1절 수치를 승인하십니까? (5문항 / 4문항 클리어 / 12스테이지 / 난이도×10 / 만점 1,500) <br>*추천: 일단 승인 후 Day 5에 조정* | 실제로 플레이해보기 전엔 알 수 없고, 코드 한 줄이라 바꾸기 쉽습니다 | Day 5 |
| **Q3** | 부정행위 대응을 7.5절 **A안**(Worker 채점)으로 가는 데 동의하십니까? <br>*추천: A안* | Cloudflare에선 A안이 B안보다 비용·구현 부담이 크지 않습니다(카드 등록 불필요) — v1.4의 "B로 타협" 사유가 사라짐 | Day 5 |
| **Q4** | 5.2절 **랭킹 메뉴는 통합 2개만** 두는 데 동의하십니까? <br>*추천: 동의* | 주제가 태그처럼 잘게 쪼개지므로(D-16) UI 층에서 막지 않으면 랭킹 메뉴가 30개가 됩니다 | Day 6 |
| **Q5'** | 소셜 로그인을 **자체 OAuth 구현**으로 갑니까, **인증 라이브러리**(Lucia 등)로 갑니까? 제공자는? <br>*추천: 라이브러리 + 구글* | Firebase Auth가 없으므로 v1.4의 Q5(제공자만 고르면 됨)보다 결정할 게 늘었습니다. 라이브러리를 쓰면 구현 부담이 크게 줄지만 의존성이 하나 늘어남 | Day 6 |
| **Q6** | 랭킹 캐시 갱신 주기는? <br>*추천: 5분* | 짧을수록 반응성이 좋으나 무료 한도를 태웁니다. 초기에는 차이를 못 느낍니다 | Day 6 |
| **Q7** | 주간 보드를 MVP에 넣습니까? <br>*추천: 넣되 Day 6이 밀리면 첫 번째로 자름* | 전체 기간 보드만 있으면 초기 상위권이 굳어 신규 플레이어가 이탈합니다 | Day 6 |

> **해소된 질문** — 주제 정의서 상세(→6.3절 확정), 시드 주제 선정(→D-11), 주제 부착 방식(→D-16), 단답 유형(→4.4절 MVP 제외), `NUMERIC_INPUT` 비율(→4.4절 확정), 문제 생성 방식(→D-14), 타겟층(→D-10), 콘텐츠 단계(→D-13)

---

## 12. 다음 행동

**Day 1~4를 막는 것은 이제 없습니다.**

1. **Day 1 착수** — 개발환경 → React 프로젝트 → Cloudflare 계정·Workers·D1 생성·연결 → 자체 익명 uid 발급. 완료 판정은 "화면에 내 `uid`가 뜬다".
   - 착수 시 **7.6절의 할 일을 먼저** 처리하십시오: Cloudflare 공식 요금 페이지에서 Workers/D1 현재 무료 한도 수치를 직접 확인.
2. **Day 3 준비물은 전부 갖춰졌습니다.** 6.3절 정의서를 그대로 프롬프트 템플릿에 붙여넣으면 됩니다 — `{주제}`·`{난이도}`·`{개수}`만 바꿔 끼우는 형태로 만들고, **범위·비범위·난이도 정의·예시문 3개·허용 태그 목록**을 함께 넣습니다.
3. **Day 4 검수 때는 6.3절 마지막 표(그대로 두기로 한 세 가지)를 특히 보십시오.**
4. **Q1~Q7은 Day 5 전까지** 정하면 됩니다.

> **이 문서는 살아있는 문서입니다.** Day 5에 직접 플레이해보면 4.2절 수치가 틀렸다는 걸 알게 될 가능성이 높습니다. 그때 **문서를 먼저 고치고 코드를 고치십시오.** 문서와 코드가 어긋나면 그 순간부터 문서는 거짓말이 되고, **거짓말하는 문서는 없는 것보다 나쁩니다.**

---

## 부록 A. 설계 변경 이력

설계가 왜 지금 모양이 됐는지 기록입니다. **같은 실수를 반복하지 않기 위한 자료**입니다.

| 버전 | 무엇이 바뀌었나 | 왜 |
|---|---|---|
| v0.1 | 초안 | 4개 핵심 결정(플랫폼·문제 소스·실시간·인증) 확정 |
| v0.2 | 목적 층 2 재정의 → "AI로 여러 분야를 학습한다" <br>지식 영역이 **1급 엔티티**로 승격 <br>랭킹 이원화(폭 보상형 + 영역별) <br>랭킹 노출 100위 → 30위 | v0.1은 "AI 제작 실습의 교훈"으로 해석했으나, 실제 의도는 **다분야 경험 자체가 콘텐츠**였음. 이에 따라 "MVP 1개 영역" 제안이 폐기되고 등록 구조가 필수가 됨 |
| v0.3 | 타겟 15~20세 확정 <br>시드: 일상 속 과학 / 사람의 착각 / 세계 문화 <br>수학이 영역 → 문제 유형으로 이동 | 15~20세는 교과 피로도가 높아 학교 시험 냄새를 피해야 함. "수학"이라는 이름표가 진입 장벽이 됨 |
| v0.4 | **콘텐츠 10단계 로드맵 도입, MVP는 1~3단계** <br>시드 교체 → 지리 / 과학 / 생활 상식 <br>난이도 축: 인지 사다리 → **인지도 축** | **v0.3의 주제들은 난이도 8 수준이었음.** "넓고 얕은"의 "얕은"을 깊이로만 읽어 난이도를 올려버린 오독. v0.3 설계는 폐기하지 않고 로드맵 8단계로 보존 |
| v0.5 | 문제 생성 **수동** 확정 (API 키 없음) <br>"1.3배 선생성" 폐기 <br>3번째 교체: 생활 상식 → **스포츠** | 수동에서는 **반복 횟수가 비용**이라 선생성이 손해. **"생활 상식"은 이름이 "나머지 전부"를 뜻해 범위가 서지 않았음** — 이름이 범위를 정해주지 못하면 AI도 사람도 기준을 못 잡음 |
| v0.6 | 정의서 골격·`NUMERIC_INPUT` 비율 승인 | Day 1~4 착수 가능 상태 |
| v1.0 | 문서 전체 재작성 | 20여 차례 부분 수정으로 버전 이력이 3층으로 쌓이고 폐기된 결정을 가리키는 문장이 남아 있었음 |
| v1.1 | 용어: 도메인 → 장르 <br>장르 체계를 도서관 분류식 7개 대분류로 <br>장르(분류)와 주제(생성 단위) 분리 | 사용자 정정. 대분류 고정으로 랭킹 보드 수의 상한을 만들려 했음 |
| v1.2 | **장르 폐기 → 주제 단일 층** (D-15) <br>**랭킹 메뉴를 통합 2개로 고정** (5.2절) <br>`topics.difficultySpec`에 예시문 포함 <br>주제 정의서 상세를 미결정으로 승격 (Q1) | 사용자 정정. 2층 구조는 무거웠음. 단 v1.1이 대분류로 막으려 했던 "보드 수 폭증"이 되살아나므로 **데이터가 아니라 UI 층에서** 막음 — 랭킹 메뉴는 통합 2개, 주제별 순위는 플레이 화면 안 |
| v1.3 | **끄투코리아 참조 → 주제를 잘게 쪼갠 태그로** (D-16) <br>**`questions.topicIds` 배열** — 문항 하나가 여러 주제에 속함 <br>넓은 태그(과학·지리·스포츠) + 좁은 태그(화학·수도·축구…) <br>태그 겹침 90% 경고 (6.4절 ⑤) <br>허용 태그 목록 강제 <br>단답 유형은 MVP 제외, 정답 표기만 단답 호환 | 사용자가 끄투 주제 분류를 참고 자료로 제시. 끄투 주제는 **표준국어대사전 「전문 분야」에서 따온 수십 개의 태그**였음. 잘게 쪼개면 주제당 문항이 모자라는데 **복수 부착이 그걸 해소** — 240문항으로 3개가 아니라 4~7개 주제를 활성화. v1.2의 UI 억제 결정이 이제 선택이 아니라 필수가 됨 |
| **v1.4** | **주제 정의서 확정** (6.3절) — 세 주제의 범위·비범위·허용 태그 + **난이도별 예시문 3개씩** <br>지적된 세 지점은 씨앗을 바꾸지 않고 그대로 두되 **어디서 잡히는지 기록** | 예시문이 프롬프트의 유일한 구체적 기준이므로 난이도별 3개가 필요했음. 특히 난이도 3은 AI가 못 만들어(6.5절) 사람이 준 씨앗이 없으면 구간이 통째로 빔. **지적 사항을 씨앗 수정이 아니라 기존 방어 장치로 흡수** — ②는 자동 검수 플래그, ③은 유형 배정으로 해소되므로 새로 만들 것이 없었음 |
| **v1.5** | **백엔드를 Firebase → Cloudflare(Workers + D1)로 전면 전환** (1.2·7장) <br>Firestore 문서 모델 → D1 관계형 스키마로 재작성 (`topic_best`·`question_topics` 등 조인 테이블 도입) <br>7.5절 부정행위 대응 추천안 B→**A**로 변경 (Worker 채점이 카드 등록 없이 가능해짐) <br>Q5 → **Q5'**로 재정의 (소셜 로그인 연결 방식 자체가 새 미결정 항목이 됨) <br>Firebase는 EP-5 뒤로 격리해 **가능성만 유지** | 사용자가 Day 1 작업 도중 방향을 뒤집음 — Firebase는 열어두되 지금은 안 쓰고 Cloudflare로 진행. 이미 별도로 Cloudflare Workers/KV 배포 경험이 있어 전환 비용이 낮았음. **Firebase Auth의 `linkWithCredential`(익명↔소셜 연결)에 대응하는 기성 기능이 Cloudflare엔 없어**, 계정 승계(5.4절)의 구현 난이도가 v1.4보다 올라감 — Day 6 결정 사항으로 명시적으로 이관 |

### 이 과정에서 배운 것 네 가지

1. **정의서로 경계를 그릴 수 없는 분류는 실패합니다.** "생활 상식"이 그랬습니다. 기준은 **이름의 길이가 아니라 "다루는 범위/다루지 않는 범위를 적었을 때 선이 그려지는가"**입니다. `음식`은 짧아도 그려지고, `생활 상식`은 길어도 안 그려집니다. 주제를 추가할 때마다 이 시험을 통과시키십시오.
2. **"얕다"와 "쉽다"는 다릅니다.** v0.3에서 이걸 혼동해 주제 셋을 통째로 교체해야 했습니다.
3. **폐기한 설계는 지우지 말고 로드맵으로 옮기십시오.** v0.3의 세 주제는 틀린 설계가 아니라 **때가 이른 설계**였습니다.
4. **구조를 단순화하면 그 구조가 막아주던 문제가 되살아납니다.** v1.1의 대분류는 랭킹 보드 수를 억제하고 있었습니다. v1.2에서 그것을 없앴으므로 같은 문제를 다른 층(UI)에서 다시 막아야 했습니다. **무엇을 없앨 때는 그것이 무엇을 막고 있었는지 먼저 확인하십시오.**
5. **비슷한 것을 이미 만든 서비스의 데이터 구조를 먼저 보십시오.** v0.2부터 v1.2까지 저는 분류 단위가 3~7개 규모라고 가정하고 다섯 번을 돌았습니다. v1.3에서 끄투코리아의 실제 분류를 열어보니 **수십 개의 태그였고, 그 사실 하나가 하한 게이트·랭킹 보드·데이터 모델을 한꺼번에 재설계하게 만들었습니다.** 참고 자료를 먼저 확인하는 것이 설계 회차를 줄입니다.
