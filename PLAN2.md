# PLAN2 — v2 로드맵 (콘텐츠 확장 + 난이도 5단계 + 업적 + 타워 단일 랭킹)

작성일 2026-08-13, 같은 날 후속 교차검증으로 갱신(§8). **아직 구현 착수 전 설계 문서**다. 최초 6개 갈림길은 §7에서 결정 완료. 이후 교차검증에서 시즌제가 폐기되고 통합 랭킹이 타워 단일 보드로 단순화됐다 — **최신 상태를 보려면 §8을 먼저 읽을 것.**

- `PLAN.md`(v1.5)는 초기 설계 원안이고 갱신하지 않는다. 현재 실제 상태는 `인수인계.md` + `docs/*.md` + 코드가 기준.
- 이 문서는 "지금 있는 것"을 측정해 확인하고, 새로 요청된 것을 그 위에 어떻게 얹을지 정리한 것이다.

---

## 1. 현재 상태 재점검 (측정한 사실)

| 항목 | 현재 |
|---|---|
| 게임 루프 | 주제 선택 → **12스테이지**(난이도 1~4를 3스테이지씩) → 스테이지당 5문항, 4개 이상 정답이어야 클리어 |
| 점수 | Σ(정답 문항 난이도 × 10), 만점 **1500** (`MAX_RUN_SCORE`, worker/index.ts:34) |
| 난이도 | **1~4 고정**. `questions.difficulty INTEGER CHECK(difficulty BETWEEN 1 AND 4)` |
| 문제 유형 | MULTIPLE_CHOICE 186 / NUMERIC_INPUT 54 / TEXT_INPUT 24 = **264문항** |
| 공식 주제(broad) | 과학·지리·스포츠 각 80문항(난이도당 20) `active`, 새 맞추기 24문항 `draft` |
| 세부 장르(narrow) | **이미 15개가 `data/topics.json`에 정의돼 있음** — 화학·물리·천문·인체·생물·단위 / 국가·수도·지형·국기·시차 / 축구·야구·구기·올림픽·기록 |
| 세부 장르 문항 수 | 전부 하한 미달. 최대 구기 26, 최소 **단위 3 / 국기 6 / 시차 9 / 기록 9**. 난이도 균형도 깨져 있음(수도: 1단계 8, 2단계 1) |
| 화면 | `topics` / `playing` / `stageResult` / `final` / `ranking` / `workshop` — 6개 (src/App.tsx:34) |
| 랭킹 | 통합 전체기간·주간 2보드 + 주제별(결과 화면 내부). 커뮤니티 주제는 랭킹 완전 제외 |
| 유저 창작마당 | 로그인+닉네임 필요, 자동 검증만 통과하면 즉시 공개, 하한 5/난이도 |
| 스케줄러 | **없음.** Cron Trigger 미설정 (`wrangler.jsonc`에 triggers 없음) |
| 실시간 통신 | **없음.** Durable Objects / WebSocket 미사용 |

### 재점검에서 나온 중요한 발견 3가지

**① 제안 ①"타워 오르기"와 ②"주제 선정 후 문제 풀기"는 지금 같은 것이었다.**
현재 게임이 이미 "주제를 고르고 → 12스테이지를 순서대로 오르는" 구조다. → **타워를 전 주제 혼합·계정 누적형으로 새로 정의**해 성격을 분리한다(결정 ①).

**② 세부 장르는 스키마 문제가 아니라 콘텐츠 문제다.**
`question_topics`(N:M)와 narrow 주제 15개가 이미 있어서 구조는 준비돼 있다. 막힌 건 문항 수뿐이다.

**③ 난이도 5단계는 랭킹 점수 체계를 건드린다.** ~~만점 1500은 "난이도 1~4 × 3스테이지 × 5문항 × 10점"에서 나온 값이다. 5단계를 추가하면 만점이 **2250**으로 바뀌고 기존 기록과 새 기록이 섞인다. → **시즌제로 분리**한다(결정 ③).~~
> ⚠ **이후 교차검증에서 뒤집힘.** 이 문제는 "점수 합산형 랭킹"에만 해당하는데, 그 랭킹 자체가 나중에 폐지됐다(§2 C4 참고). 시즌제도 함께 폐기됐다 — 최신 결정은 §7 "이후 교차검증 결정 기록" 참고.

---

## 2. 확정된 설계

### C1. 타워 오르기 — 전 주제 혼합 무한 타워, 유일한 랭킹 보드

> ⚠ 이후 교차검증(§7)에서 크게 갱신됨: 층당 문항 수·데이터 모델·랭킹 위상이 전부 바뀌었다. 아래는 최신 상태.

| | 주제 플레이(보드 모드, PLAN3) | 타워(신규) |
|---|---|---|
| 범위 | 주제 1개 | **공식 active 주제 전체 혼합** (커뮤니티 제외) |
| 진행 | 매번 새 보드, 랭킹 무관 | **계정에 누적** — 최고 층수가 저장되고 이어서 도전 |
| 단위 | 9칸 중 자유 선택 | **1층 = 1문항**, 즉시 채점, 순서대로만 진행 |
| 지표 | (랭킹 없음) | **도달 층수 = 이 앱의 유일한 랭킹 지표**(§2 C4) |

- **층↔난이도 곡선**: 1~5층 난이도1, 6~10층 2, 11~15층 3, 16~20층 4, **21층 이상은 난이도5 반복**. 21층 돌파는 난이도5 문항 확보(P1)에 의존한다.
- **1층 = 1문항, 즉시 채점.** 보드 모드와 같은 원칙이다 — 여러 문항을 모아 배치로 채점하면 PLAN4 §2에서 확인한 "정답 여부를 늦게 안다"는 문제가 타워에 그대로 재발한다. (원래 초안 문구 "최근 10층(50문항)"은 층당 5문항을 암묵적으로 전제하고 있었다 — 이제 오기로 정정.)
- **문항 고갈 대응**: 판 전체 무중복 대신 **"최근 50문항 내 무중복"**(=최근 50층)으로 완화한다. 무한 진행이 전제라 전체 무중복은 애초에 불가능하다. 50이라는 크기는 체감상 반복이 안 느껴지는 수준으로 잡은 값이고, 실사용 후 조정 여지가 있다.
- **실패 처리**: 5문항(=5층) 단위 체크포인트에서 재시작(1·6·11·16층…). `best_floor`는 유지된다.
- **데이터 모델 — `runs` 테이블을 재사용하지 않는다.** `runs.topic_id`가 `NOT NULL`이라 "주제 하나"를 전제하는데, 타워는 여러 주제를 섞어서 이 전제와 안 맞는다(교차검증 중 발견). 대신 전용 테이블 하나로 충분하다:
  ```sql
  CREATE TABLE IF NOT EXISTS tower_progress (
    uid                  TEXT PRIMARY KEY REFERENCES users(uid),
    floor_reached        INTEGER NOT NULL DEFAULT 1,
    best_floor           INTEGER NOT NULL DEFAULT 0,
    current_question_id  TEXT,   -- 지금 낸 문항 id (서버 채점 보안 모델, PLAN 7.5절 A안과 같은 원칙)
    recent_served_json   TEXT,   -- 최근 50문항 id (무중복 롤링 윈도우)
    updated_at            TEXT NOT NULL
  );
  ```
  시즌제가 폐기되면서(§2 C4) `season_id` 컬럼도 필요 없어졌다.
- API 2개: `POST /api/tower/answer`(현재 층 채점 → 성공 시 다음 층 문항 발급, 실패 시 체크포인트로 복귀), `GET /api/tower/active`(이어하기 — 현재 층·문항 반환).
- **랭킹**: `best_floor` 내림차순. 통합 랭킹이 폐지되면서(§2 C4) **타워가 이 앱의 유일한 랭킹 보드가 된다.**
- 커뮤니티 문항은 타워에 섞지 않는다(랭킹 반영 콘텐츠이므로 기존 경계선과 동일).

### C2. 주제 선정하고 문제 풀기 — ~~시작 난이도 선택~~ **폐지, PLAN3로 완전 대체**

> ⚠ **이 절은 폐기됐다.** 아래는 최초 설계를 기록으로만 남긴 것이고 구현 대상이 아니다. 60문항(15스테이지)도 피로감이 크다는 지적을 받아 **`PLAN3.md`의 문제 보드(3×3) 모드가 이 콘텐츠를 완전히 대체**한다 — 스테이지 진행·시작 난이도 선택 UI 전부 폐지, 대신 9칸 무작위 격자에서 자유롭게 문항을 골라 푸는 방식으로 바뀐다. 상세는 `PLAN3.md` §7·§9.

<details>
<summary>최초 설계(폐기됨, 기록용)</summary>

- 주제 선택 → **시작 난이도 선택** → 거기서부터 5단계까지 오름차순 진행.
- 스테이지 수는 시작 난이도에 따라 달라진다: 1부터면 15스테이지, 4부터면 6스테이지.
- 점수식은 그대로 Σ(난이도×10). 따라서 **만점은 시작 난이도별로 다르다** (1부터 2250 / 2부터 2100 / 3부터 1800 / 4부터 1350 / 5부터 750).
- `topic_best`는 시작 난이도와 무관하게 **최고점 하나만** 기록한다 → 최고점 경쟁은 자연히 "1단계부터 완주"가 되고, 높은 시작 난이도는 **짧게 즐기는 선택지**가 된다.
- 시작 난이도를 고르려면 그 난이도 문항이 하한을 채워야 한다 — 못 채운 난이도는 UI에서 잠금 표시.

</details>

### C3. 배틀 — **보류**

로드맵에서 제외한다. 타워·난이도·업적을 먼저 완성하고 실사용 반응을 본 뒤 재검토.
(참고: 실시간 대전은 Workers 구조상 Durable Objects가 필수이고 동시 접속자가 적으면 매칭이 성립하지 않는다. 재개할 때는 D1만으로 가능한 비동기 고스트전이 현실적인 출발점이다.)

### C4. 순위 확인 — 타워 단일 보드로 단순화, 시즌제 폐기

> ⚠ **이 절은 최초 설계(시즌제 + 3개 보드)에서 완전히 바뀌었다.** 아래는 교차검증(§7)에서 확정된 최신 상태다.

교차검증 중 두 가지가 드러났다:
1. `users.global_score`는 `topic_best`의 SUM으로 계산되는 **파생값**이었다(`worker/index.ts` `finalizeRun()`). `topic_best`를 채우던 유일한 모드가 C2(구 스테이지 판)였는데 그게 폐지됐다(§2 C2) — **통합 랭킹(전체기간·주간)이 데이터 원천을 잃었다.**
2. 시즌제를 도입한 이유는 "난이도5로 만점이 바뀌어 옛 점수와 새 점수가 섞인다"는 문제였는데, 이건 점수 합산형 랭킹에만 해당한다. 타워의 지표(도달 층수)는 만점 개념이 없어 이 문제 자체가 발생하지 않는다.

**결정: 통합 랭킹 보드 폐지, 시즌제 폐기.**

- 랭킹 보드는 **타워 층수 하나**만 남는다.
- 기존 "주제별 순위"(결과 화면 안에서 보여주던 기능)도 함께 폐지 — `topic_best`가 더 이상 안 채워지므로 근거가 없다.
- `seasons` 테이블, `topic_best`/`topic_best_weekly`의 PK 확장, `global_score` 재계산 — **전부 불필요해졌다.** P0의 위험한 테이블 재생성이 2건에서 **`questions`(CHECK 확장) 1건**으로 줄어든다.
- `topic_best`/`topic_best_weekly`/`users.global_score`는 **더 이상 쓰지 않지만 지우지도 않는다** — 기존 데이터를 동결된 역사 기록으로 그대로 둔다(§6 "손대지 않는 것", DROP 금지 원칙과 일치).
- 제거 대상(P0/P2 구현 시): `worker/ranking.ts`의 `handleGlobalRanking`/`upsertWeeklyBest`/관련 라우트, `handleTopicRanking`. `src/types.ts`의 `GlobalBoardId`/`RankingBoardResponse`/`TopicRankingResponse`. 대신 타워 전용 랭킹 조회 함수 하나로 대체한다.

### A1+A2. 난이도 5단계 (전문가 유지 + 매니아 추가)

**명칭 매핑**: 현재 1~4 = 쉬움 / 보통 / 어려움 / 전문가 (그대로), **매니아 = 신규 5단계**. 기존 264문항은 재분류하지 않는다.

건드려야 하는 곳 — 광범위하다:

| 위치 | 변경 |
|---|---|
| `migrations/0003_*.sql` | `questions.difficulty` CHECK를 1~5로 확장 → **테이블 재생성 필요**. 0002와 동일 절차(자식 테이블 FK 먼저 제거 → questions 재생성) |
| `db/schema.sql` | 베이스라인 CHECK도 1~5로 (신규 DB용). **DROP 절대 금지** |
| `topics` | `q_count_5` 컬럼 추가 (ALTER로 가능) |
| `src/types.ts` | `Difficulty = 1..5` |
| `worker/index.ts` | ⚠ C2 폐지로 `difficultyForStage()`·`MAX_RUN_SCORE`·`TOTAL_STAGES`는 스테이지 모드 전용 죽은 코드가 된다 — 수정이 아니라 **삭제 대상**(P2에서 보드 모드 API로 교체할 때 같이 제거). 타워는 자체 층↔난이도 곡선 함수를 새로 둔다(§2 C1) |
| `worker/community.ts` | `validateQuestion` 난이도 범위, 게이트 판정 루프 `[1,2,3,4]` → `[1..5]` |
| `scripts/*.mjs` | `validate.mjs` 검증 범위, `build-seed.mjs` 게이트 판정, `lib.mjs` 상수 |
| `data/topics.json` | 주제별 `difficultySpec`에 5단계 규칙·예시 추가 (**20개 주제 전부**) |
| `src/App.tsx`, `Workshop.tsx` | 난이도 표기(쉬움~매니아 5단계). ~~시작 난이도 선택 UI~~는 C2 폐지로 불필요(PLAN3.md §1) |
| **콘텐츠** | 공식 3주제 × 난이도5 20문항 = **최소 60문항 신규 작성** |

⚠ 마이그레이션은 인수인계 2번 교훈대로 배포 전 (a) 데이터 있는 로컬 DB, (b) 빈 DB에 schema+전체 마이그레이션 순서 적용 — 둘 다 통과해야 한다.

### A3. 장르 세분화 — 공식 하한(20/난이도) 유지, 문항 차는 순서대로 개방

- 세부 장르도 broad와 같은 기준(난이도당 20, 5단계 = **장르당 100문항**)을 적용한다. 품질 기준을 콘텐츠 종류마다 다르게 두지 않는다.
- 현실 인식: 15개 장르 전부 열려면 1,500문항이 필요하고 지금은 264문항이다. **이건 한 번에 끝나는 작업이 아니라 상시 콘텐츠 과제**다. 문항이 차는 장르부터 자동으로 `active`가 되는 현행 게이트 구조를 그대로 쓰면 되므로 코드 작업은 사실상 없다.
- 개방 예상 순서(현재 보유량 기준): 구기 26 → 수도 23 → 화학 21 → 국가 21 → 지형 21 → 축구 18 → 올림픽 17 → … → 단위 3, 국기 6은 한참 뒤.
- ~~세부 장르가 열리면 그 장르도 topic_best에 잡히므로 통합 랭킹(폭 보상형)에 자연히 반영된다.~~ ⚠ 통합 랭킹이 폐지되면서(§2 C4) 이 문장은 더 이상 성립하지 않는다. 장르 개방의 효과는 이제 **콘텐츠 가용성**(보드 모드·타워가 뽑을 수 있는 문항 풀이 넓어짐)뿐이고, 랭킹에는 영향을 주지 않는다.

### A4. 업적 시스템

요청된 4종: ① 어려운 난이도 클리어 ② 문제 N개 클리어 ③ 주간 랭커 ④ 장르 마스터.

- 새 테이블: `achievements(id, name, description, icon, kind, threshold_json)` + `user_achievements(uid, achievement_id, unlocked_at, progress)`
- **업적은 타워 플레이만 인정한다**(교차검증 §7 결정). 보드 모드는 순수 연습으로 완전히 분리 유지 — 커뮤니티 주제 제외 원칙과 같은 결.
- 판정 위치:
  - ①②④ → ~~`finalizeRun()` 안에서 판정~~ **타워의 답안 채점 경로(`POST /api/tower/answer`, §2 C1)에서 직접 판정.** `finalizeRun()`은 C2(구 스테이지 판) 폐지 + 타워가 `runs`를 안 쓰는 구조(§2 C1)로 재설계되면서 **어떤 모드에서도 더 이상 호출되지 않는다 — P2 구현 시 삭제 대상.**
  - ③ 주간 랭커 → ⚠ 통합 주간 보드가 폐지되면서(§2 C4) "주간 랭커"의 정의가 필요하다. **타워 주간 랭커**(이번 주 타워에서 가장 많이 오른 유저)로 재정의하는 게 자연스럽다 — 다만 이건 새로 발견된 세부사항이라 P3 구현 시 확정.
- 누적 카운터(②)는 이제 **타워 플레이만 집계**하면 되므로 `users.total_correct`(타워에서 맞힌 문항 수) 컬럼 하나로 충분.
- **커뮤니티 주제 플레이는 업적에 반영하지 않는다** — 애초에 타워 콘텐츠가 아니므로 자동으로 제외된다.
- 타워 층수 업적(예: 20층 돌파)도 같은 틀에 얹는다.

---

## 3. 공통 선행 작업

- **P0-a. 배포 브랜치 정리** — `deploy.yml` 트리거에 현재 작업 브랜치(`claude/handover-matters-review-gs5597`) 추가. push = 프로덕션 배포라는 점 유의.
- **P0-b. 난이도 5단계(A1/A2)** — 타워 층 난이도 곡선, 업적 ①, 장르 개방이 전부 여기에 의존한다. **크리티컬 패스.** ~~+ 시즌제(C4)~~ 시즌제는 교차검증 결과 폐기됐다(§2 C4) — P0는 이제 `questions` 테이블 재생성 1건만 있으면 된다.

---

## 4. 단계별 로드맵

| 단계 | 내용 | 산출물 | 의존 |
|---|---|---|---|
| **P0** | 배포 브랜치 정리 / 난이도 1~5 확장 | migration 0003, 타입·상수·스크립트 일괄 수정 | — |
| **P1** | 공식 3주제 난이도5 문항 60개 + 새 맞추기 56개 보충 | `data/questions/*.json` | P0 |
| **P2** | 문제 보드 모드(PLAN3.md) — 기존 스테이지 판(C2) 완전 대체 | worker 출제 로직, 3×3 격자 UI, 결과 화면 | P0, P1 |
| **P3** | 업적 시스템(A4) — 테이블 + finalizeRun 판정 + 업적 화면 | migration 0005, 새 화면 | P2 |
| **P4** | 타워 오르기(C1) — 혼합 출제, 체크포인트, 타워 랭킹 보드 | `tower_progress`, 타워 화면 | P1, P2 |
| **P5** | 세부 장르 개방(A3) — 문항 차는 대로 자동 활성 | `data/questions/*.json` (상시) | P1 이후 상시 |
| (보류) | 배틀(C3) | — | 실사용 반응 확인 후 |

⚠ `PLAN4.md`에 이 표보다 먼저 오는 **P-1 단계**(문항 작성 기준 문서화 + 커뮤니티 추천/비추천 + 피드백 화면)가 별도로 정의돼 있다 — 위험한 마이그레이션과 무관해 독립적으로 먼저 진행 가능. 이 표는 PLAN4 이전에 작성된 것이라 P-1이 빠져 있다.

**핵심 판단: P1(콘텐츠)이 전체 일정을 지배한다.** 스키마·코드는 각각 하루 단위지만 필요한 신규 문항은 당장 116개(난이도5 60 + 새 맞추기 56)이고, 세부 장르까지 보면 1,000개 단위다. 기능을 늘리는 속도보다 문항을 채우는 속도가 느리면 화면만 늘고 플레이할 게 없는 상태가 된다.

---

## 5. 리스크

1. **마이그레이션 사고 재발** — 이번 P0는 `questions`(CHECK 확장) **한 테이블을 재생성**한다(교차검증 후 축소됨, §2 C4). 실사용자 데이터가 있는 상태이므로 0002 절차 + 이중 로컬 검증을 반드시 반복한다.
2. ~~시즌 전환의 비가역성~~ — 시즌제 자체가 폐기돼 해당 없음.
3. **문항 고갈** — 타워는 무중복 완화 규칙(최근 50문항) 없이는 무한 진행이 불가능하다. 21층 이후 난이도5 반복 구조라 난이도5 문항이 적으면 사실상 20층이 상한이 된다.
4. **`build-seed.mjs`의 커뮤니티 문항 삭제 위험** — 난이도5 작업으로 이 스크립트를 다시 건드린다. `author_uid IS NULL` 범위 제한 유지를 매번 확인.
5. **기능 과다** — P2까지 완성해 실사용 반응을 본 뒤 P4를 시작하는 편이 안전하다.
6. **랭킹 단순화의 부작용** — 랭킹 지표가 타워 층수 하나로 줄면서 "이 주제를 잘한다"를 드러낼 방법이 사라진다. PLAN4 §6의 "장르별 대표 배지"가 타워 플레이 기반으로 이 역할을 일부 대신한다.

---

## 6. 손대지 않는 것

- `PLAN.md`(v1.5) — 원안 그대로 보존.
- 커뮤니티 주제의 랭킹 제외 원칙 — 업적·타워에도 같은 경계선 적용.
- 서버 채점(정답은 채점 후에만 내려감) — 타워에도 동일 적용.
- `db/schema.sql`에 DROP TABLE 금지.
- 기존 264문항의 난이도 재분류.
- `topic_best`/`topic_best_weekly`/`users.global_score` — 더 이상 쓰지 않지만(§2 C4) 지우지도 않는다. 동결된 역사 기록으로 둔다.

---

## 7. 결정 기록

| # | 질문 | 결정 |
|---|---|---|
| ① | 타워와 주제 플레이 구분 | **전 주제 혼합 무한 타워** (주제 플레이는 현행 확장) |
| ② | "장르 당 난이도 설정"의 의미 | **시작 난이도 선택** (오름차순 구조 유지) — ⚠ 이후 PLAN3 검토에서 뒤집힘. 스테이지 자체가 폐지되고 보드 모드(완전 무작위)로 대체됨(§2 C2, PLAN3.md §9) |
| ③ | 난이도5 도입 시 기존 랭킹 점수 | **시즌제 도입** (시즌1 보존, 시즌2 개시) — ⚠ 이후 교차검증에서 뒤집힘. 시즌제의 전제(통합 랭킹)가 폐지되면서 시즌제 자체도 폐기됨(§8 ⑧⑨) |
| ④ | 세부 장르 개방 기준 | **공식 하한 그대로(20/난이도)** — 차는 순서대로 개방 |
| ⑤ | 배틀 방식 | **보류** — 나머지 완성 후 재검토 |
| ⑥ | 난이도 명칭 매핑 | **기존 1~4 유지 + 매니아(5) 추가** |

## 8. 이후 교차검증 결정 기록 (PLAN2/3/4 전체 재점검, 같은 날 후속 대화)

PLAN2·PLAN3·PLAN4를 서로 대조하다 발견한 것들. 특히 ⑦→⑨→⑩이 연쇄적으로 이어져 로드맵이 크게 단순해졌다.

| # | 질문 | 결정 | 파급 효과 |
|---|---|---|---|
| ⑦ | `topic_best`를 채우는 모드가 없어짐(C2 폐지로) — 주제별 순위·장르 마스터가 여기 의존 | 처음엔 "주제별 순위만 폐지"로 답변 | → 이어서 ⑧ 발견으로 확장됨 |
| ⑧ | (발견) `global_score`가 사실 `SUM(topic_best.score)` 파생값이었음 — 통합 랭킹 전체가 같이 멈춘다는 사실이 뒤늦게 드러남 | **통합 랭킹 보드(전체기간·주간) 자체를 폐지** | 랭킹 = 타워 층수 하나로 단순화(§2 C4) |
| ⑨ | (발견) 시즌제의 존재 이유(점수 합산형 랭킹의 만점 변경 문제)가 ⑧로 사라짐 | **시즌제 폐기** | P0의 위험한 테이블 재생성 2건→1건(§2 C4, §5) |
| ⑩ | 업적(어려운 난이도 클리어·장르 마스터)이 어느 모드를 근거로 판정되나 | **타워 플레이만 인정** — 보드는 순수 연습으로 완전 분리 | A4 판정 위치가 `finalizeRun()`에서 타워 전용 경로로 이동(§2 A4) |
| ⑪ | 타워 한 층이 몇 문항인가(원문 표현이 5문항/층을 암묵 전제해 배치 피드백 문제 재발 우려) | **1층 = 1문항, 즉시 채점** | §2 C1 전면 갱신 — `runs` 재사용 포기하고 `tower_progress` 전용 테이블로 |

부수 발견: 타워는 여러 주제를 섞는데 `runs.topic_id`가 `NOT NULL`이라 스키마와 안 맞았다 — `runs`를 아예 재사용하지 않는 것으로 해소(§2 C1).
