# PLAN3 — 문제 보드(3×3) 모드: 플레이 피로감 해소

작성일 2026-08-13, §9 결정 반영 갱신. **구현 착수 전 설계 문서.** `PLAN2.md`의 C2(주제 플레이)를 **완전히 대체**하므로 PLAN2와 함께 읽어야 한다. §9의 4개 결정 모두 확정.

---

## 1. 문제 — 피로감은 어디서 오나

사용자 지적: **"12스테이지를 모두 풀어야 하는 피로감이 너무 크다."** 코드에서 확인한 실제 원인은 셋이다.

| # | 원인 | 근거 |
|---|---|---|
| ① | **한 번에 60문항** — 12스테이지 × 5문항을 끊지 않고 이어서 푼다 | `TOTAL_STAGES=12`, `QUESTIONS_PER_STAGE=5` |
| ② | **이어하기가 없다** — 나갔다 오면 판이 통째로 사라진다 | 화면 상태(`Screen`)가 메모리에만 있고(src/App.tsx:34), 진행 중인 판을 되돌려주는 API가 없다. `runs`에 `in_progress`로 남지만 복원 경로도, `abandoned`로 정리하는 코드도 없다 |
| ③ | **한 번 미끄러지면 전부 날아간다** — 5문항 중 4개를 못 맞히면 그 자리에서 판 종료 | `runEnds = !cleared \|\| ...` (worker/index.ts:384) |

②가 특히 크다. 60문항이 길다는 것보다 **"중간에 못 끊는다"**가 피로의 본질이다. 끊을 수 있으면 60문항도 나눠서 풀 수 있다.

**PLAN2의 C2는 폐지되고 이 문서의 보드 모드로 완전히 대체된다**(§9 결정 ②, 7절). 15스테이지(최대 75문항)로 늘리는 안이 60문항도 길다는 지적과 정면으로 부딪혔기 때문이다. "시작 난이도 선택" UI도 함께 폐지된다 — 보드가 완전 무작위 구성(§9 결정 ①)이라 난이도를 미리 고르는 단계 자체가 없다. 대신 사용자는 9칸에 표시된 난이도를 보고 어떤 칸을 풀지 스스로 고른다.

---

## 2. 해결책 — 문제 보드(3×3)

9칸 격자에서 **풀고 싶은 문항을 골라, 한 문항씩, 아무 때나** 푼다.

| | 현행 스테이지 판 | 문제 보드(신규) |
|---|---|---|
| 단위 | 5문항 묶음 제출 | **문항 1개씩 즉시 채점** |
| 순서 | 강제 (1→12스테이지) | **자유 — 9칸 중 아무거나** |
| 길이 | 60문항 연속 | **9문항**, 한 문항만 풀고 나가도 됨 |
| 이탈 | 판 소실 | **매 문항 저장, 들어오면 그대로 복원** |
| 실패 | 4/5 미만이면 판 종료 | **실패 없음** — 틀린 칸은 오답 표시로 남고 보드는 계속 진행 |
| 완료 | 12스테이지 클리어 | 9칸 다 풀면 새 보드 자동 발급. **또는 언제든 수동으로 재발급**(§9 결정 ④) |

이 설계가 세 원인을 각각 없앤다: ①은 9문항으로, ②는 문항 단위 저장으로, ③은 실패 개념 제거로.

---

## 3. UI 설계

### 레이아웃
```
데스크톱 (2단)                          모바일 (세로 스택)
┌────────────────────┬──────────┐      ┌──────────────────┐
│                    │ ▣ ▣ ▣    │      │  ▣ ▣ ▣           │
│   문제 영역        │ ▣ ▣ ▣    │      │  ▣ ▣ ▣  ← 보드   │
│   (선택한 문항)    │ ▣ ▣ ▣    │      │  ▣ ▣ ▣           │
│                    │  ↑ 보드  │      ├──────────────────┤
│                    │  (우측)  │      │  문제 영역        │
└────────────────────┴──────────┘      └──────────────────┘
```
요청대로 보드는 **우측**에 둔다. 모바일은 폭이 없으므로 보드를 위로 올리고 문제를 아래에 둔다(기존 모바일 헤더 줄바꿈 버그를 고친 이력이 있어 실제 모바일 사용자가 있다 — `docs/2026-08-13-mobile-header-fix.md`).

### 칸 상태 (4가지)
| 상태 | 표시 |
|---|---|
| 미풀이 | 기본 테두리 + 난이도 숫자 |
| 현재 선택 | 강조 테두리 |
| 정답 | 채움색 + ✓ |
| 오답 | 채움색(경고) + ✗ |

### 호버 정보 — **모바일에는 hover가 없다**
요청은 "마우스를 가져다 대면 장르와 난이도 표시"인데, 터치 기기에는 hover가 존재하지 않는다. 그래서 이중 설계로 간다:

- **칸에 상시 표시**: 난이도 숫자(1~5) + 장르 아이콘/약칭. 정보를 툴팁에만 두지 않는다.
- **툴팁(데스크톱 hover / 키보드 포커스)**: 장르 전체 이름 + 난이도 이름(쉬움~매니아) + 문제 유형(객관식/숫자/단답) + 이미지 문항 여부.
- 터치에서는 **탭 = 바로 진입**(미리보기 2단계를 만들면 오히려 손이 더 간다). 상시 표시가 있으므로 정보 손실이 없다.
- 툴팁은 hover 전용으로 만들지 않는다 — `:focus-visible`에서도 떠야 키보드로 접근 가능하다.

**문항 본문은 툴팁에 넣지 않는다.** 9칸을 훑어보며 쉬운 것만 고르는 게 아니라, 장르·난이도를 보고 고르게 하는 것이 이 UI의 의도다.

---

## 4. 보드 구성 규칙 (3×3 = 9문항) — §9 결정 ①: 완전 무작위

행/열에 장르·난이도를 고정 배치하지 않는다. **9칸 각각이 독립적으로 장르(narrow 우선)와 난이도를 무작위로 뽑는다.** 칸마다 결과가 다 달라 격자 자체가 "표"의 의미(행=이것, 열=저것)를 갖진 않지만, 대신 훨씬 단순해진다 — 장르별로 3칸을 채워야 하는 제약이 없어 폴백 로직이 칸 단위로 축소된다.

```
  ▣ 화학·쉬움     ▣ 국기·전문가    ▣ 과학(전체)·보통   ← 칸마다 독립적으로
  ▣ 물리·매니아    ▣ 천문·보통      ▣ 생물·어려움         장르+난이도 무작위 추첨
  ▣ 화학·보통     ▣ 과학(전체)·쉬움 ▣ 단위·어려움
```

- **주제 선택은 유지한다.** 사용자가 broad 주제(과학 등)를 먼저 고르고, 9칸은 그 주제에 속한 콘텐츠에서만 뽑는다 — 타워(전 주제 혼합)와 성격이 겹치지 않도록 하기 위한 가정. 이 부분은 문서에 명시되지 않았던 판단이라 여기 남긴다.
- **칸별 폴백**: narrow 장르 중 하나를 뽑았는데 그 장르+난이도 조합에 문항이 없으면(단위·국기처럼 부족한 장르가 많음), 그 칸만 broad 주제 전체로 폴백한다. 장르 3개를 맞추는 제약이 없으므로 폴백은 칸 단위로 독립적이라 §4 이전 안보다 구현이 단순하다.
- **새 맞추기(birds)처럼 narrow 장르가 아예 없는 주제**는 모든 칸이 처음부터 broad 폴백(= "새 맞추기" 그 자체)으로 뽑히고, 난이도만 무작위로 달라진다 — 별도 예외 처리 불필요.
- 한 보드 안에서는 문항이 중복되지 않는다. 직전 보드에서 낸 문항도 가급적 제외(최근 N개 제외 목록 유지).

---

## 5. 데이터 모델 — **테이블 재생성 없이 ALTER만으로 된다**

기존 `runs`를 그대로 재사용한다. `mode` 컬럼이 이미 있고(`'solo'` 기본값) 서버 채점용 `served_json`도 있다.

```sql
-- migrations/000N_add_board_mode.sql
ALTER TABLE runs ADD COLUMN board_json TEXT;   -- 9칸 상태
```

`board_json` 구조 (**정답·해설은 넣지 않는다** — 서버 채점 원칙 유지):
```json
[{ "questionId": "...", "topicId": "chemistry", "difficulty": 2,
   "solved": true, "correct": false, "given": "산소" }, ...]
```

- 보드 생성 시 서버가 9문항 id를 `served_json`에 박아둔다 → 클라이언트가 내지 않은 문항의 답을 제출하는 걸 막는 현행 보안 모델(PLAN 7.5절 A안)이 그대로 유지된다.
- **완료 판정은 `finalizeRun()`을 재사용하지 않는다.** `finalizeRun()`은 스테이지 수·랭킹 갱신을 전제로 짜인 함수라 시그니처(`stagesCleared` 등)가 보드와 안 맞고, 애초에 보드는 랭킹에 반영하지 않으므로(6절) 그 로직 대부분이 불필요하다. 대신 9칸이 전부 `solved`가 되면 `runs.status='completed'`로만 표시하고, `users.play_count`·업적용 `total_correct`(PLAN2 A4)·`question_stats`만 갱신하는 가벼운 별도 경로를 둔다.
- **고아 판 정리**: 새 보드를 발급하면(수동 재발급 포함) 그 유저의 기존 `in_progress` 보드를 `abandoned`로 바꾼다. 지금은 `abandoned` 상태를 쓰는 코드가 없어서 프로덕션에 버려진 `in_progress` 행이 쌓여 있을 가능성이 높다 — 이 작업에서 같이 정리한다.

### API 추가 (3개)
| 엔드포인트 | 역할 |
|---|---|
| `POST /api/boards` | 주제로 새 보드 발급 (9칸 무작위 추첨해 `runs` 생성). **§9 결정 ④: 언제든 호출 가능** — 진행 중인 보드가 있으면 그 보드를 `abandoned`로 바꾸고 새로 발급한다(랭킹 미반영이라 안전, 6절) |
| `GET /api/boards/active` | **이어하기** — 진행 중인 보드 반환. 미풀이 칸은 문항 본문만, 푼 칸은 채점 결과(정답·해설 포함) |
| `POST /api/boards/:id/answer` | 문항 1개 즉시 채점 → `board_json`·`question_stats` 갱신 → 정답·해설 반환 |

`src/api.ts`(EP-5 계층)에만 함수를 추가하고 화면은 fetch를 직접 부르지 않는다 — 기존 규칙 유지.

### 프론트
- `Screen`에 `{ kind: 'board'; ... }` 추가.
- **앱 시작 시 `GET /api/boards/active`를 한 번 호출**해 진행 중인 보드가 있으면 바로 복원한다. 이게 "나갔다 들어와도 이어서" 요구의 핵심이다.
- 클라이언트에 별도 저장(localStorage)은 두지 않는다 — 서버가 단일 진실 원천이고, 기기를 바꿔도 이어진다.

---

## 6. 점수·랭킹 — §9 결정 ③: 연습 전용, 랭킹 미반영

**보드 모드는 `topic_best`/`global_score`/시즌 랭킹에 전혀 반영하지 않는다.** 커뮤니티 주제와 같은 경계선을 하나 더 얻는 셈이다 — "채점된 정답·해설을 즉시 보여주는 연습판"이 이 모드의 목적이고, 랭킹 경쟁은 여전히 타워·(폐지 전 존재했던) 정식 판의 몫이었으나 그 판 자체가 폐지됐으므로, **결과적으로 주제 단위 랭킹 경쟁 콘텐츠는 이번 로드맵에서 타워로 일원화된다.**

이 결정이 앞선 설계를 크게 단순화한다:
- 보드 점수 = Σ(정답 문항 난이도 × 10)를 **그 세션에서 진행 상황 표시용으로만** 계산하고 어디에도 저장하지 않는다. 만점이 칸마다 뽑힌 난이도에 따라 매번 달라지는 문제(구 설계의 180~360점 이슈)가 애초에 사라진다.
- `topic_best`에 `mode` 컬럼을 추가할 필요가 없다. PLAN2 P0의 `topic_best` PK 확장은 여전히 필요하지만(시즌 구분, `season_id`만) **더 이상 보드 모드와 조율할 필요가 없어 P0 범위가 그만큼 줄어든다.**
- **⚠ 랭킹 미반영이라도 `question_stats`(문항별 정답률 집계)와 업적용 `users.total_correct`(PLAN2 A4)는 그대로 갱신한다.** 이 둘은 "공식 콘텐츠 품질 근거"와 "문제 해결 횟수 업적"이라는 별개의 목적이라, 랭킹 제외 원칙과 다른 기준으로 판단한다 — 커뮤니티 주제 플레이는 이 둘도 제외하지만(PLAN2 A4), 보드는 공식 콘텐츠이므로 여기서는 반영한다. (직접 정함 — 사소한 판단이라 명시만 하고 넘어감)

---

## 7. PLAN2 재조정

| PLAN2 항목 | 조정 |
|---|---|
| **C2 주제 플레이** | **폐지, 보드 모드로 완전 대체.** "시작 난이도 선택" UI도 함께 폐지 — 보드가 완전 무작위라 사전 선택 단계가 없다 |
| 기존 12(15)스테이지 판 | **폐지.** 화면(`playing`/`stageResult`)과 API(`POST /api/runs`, `POST /api/runs/:id/submit`)를 보드 모드 것으로 교체한다 |
| **C1 타워** | 그대로. 역할이 더 선명해진다 — **긴 호흡·랭킹 경쟁은 타워, 짧은 호흡·연습은 보드** |
| P0 마이그레이션 | `topic_best` PK에 `mode` 컬럼 **불필요** — 보드가 랭킹 미반영이라(6절) `season_id` 확장만 하면 된다. P0 범위가 그만큼 줄어듦 |
| 로드맵 순서 | **보드 모드를 P2 자리에 놓고 우선순위를 올린다.** 피로감은 지금 사용자가 실제로 겪고 있는 문제라 신규 콘텐츠보다 앞선다 |

수정 로드맵: **P0**(난이도5 + 시즌제, `topic_best` PK에 season_id만) → **P1**(문항 보충) → **P2 보드 모드**(기존 스테이지 판 완전 대체) → P3 업적 → P4 타워 → P5 세부 장르

보드 모드는 난이도5가 없어도 1~4 구간만으로 동작하고, 랭킹과 무관해 P0의 위험한 마이그레이션과도 완전히 독립적이다. **급하면 P0/P1보다 먼저 낼 수도 있다** — 재생성 횟수 문제도 더 이상 없다(6절에서 해소됨).

---

## 8. 구현 단계

| 단계 | 내용 |
|---|---|
| P2-a | `runs.board_json` 마이그레이션 + 보드 발급/조회/채점 API 3종. 기존 스테이지 판 API(`POST /api/runs`, `.../submit`) 제거 |
| P2-b | 보드 구성 로직 — 칸별 독립 무작위 추첨, 문항 부족 시 칸 단위 broad 폴백, 최근 출제 제외 |
| P2-c | 프론트 — 3×3 격자 UI(우측/모바일 상단), 칸 상태 4종, 툴팁(hover+focus), 문제 영역. 기존 `playing`/`stageResult` 화면 제거 |
| P2-d | 앱 시작 시 이어하기 복원, 재발급 시 기존 보드 `abandoned` 처리 |

## 9. 결정 기록

| # | 질문 | 결정 |
|---|---|---|
| ① | 격자 구성 | **완전 무작위** — 칸마다 독립적으로 장르·난이도 추첨 |
| ② | 기존 12(15)스테이지 판 | **폐지**, 보드 모드로 완전 대체 |
| ③ | 보드 점수의 랭킹 반영 | **반영 안 함** — 연습 전용. `question_stats`·업적용 정답 수는 반영(6절) |
| ④ | 새 보드 발급 시점 | **언제든** — 기존 보드는 대체(폐기)됨 |

## 10. 리스크

1. **모바일 hover 부재** — 툴팁에만 정보를 두면 모바일에서 장르·난이도를 알 수 없다. 칸 상시 표시가 필수(3절).
2. **세부 장르 문항 부족** — 칸 단위 폴백이 있어도, narrow 장르가 심하게 부족한 주제(단위 3문항 등)는 대부분의 칸이 broad로 폴백해 "장르가 보이는 재미"가 옅어진다. P1(문항 보충)이 실질적인 해결책.
3. **랭킹 경쟁 콘텐츠의 공백** — 주제 단위 경쟁이 사라지고 타워로 일원화된다. 타워는 전 주제 혼합이라 "이 주제를 잘한다"를 드러내는 지표가 약해질 수 있다 — 6절에서 유지하기로 한 주제별 `question_stats`/업적이 이 공백을 부분적으로 메운다.
4. **버려진 `in_progress` 행** — 지금까지 정리 코드가 없어 프로덕션에 쌓여 있을 것이다. 이어하기 기능이 이걸 되살려 보여주면 안 되므로, 보드 모드에만 이어하기를 적용하고 기존 `solo` 판은 정리 대상으로 둔다.
5. **무한 재발급으로 인한 몰입도 저하** — 랭킹이 없으니 악용 문제는 사라졌지만, 반대로 "완료"라는 개념이 약해져 성취감이 옅어질 수 있다. 업적(A4 "문제 N개 클리어")이 이 역할을 대신 하도록 설계를 맞춰야 한다.
