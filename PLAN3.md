# PLAN3 — 문제 보드(3×3) 모드: 플레이 피로감 해소

작성일 2026-08-13. **구현 착수 전 설계 문서.** `PLAN2.md`의 C2(주제 플레이)를 대체하는 제안이므로 PLAN2와 함께 읽어야 한다.

---

## 1. 문제 — 피로감은 어디서 오나

사용자 지적: **"12스테이지를 모두 풀어야 하는 피로감이 너무 크다."** 코드에서 확인한 실제 원인은 셋이다.

| # | 원인 | 근거 |
|---|---|---|
| ① | **한 번에 60문항** — 12스테이지 × 5문항을 끊지 않고 이어서 푼다 | `TOTAL_STAGES=12`, `QUESTIONS_PER_STAGE=5` |
| ② | **이어하기가 없다** — 나갔다 오면 판이 통째로 사라진다 | 화면 상태(`Screen`)가 메모리에만 있고(src/App.tsx:34), 진행 중인 판을 되돌려주는 API가 없다. `runs`에 `in_progress`로 남지만 복원 경로도, `abandoned`로 정리하는 코드도 없다 |
| ③ | **한 번 미끄러지면 전부 날아간다** — 5문항 중 4개를 못 맞히면 그 자리에서 판 종료 | `runEnds = !cleared \|\| ...` (worker/index.ts:384) |

②가 특히 크다. 60문항이 길다는 것보다 **"중간에 못 끊는다"**가 피로의 본질이다. 끊을 수 있으면 60문항도 나눠서 풀 수 있다.

**⚠ PLAN2의 C2는 이 문제를 악화시킨다.** 난이도5를 넣으면서 15스테이지(최대 **75문항**)로 늘리기로 했는데, 지금 사용자가 60문항도 길다고 하고 있다. **PLAN2 C2는 이 문서의 보드 모드로 교체하는 것이 맞다**(7절).

---

## 2. 해결책 — 문제 보드(3×3)

9칸 격자에서 **풀고 싶은 문항을 골라, 한 문항씩, 아무 때나** 푼다.

| | 현행 스테이지 판 | 문제 보드(신규) |
|---|---|---|
| 단위 | 5문항 묶음 제출 | **문항 1개씩 즉시 채점** |
| 순서 | 강제 (1→12스테이지) | **자유 — 9칸 중 아무거나** |
| 길이 | 60문항 연속 | **9문항**, 한 문항만 풀고 나가도 됨 |
| 이탈 | 판 소실 | **매 문항 저장, 들어오면 그대로 복원** |
| 실패 | 4/5 미만이면 판 종료 | **실패 없음** — 틀린 칸은 오답 표시로 남고 보드는 계속 진행 |
| 완료 | 12스테이지 클리어 | 9칸 다 풀면 보드 완료 → 새 보드 발급 |

이 설계가 세 원인을 각각 없앤다: ①은 9문항으로, ②는 문항 단위 저장으로, ③은 실패 개념 제거로.

---

## 3. UI 설계

### 레이아웃
```
데스크톱 (2단)                          모바일 (세로 스택)
┌────────────────────┬──────────┐      ┌──────────────────┐
│                    │ ▣ ▣ ▣    │      │  ▣ ▣ ▣           │
│   문제 영역        │ ▣ ▣ ▣    │      │  ▣ ▣ ▣  ← 보드   │
│   (선택한 문항)    │ ▣ ▣ ▣    │      │  ▣ ▣ ▣           │
│                    │  ↑ 보드  │      ├──────────────────┤
│                    │  (우측)  │      │  문제 영역        │
└────────────────────┴──────────┘      └──────────────────┘
```
요청대로 보드는 **우측**에 둔다. 모바일은 폭이 없으므로 보드를 위로 올리고 문제를 아래에 둔다(기존 모바일 헤더 줄바꿈 버그를 고친 이력이 있어 실제 모바일 사용자가 있다 — `docs/2026-08-13-mobile-header-fix.md`).

### 칸 상태 (4가지)
| 상태 | 표시 |
|---|---|
| 미풀이 | 기본 테두리 + 난이도 숫자 |
| 현재 선택 | 강조 테두리 |
| 정답 | 채움색 + ✓ |
| 오답 | 채움색(경고) + ✗ |

### 호버 정보 — **모바일에는 hover가 없다**
요청은 "마우스를 가져다 대면 장르와 난이도 표시"인데, 터치 기기에는 hover가 존재하지 않는다. 그래서 이중 설계로 간다:

- **칸에 상시 표시**: 난이도 숫자(1~5) + 장르 아이콘/약칭. 정보를 툴팁에만 두지 않는다.
- **툴팁(데스크톱 hover / 키보드 포커스)**: 장르 전체 이름 + 난이도 이름(쉬움~매니아) + 문제 유형(객관식/숫자/단답) + 이미지 문항 여부.
- 터치에서는 **탭 = 바로 진입**(미리보기 2단계를 만들면 오히려 손이 더 간다). 상시 표시가 있으므로 정보 손실이 없다.
- 툴팁은 hover 전용으로 만들지 않는다 — `:focus-visible`에서도 떠야 키보드로 접근 가능하다.

**문항 본문은 툴팁에 넣지 않는다.** 9칸을 훑어보며 쉬운 것만 고르는 게 아니라, 장르·난이도를 보고 고르게 하는 것이 이 UI의 의도다.

---

## 4. 보드 구성 규칙 (3×3 = 9문항)

**권장안: 행 = 세부 장르 3개, 열 = 난이도 3단계.** 요청의 "장르와 난이도가 표시"와 격자가 정확히 대응한다.

```
          쉬움    보통   어려움      ← 열: 선택한 시작 난이도부터 3연속
  화학  │  ▣  │  ▣  │  ▣  │
  물리  │  ▣  │  ▣  │  ▣  │        ← 행: 선택한 주제의 세부 장르 3개
  천문  │  ▣  │  ▣  │  ▣  │
```

- **열(난이도)**: PLAN2에서 정한 "시작 난이도 선택"이 여기로 흡수된다. 1부터면 쉬움·보통·어려움, 3부터면 어려움·전문가·매니아.
- **행(장르)**: 선택한 broad 주제(과학)에 속한 narrow 장르(화학/물리/천문/인체/생물/단위) 중 **해당 난이도 문항을 가진 것 3개를 무작위**로.
- **⚠ 폴백 필수**: 세부 장르는 현재 문항이 크게 부족하다(단위 3개, 국기 6개, 시차 9개). 3개 장르를 못 채우면 그 행은 **broad 주제 전체("과학")**에서 뽑고 툴팁에도 broad 이름을 표시한다. 이 폴백이 없으면 보드 자체를 못 만든다.
- 보드 안에서는 문항이 중복되지 않는다. 직전 보드에서 낸 문항도 가급적 제외(최근 N개 제외 목록 유지).

*대안: 행도 난이도로 두고 장르는 섞는 안(9문항 무작위). 구현은 쉽지만 격자에 의미가 없어져 "표"라는 요청의 의도에서 멀어진다.*

---

## 5. 데이터 모델 — **테이블 재생성 없이 ALTER만으로 된다**

기존 `runs`를 그대로 재사용한다. `mode` 컬럼이 이미 있고(`'solo'` 기본값) 서버 채점용 `served_json`도 있다.

```sql
-- migrations/000N_add_board_mode.sql
ALTER TABLE runs ADD COLUMN board_json TEXT;   -- 9칸 상태
```

`board_json` 구조 (**정답·해설은 넣지 않는다** — 서버 채점 원칙 유지):
```json
[{ "questionId": "...", "topicId": "chemistry", "difficulty": 2,
   "solved": true, "correct": false, "given": "산소" }, ...]
```

- 보드 생성 시 서버가 9문항 id를 `served_json`에 박아둔다 → 클라이언트가 내지 않은 문항의 답을 제출하는 걸 막는 현행 보안 모델(PLAN 7.5절 A안)이 그대로 유지된다.
- 완료 판정: 9칸 전부 `solved` → 기존 `finalizeRun()` 경로로 점수 확정.
- **고아 판 정리**: 새 보드를 시작하면 그 유저의 기존 `in_progress` 보드를 `abandoned`로 바꾼다. 지금은 `abandoned` 상태를 쓰는 코드가 없어서 프로덕션에 버려진 `in_progress` 행이 쌓여 있을 가능성이 높다 — 이 작업에서 같이 정리한다.

### API 추가 (3개)
| 엔드포인트 | 역할 |
|---|---|
| `POST /api/boards` | 주제 + 시작 난이도로 새 보드 발급 (9문항 뽑아 `runs` 생성) |
| `GET /api/boards/active` | **이어하기** — 진행 중인 보드 반환. 미풀이 칸은 문항 본문만, 푼 칸은 채점 결과(정답·해설 포함) |
| `POST /api/boards/:id/answer` | 문항 1개 즉시 채점 → `board_json`·`question_stats` 갱신 → 정답·해설 반환 |

`src/api.ts`(EP-5 계층)에만 함수를 추가하고 화면은 fetch를 직접 부르지 않는다 — 기존 규칙 유지.

### 프론트
- `Screen`에 `{ kind: 'board'; ... }` 추가.
- **앱 시작 시 `GET /api/boards/active`를 한 번 호출**해 진행 중인 보드가 있으면 바로 복원한다. 이게 "나갔다 들어와도 이어서" 요구의 핵심이다.
- 클라이언트에 별도 저장(localStorage)은 두지 않는다 — 서버가 단일 진실 원천이고, 기기를 바꿔도 이어진다.

---

## 6. 점수·랭킹

- 보드 점수 = Σ(정답 문항 난이도 × 10). **만점은 난이도 구간에 따라 다르다**: 1~3 구간이면 (1+2+3)×3×10 = **180점**, 3~5 구간이면 (3+4+5)×3×10 = **360점**.
- "언제든 다시 풀 수 있다"는 요구 때문에 **누적 점수는 쓸 수 없다**(반복하면 무한히 오른다). 다만 현행 `topic_best`가 이미 **최고점 하나만 남기는** 구조라 반복 자체는 문제가 되지 않는다.
- 문제는 **스테이지 판 점수(최대 2250)와 보드 점수(180~360)가 같은 `topic_best`에 섞이면 안 된다**는 것. → `topic_best`에 `mode` 컬럼을 추가하고 PK를 `(uid, topic_id, season_id, mode)`로 확장한다.
- **🔑 PLAN2의 P0에 이미 `topic_best` PK 확장(season_id) 마이그레이션이 예정돼 있다. `mode`를 그때 같이 넣으면 위험한 테이블 재생성을 두 번이 아니라 한 번만 하면 된다.** 이 순서를 놓치면 재생성을 한 번 더 해야 한다.
- 난이도 구간별로 만점이 다르므로, 랭킹 표시는 점수 그대로가 아니라 **구간을 함께 표기**하거나 보드 모드 전용 보드를 따로 두는 편이 읽기 쉽다. (P2 구현 시 확정)

---

## 7. PLAN2 재조정

| PLAN2 항목 | 조정 |
|---|---|
| **C2 주제 플레이(15스테이지 + 시작 난이도)** | **보드 모드로 교체.** 15스테이지 = 최대 75문항으로 피로감이 더 커진다. 시작 난이도 선택은 보드의 열(난이도 구간) 선택으로 살아남는다 |
| 기존 12스테이지 판 | 유지할지 폐지할지 결정 필요(9절 ②). 유지한다면 "정주행 모드"로 이름을 바꿔 남기는 안 |
| **C1 타워** | 그대로. 오히려 역할이 선명해진다 — **긴 호흡은 타워, 짧은 호흡은 보드** |
| P0 마이그레이션 | `topic_best` PK에 `mode`도 함께 추가(6절) |
| 로드맵 순서 | **보드 모드를 P2 자리에 놓고 우선순위를 올린다.** 피로감은 지금 사용자가 실제로 겪고 있는 문제라 신규 콘텐츠보다 앞선다 |

수정 로드맵: **P0**(난이도5 + 시즌제 + `topic_best` PK에 season_id·mode) → **P1**(문항 보충) → **P2 보드 모드** → P3 업적 → P4 타워 → P5 세부 장르

보드 모드는 난이도5가 없어도 1~4 구간만으로 동작하므로, 급하면 **P0/P1보다 먼저 낼 수도 있다**(단, 그 경우 `topic_best` 재생성을 두 번 하게 된다 — 6절 참고).

---

## 8. 구현 단계

| 단계 | 내용 |
|---|---|
| P2-a | `runs.board_json` 마이그레이션 + 보드 발급/조회/채점 API 3종 (+ `abandoned` 정리) |
| P2-b | 보드 구성 로직 — 장르×난이도 격자, 문항 부족 시 broad 폴백, 최근 출제 제외 |
| P2-c | 프론트 — 3×3 격자 UI(우측/모바일 상단), 칸 상태 4종, 툴팁(hover+focus), 문제 영역 |
| P2-d | 앱 시작 시 이어하기 복원, 보드 완료 → 결과·새 보드 발급, 랭킹 반영 |

## 9. 결정이 필요한 것

| # | 질문 | 영향 |
|---|---|---|
| ① | 격자 구성 — 행=세부 장르 / 열=난이도 (권장) vs 9칸 무작위 | 4절, 폴백 복잡도 |
| ② | 기존 12(15)스테이지 판은 폐지인가, "정주행 모드"로 유지인가 | 7절, 랭킹 지표 수 |
| ③ | 보드 점수를 랭킹에 반영하나, 연습 모드로 뺄 것인가 | 6절, `topic_best` PK 설계 |
| ④ | 보드는 9칸 완료 후에만 새로 받나, 아무 때나 새로 받을 수 있나 | 어뷰징(쉬운 보드만 골라 새로고침) 여부 |

## 10. 리스크

1. **모바일 hover 부재** — 툴팁에만 정보를 두면 모바일에서 장르·난이도를 알 수 없다. 칸 상시 표시가 필수(3절).
2. **세부 장르 문항 부족** — 폴백 없이는 보드 생성 자체가 실패한다(단위 3문항, 국기 6문항).
3. **`topic_best` 재생성 횟수** — PLAN2 P0와 합치지 않으면 위험한 마이그레이션을 두 번 하게 된다(6절).
4. **버려진 `in_progress` 행** — 지금까지 정리 코드가 없어 프로덕션에 쌓여 있을 것이다. 이어하기 기능이 이걸 되살려 보여주면 안 되므로, 보드 모드에만 이어하기를 적용하고 기존 `solo` 판은 정리 대상으로 둔다.
5. **자유 선택의 부작용** — 쉬운 칸만 풀고 어려운 칸을 남기는 패턴이 생긴다. 실패가 없는 설계라 문제는 아니지만, 보드 완료 보상(업적·점수)이 있어야 끝까지 채우게 된다.
