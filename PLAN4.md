# PLAN4 — 커뮤니티 신뢰 신호 + 문항 품질 기준 + 피드백 창구

작성일 2026-08-13. **구현 착수 전 설계 문서.** PLAN2·PLAN3와 함께 읽는다.

---

## 1. 이번 요청을 기존 계획에 매핑

사용자가 이번에 준 항목 중 상당수는 **이미 PLAN2/PLAN3에 있는 걸 재확인**한 것이고, **완전히 새로운 것은 4개**다.

| 이번 항목 | 상태 |
|---|---|
| 업적 시스템(난이도·해결 횟수 기반) | PLAN2 A4에 이미 동일 설계 존재 — 재확인만 |
| "12문제 부담" | PLAN3(보드 모드)가 정확히 이 문제를 다룸 — 재확인만 |
| 전문가급 문제·레벨 추가 | PLAN2 A1/A2(매니아 난이도)에 이미 있음 — 재확인만 |
| "정답 피드백 부족" | **코드로 확인된 실제 결함** — 2절 |
| 주제 추천/비추천 | **신규** — 3절 |
| 유저 피드백 신청 화면 | **신규** — 4절 |
| "문제에 대해 정확한 정의가 필요" → 문항 작성 기준 명문화 | **신규(문서 산출물)** — 5절 |
| "기획 인원끼리 강점 파악" → 장르별 대표 배지 표시 | **신규(A4에 얹는 표시 요구사항)** — 6절 |

---

## 2. "정답 피드백 부족" — 코드로 확인된 원인

`Quiz` 컴포넌트(src/App.tsx:276-351)는 스테이지 5문항을 전부 답해야 `제출` 버튼이 뜨고, 그제서야 `StageResult`에서 5문항 전체의 정답·해설이 한꺼번에 나온다. **1번 문항을 맞았는지는 5번 문항까지 다 풀어야 안다.**

- PLAN3(보드 모드)는 애초에 "문항 1개씩 즉시 채점"으로 설계돼 있어 이 문제를 그대로 해결한다. 별도 작업 불필요 — **PLAN3 우선순위를 올리는 근거가 하나 더 늘었다.**
- 다만 기존 12(15)스테이지 판을 유지하기로 하면(PLAN3 §9 결정 ②, 아직 미정) 그 모드의 배치 피드백 문제는 그대로 남는다. 이 문서에서 새로 결정하지 않고, PLAN3의 열린 결정 사항으로 남겨둔다.

---

## 3. 커뮤니티 주제 추천/비추천 — 신규, 커뮤니티 전용

**적용 범위: 유저 창작마당(커뮤니티) 주제만.** 공식 주제는 이미 검증된 콘텐츠라 신호가 필요 없다는 사용자 결정을 따른다. 랭킹에 전혀 영향을 주지 않는 순수 발견·품질 신호다 — 커뮤니티 콘텐츠를 공식 랭킹과 분리하는 기존 경계선(인수인계.md)을 그대로 지킨다.

### 스키마 — 새 테이블 1개, 위험한 재생성 없음
```sql
CREATE TABLE IF NOT EXISTS community_topic_votes (
  uid        TEXT NOT NULL REFERENCES users(uid),
  topic_id   TEXT NOT NULL REFERENCES topics(id),
  vote       INTEGER NOT NULL CHECK(vote IN (-1, 1)),
  updated_at TEXT NOT NULL,
  PRIMARY KEY (uid, topic_id)
);
```
`ALTER`나 재생성이 필요한 기존 테이블을 건드리지 않는다 — PLAN2의 위험한 마이그레이션들과 완전히 무관하게 독립적으로 진행 가능.

### 규칙 (직접 정함 — 사소한 결정이라 사용자 확인 없이 합리적으로 정하고 명시)
- 로그인+닉네임 있는 유저만 투표(콘텐츠 생성과 같은 `requireAuthor` 게이트 재사용).
- **자기 주제에는 투표 불가.**
- 유저당 주제 하나에 투표 하나(PK가 보장), 다시 누르면 취소/변경.
- **비추천이 쌓여도 자동으로 숨기거나 비활성화하지 않는다.** 정렬·발견에만 쓰는 신호이고, 자동 모더레이션은 의도적으로 넣지 않는다 — 몰아주기 비추천으로 콘텐츠가 자동으로 사라지는 악용을 막기 위함.

### API/화면
- `POST /api/community/topics/:id/vote` body `{ vote: 1 | -1 | 0 }` (0 = 취소)
- `handleListCommunityTopics` 응답(`CommunityTopicPayload`)에 `upvotes`/`downvotes`(또는 순합계) 추가
- Workshop.tsx 주제 목록에 추천 버튼 + 정렬 옵션("인기순") 추가

---

## 4. 유저 피드백 신청 화면 — 신규, 앱 전반 건의사항

**용도: 버그·아이디어·불편사항 등 자유 서술형 제안함.** 특정 문항 신고나 커뮤니티 콘텐츠 검토 요청이 아니라 앱 전반에 대한 채널이다.

### 스키마 — 새 테이블 1개
```sql
CREATE TABLE IF NOT EXISTS feedback (
  id         TEXT PRIMARY KEY,
  uid        TEXT NOT NULL REFERENCES users(uid),
  category   TEXT CHECK(category IN ('bug','idea','other')) NOT NULL,
  body       TEXT NOT NULL,
  created_at TEXT NOT NULL
);
```

### 정한 것 (직접 정함)
- **로그인 불필요** — 세션 uid(익명 포함)만 있으면 제출 가능. 콘텐츠 생성과 달리 진입장벽을 낮추는 게 목적.
- 본문 길이 상한(예: 1000자)만 자동 검증, 그 외 필터링 없음.
- 제출 후 "보내짐" 확인만 보여주고 상태 추적 UI(처리 중/완료 등)는 만들지 않는다 — 과설계.

### ⚠ 관리자 화면이 없다는 문제
이 앱에는 **관리자용 조회 화면이 전혀 없다.** 피드백이 쌓여도 지금 구조로는 `wrangler d1 execute --remote --command "SELECT ..."`로 직접 DB를 조회하는 것 말고는 읽을 방법이 없다. 이번 범위에서는:
- 제출 화면·API만 만들고, **조회는 당분간 DB 직접 쿼리로 충분하다고 가정**한다(소규모 프로젝트 관례상 합리적).
- 피드백 양이 늘어 직접 조회가 버거워지면 그때 관리자 화면을 별도 항목으로 논의한다. **지금 만들지 않는다** — 요청 범위를 넘는 과설계.

---

## 5. 문항 작성 기준 명문화 — 신규 문서 산출물 (코드 변경 없음)

**결정: "문제에 대해 정확한 정의가 필요하다" = 문항 작성 기준을 문서로 명문화하는 것.**

현재 품질 검증은 형식만 본다 — `scripts/validate.mjs`(중복·문항 수), `worker/community.ts`의 `validateQuestion`(빈 값·선택지 개수·URL 형식). **모호한 질문, 복수 정답 가능성, 표기 불일치 같은 의미적 품질은 아무도 안 잡는다.** 특히 커뮤니티 문항은 사람 검수가 아예 없어서(인수인계.md) 이 공백이 가장 크다.

### 산출물
- `docs/question-authoring-standard.md` 신설 — 다음을 명문화:
  - **단일 정답 원칙**: 질문에 정답이 하나로 수렴하지 않으면 안 됨(특히 TEXT_INPUT — "이 동물은?" 대신 "이 새의 학명은?"처럼 답의 형태를 질문 안에서 고정)
  - **모호성 금지**: "가장", "제일" 같은 최상급 표현은 근거(공식 기록·통계)가 명확할 때만 — `worker/community.ts`의 `SUPERLATIVE` 배열이 이미 이 문제의식을 갖고 있었다는 근거
  - **표기 통일**: 단위·외래어 표기(예: "칼슘" vs "Ca"), 학명 표기 규칙(새 맞추기 사례처럼)
  - **난이도 판정 기준과의 정합**: `data/topics.json`의 `difficultySpec` 규칙·예시를 참조하게 연결
  - **문항 길이·선택지 형식**: 기존 `BODY_MAX=80`자 등 이미 있는 제약을 근거와 함께 재수록
- 적용 대상: `prompts/generate-questions.md`(AI 생성 프롬프트에 반영), `scripts/review.mjs`/`review.html`(검수 화면에 체크리스트로 노출), 커뮤니티 창작마당 문항 작성 폼(Workshop.tsx)에 짧은 가이드 링크/툴팁 추가.
- **자동 검증 규칙 확대는 이번 범위에 넣지 않는다** — 의미적 품질(모호성 등)은 기계가 못 잡는 영역이 많아서, 우선 "기준 문서 + 검수 화면 체크리스트"로 사람이 참고하게 하고, 규칙화할 수 있는 게 드러나면 그때 `validate.mjs`에 편입한다.

---

## 6. 장르별 대표 배지 표시 — PLAN2 A4에 표시 요구사항 추가

**결정: "기획 인원끼리 강점 파악" = 친구 단위 그룹 랭킹이 아니라, 장르별 대표 배지/타이틀을 프로필·랭킹에 노출하는 것.** 별도 그룹 기능을 만들지 않고, 기존 통합 랭킹·주제별 순위 화면에 "이 사람은 물리 강자"가 드러나게 하는 정도로 충분하다는 결정.

이건 새 기능이 아니라 **PLAN2 A4(업적 시스템)의 "④ 장르 마스터"에 표시 요구사항을 얹는 것**이다.

### 필요한 추가 설계
- "장르 마스터" 기준 확정 필요(PLAN2 A4는 4종 업적만 나열하고 기준을 안 정함): 예 — 특정 narrow 주제에서 만점 달성, 또는 해당 장르 누적 정답 수 N개 이상.
- 유저가 여러 장르 마스터 배지를 가질 수 있으면 "대표 배지" 하나를 어떻게 고를지 필요 — 최근 획득순 또는 유저가 직접 지정(설정 화면에 "대표 배지 선택" 추가). **직접 지정 방식을 권장** — 자동 선택보다 유저가 원하는 정체성을 드러낼 수 있어 "강점 파악"이라는 취지에 더 맞는다.
- `users`에 `display_badge_id` 컬럼(nullable, ALTER로 충분) 추가해 캐시.
- `RankingEntry`(src/types.ts) 응답에 `badge` 필드 추가, 랭킹 화면·결과 화면에서 닉네임 옆에 표시.

이 작업은 PLAN2의 P3(업적) 안에서 같이 하는 게 맞다 — 업적 테이블·판정 로직이 먼저 있어야 배지도 존재한다. **새 단계를 만들지 않고 P3의 범위에 포함시킨다.**

---

## 7. 로드맵 재조정

이번에 나온 신규 항목 3개(추천/비추천, 피드백 화면, 문항 기준 문서)는 **공통점이 있다 — 전부 PLAN2의 위험한 마이그레이션(난이도5, 시즌제)과 완전히 무관하고, 새 테이블 추가나 문서 작성뿐이라 재생성 위험이 없다.**

→ **P0(난이도5+시즌제)보다 먼저, 또는 병행해서 진행할 수 있다.** 오히려 먼저 하는 게 낫다:
- 문항 작성 기준 문서는 **P1(콘텐츠 대량 작성)보다 먼저** 있어야 새로 쓰는 문항이 처음부터 기준을 따른다.
- 추천/비추천·피드백 화면은 사용자가 실제로 쓰는 "커뮤니티 활성화" 축이라 위험한 스키마 작업을 기다릴 이유가 없다.

### 수정된 로드맵
| 단계 | 내용 | 의존 |
|---|---|---|
| **P-1 (신규, 최우선)** | 문항 작성 기준 문서화 + 커뮤니티 추천/비추천 + 유저 피드백 화면 | — (완전 독립) |
| P0 | 배포 브랜치 정리 / 난이도 1~5 확장 / 시즌제 스키마 | P-1과 무관, 병행 가능 |
| P1 | 문항 보충(P-1의 기준 문서 적용) | P0, P-1 |
| P2 | 보드 모드(PLAN3) | P0, P1 |
| P3 | 업적 시스템 **+ 장르별 대표 배지 표시** | P2 |
| P4 | 타워 | P1, P2 |
| P5 | 세부 장르 개방 | P1 이후 상시 |
| (보류) | 배틀 | — |

---

## 8. 리스크 및 주의점

1. **관리자 화면 부재** — 피드백을 이번 범위에서 UI로 조회하지 않기로 했으므로, 실제로 쌓인 피드백을 사용자가 주기적으로 DB 직접 조회로 확인해야 한다. 잊혀지기 쉬운 운영 부담이니, 나중에라도 이 부담이 커지면 별도 관리자 화면 논의가 필요하다.
2. **추천/비추천의 악용 여지** — 자동 모더레이션이 없으므로 몰아주기 추천으로 품질 낮은 주제가 상위 노출될 수 있다. 지금은 발견 신호일 뿐이고 랭킹에 영향이 없어 피해가 제한적이지만, 정렬 알고리즘(단순 합산 vs 최근 가중치 등)은 실사용 확인 후 조정 여지를 남긴다.
3. **문항 작성 기준의 실효성** — 문서만으로는 강제력이 없다. 특히 커뮤니티 문항은 사람 검수가 없는 구조라(인수인계.md) 기준을 어겨도 걸러지지 않는다. 이 문서가 실제로 효과가 있으려면 창작마당 폼에 눈에 띄게 노출돼야 한다(5절에 반영함).
4. **장르 마스터 기준 미확정** — 6절에서 지적한 대로 구체적 임계값은 P3 구현 시점에 정해야 한다. 지금은 "직접 지정형 대표 배지"라는 표시 방식만 확정.

---

## 9. 결정 기록 (이번 대화)

| # | 질문 | 결정 |
|---|---|---|
| ① | "문제에 대한 정확한 정의" | 문항 작성 기준 명문화 |
| ② | 유저 피드백 화면 용도 | 앱 전반 건의사항 (특정 문항 신고 아님) |
| ③ | "기획 인원끼리 강점 파악" | 장르별 대표 배지/타이틀 표시 (그룹 랭킹 아님) |
| ④ | 추천/비추천 적용 범위 | 커뮤니티(유저 창작) 주제만 |
