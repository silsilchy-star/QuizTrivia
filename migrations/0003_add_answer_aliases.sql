-- 단답형(TEXT_INPUT) 문항의 "또 맞는 답" 목록.
--
-- 단답형은 지금 정답 문자열 하나와만 일치한다(대소문자·공백만 정규화). 한국어
-- 단답에서 이건 너무 가혹하다 — 정답이 "에베레스트"일 때 "에베레스트산"을 적은
-- 사람은 알고도 틀린 처리를 받는다. 그런 표기 흔들림을 문항마다 적어둔다.
--
-- ⚠ 컬럼 추가라 옛 워커도 견딘다(인수인계 교훈 17번). 배포 중 워커 배포가
--    실패해 DB만 앞서가도, 이 컬럼을 모르는 옛 워커는 그냥 무시할 뿐이다.
--
-- ⚠ db/schema.sql에는 넣지 않는다. 기존 테이블의 컬럼 변경은 마이그레이션으로만
--    한다(교훈 10번) — schema.sql의 CREATE TABLE은 이미 테이블이 있는 DB에서
--    실행되지 않으므로 거기 적어봐야 프로덕션에는 반영되지 않는다.
--    0001(video_kind/video_id)과 같은 취급이다.
ALTER TABLE questions ADD COLUMN answer_aliases_json TEXT;
