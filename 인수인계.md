# QuizTrivia 인수인계

다른 작업창(세션)에서 이어받을 때 이 문서 하나로 맥락을 파악할 수 있게 정리했다. 각 항목의 자세한 내용은 `docs/*.md`에 커밋 단위로 남아있으니, 특정 결정의 배경이 궁금하면 거기부터 본다. **2026-08-13 최신화** — v2 로드맵(PLAN2/3/4) 설계 완료 후 P0·P1 일부를 구현·배포한 시점 기준.

## 프로젝트가 뭔가
**랭킹 기반 다주제 학습 퀴즈 게임.** 설계 원안은 저장소 루트의 `PLAN.md`(v1.5) — 다만 Firebase→Cloudflare 전환, Day 5~6 결정 등은 이미 실제로 다르게 진행됐고, **지금은 그 위에 `PLAN2.md`/`PLAN3.md`/`PLAN4.md`(v2 로드맵 — 아래 참고)가 얹혀 있다.** 무엇이 "지금 실제로 있는지"는 **이 문서 + `docs/*.md` + 실제 코드**를 기준으로 판단할 것. `PLAN.md`는 갱신 안 함. `PLAN2/3/4.md`는 **구현 착수 전 설계 문서**였다가 지금 PLAN2의 P0·P1 일부가 실제로 구현·배포됐다 — 진행 상황은 아래 "v2 로드맵 진행 상황" 참고.

- 리포지토리: `silsilchy-star/QuizTrivia`
- 작업 브랜치: **`claude/work-handover-file-check-zknp0b`** (2026-08-13 후속 세션에서 이 브랜치로 전환. `claude/handover-matters-review-gs5597`도 `deploy.yml` 트리거에 남아있음 — 둘 다 push=배포이니 주의. 완전히 폐기된 `claude/repo-permissions-check-ff2du7`은 트리거에서 제거했다)
  - ⚠ **세션마다 브랜치가 새로 생기는 구조라, 새 세션은 자기 브랜치가 (a) 최신 커밋에서 출발했는지 (b) `deploy.yml` 트리거에 있는지를 가장 먼저 확인해야 한다.** 실제로 이 세션은 8커밋 뒤처진 지점에서 시작했고, 그대로 트리거만 추가해 push했다면 `build-seed.mjs`의 `DELETE FROM questions WHERE author_uid IS NULL AND id NOT IN (...)`이 **프로덕션의 난이도5 문항 60개를 조용히 삭제**할 뻔했다(에러 없이 콘텐츠만 롤백되므로 발견이 매우 어려움).
- 배포 주소: `https://quiztrivia.silsilchy.workers.dev`
- 배포 방식: 위 두 브랜치 중 하나에 push하면 `.github/workflows/deploy.yml`이 **자동으로** 프로덕션에 배포됨 (별도 승인 절차 없음 — push = 배포이니 주의)

## 아키텍처 한눈에
| 계층 | 선택 |
|---|---|
| 프론트 | React 19 + TypeScript + Vite |
| 백엔드 | Cloudflare Workers (`worker/index.ts` 등) |
| DB | Cloudflare D1 (SQLite), 바인딩 이름 `DB` |
| 정적 파일 | Workers Static Assets (`dist/` 빌드 결과를 같은 Worker가 서빙) |
| 인증 | 자체 익명 uid 발급 + Google OAuth(Arctic 라이브러리)로 계정 승계 |

- `src/api.ts` — **EP-5 저장소 계층**. 화면 코드는 절대 `fetch`를 직접 안 부르고 이 파일만 거친다. 백엔드를 바꿀 일이 생기면 여기만 고치면 된다는 게 설계 의도.
- `src/types.ts` — 프론트/워커가 공유하는 타입 정의. 둘 다 이 파일을 include. `Difficulty`는 이제 `1|2|3|4|5`.
- 서버 채점(PLAN 7.5절 A안) — 정답/해설은 채점 전까지 클라이언트에 안 내려가고, 서버가 출제 시점에 문항 id를 `runs.served_json`에 박아둬서 안 낸 문항 답 제출을 막는다.

## 지금 라이브 상태로 있는 것
- **핵심 게임 루프는 아직 구(舊) 방식 그대로다**: 주제 선택 → 12스테이지(난이도 1→4 구간별 3스테이지씩) → 스테이지당 5문항, 4문항 이상 정답이어야 클리어 → 판 전체 무중복 → 점수 = Σ(정답 문항 난이도×10), 만점 1500. **`PLAN3.md`의 "문제 보드(3×3)" 모드가 이걸 완전히 대체할 예정이지만 아직 미구현(P2, 아래 로드맵 참고).**
- **DB/백엔드는 난이도 1~5를 전부 지원**하도록 이미 확장됨(P0 완료) — 다만 위 구 게임 루프(`difficultyForStage()` 등)는 여전히 1~4만 쓴다. 이건 **의도적으로 방치한 죽은 코드**다 — P2에서 보드 모드로 통째로 교체될 예정이라 지금 고쳤다 다시 지우는 게 낭비이기 때문. 새 타워 모드(P4)가 자체 층↔난이도 곡선으로 5단계를 쓰게 된다.
- **공식 콘텐츠 3개 — 과학·지리·스포츠**: 각 **100문항(난이도당 20, 1~5 전부)**으로 전부 `active`. (2026-08-13 이전엔 80문항/난이도1~4만 있었음 — 이번에 매니아(난이도5) 20개씩 60개 추가.)
- **새 맞추기(birds) 주제**: 24문항만 있어서 아직 `draft`(비공개). **100개(난이도당 20, 1~5 전부)** 채워야 활성화됨 — 즉 **76개 더 필요**. (예전 인수인계엔 "56개"라고 적혀 있었는데, 그건 난이도 1~4 기준이었다. 게이트가 1~5 전체로 바뀌면서 필요량이 늘었다 — 아래 "남은 일" 참고.)
- **소셜 로그인 + 계정 승계**: 구글 로그인, 익명 플레이 기록이 로그인 시 그대로 이어짐. 로그인 안 해도 플레이는 가능(랭킹 집계만 안 됨).
- **랭킹**: 통합 2개 보드(전체 기간/주간)만 랭킹 메뉴에 노출, 주제별 순위는 결과 화면 안에서만. 로그인(계정 승계) 안 한 유저는 랭킹 집계 대상이 아님. **⚠ 이 구조는 PLAN2 C4에 의해 폐기 예정** — 타워 모드(P4) 완성 시 "타워 단일 랭킹 보드"로 대체되고, 시즌제 개념 자체도 없앤 상태다. 지금 코드는 아직 안 바뀜.
- **유저 창작마당**: 로그인 + 닉네임 설정한 유저가 직접 주제·문제를 만들 수 있음. 자동 검증만 통과하면 검수 없이 즉시 공개, 활성화 하한은 공식(20/난이도)보다 훨씬 낮은 5/난이도 — **이제 난이도 1~5 전부 기준**(전엔 1~4). 랭킹에는 전혀 반영 안 됨. 실제 사용자가 만든 주제가 프로덕션에 있음.
  - 문제 유형: 객관식(MULTIPLE_CHOICE) / 숫자입력(NUMERIC_INPUT) / 단답형(TEXT_INPUT, 이미지 URL 선택 첨부 가능, 정답은 대소문자·공백 차이 무시).
  - 창작마당 난이도 선택 라벨을 PLAN2 공식 명칭("쉬움/보통/어려움/전문가/매니아")으로 통일함(이전엔 "매우 쉬움/쉬움/보통/어려움"으로 따로 놀았음).
  - 내 주제 삭제 기능 있음(확인 단계 거침, 되돌릴 수 없음).

## v2 로드맵 진행 상황 (`PLAN2.md`/`PLAN3.md`/`PLAN4.md`)

이 세 문서가 다음 개발 방향의 설계 원본이다. 순서: PLAN2(전체 로드맵) → PLAN3(보드 모드 상세) → PLAN4(커뮤니티 신뢰·품질 기준). PLAN2 §8에 세 문서를 교차검증하며 확정한 결정들이 정리돼 있다.

**핵심 설계 결정 요약**
- **C1 타워 오르기** — 전 주제 혼합 무한 타워, 유일한 랭킹 보드가 될 예정. 1~5층 난이도1 … 21층 이상은 난이도5 반복.
- **C2 구 스테이지 판(12스테이지)** — **폐지 확정, PLAN3의 "문제 보드(3×3)" 모드로 완전 대체.** "시작 난이도 선택" UI도 같이 없어진다.
- **C3 배틀 모드** — 로드맵에서 제외(보류).
- **C4 랭킹** — 통합 2개 보드+주제별 순위 구조 폐기, 시즌제도 폐기. 타워 단일 보드로 단순화.
- **A1+A2 난이도 5단계** — ✅ **완료(P0+P1 일부)**.
- **A3 세부 장르 개방** — 공식 하한(20/난이도) 유지, 문항 차는 순서대로 개방. 아직 미착수.
- **A4 업적 시스템** — 아직 미착수. 타워의 답안 채점 경로에서 판정하도록 재설계됨(구 `finalizeRun()` 기반 설계는 폐기).

**로드맵 표** (PLAN2.md §4)

| 단계 | 내용 | 상태 |
|---|---|---|
| **P0** | 배포 브랜치 정리 / 난이도 1~5 확장(migration 0003, 타입·상수·스크립트 일괄 수정) | ✅ **완료·배포됨** (커밋 `d4a5982`) |
| **P1** | 공식 3주제 난이도5 문항 60개 + 새 맞추기 76개(구 계획 56개에서 늘어남) 보충 | 🟡 **부분 완료·배포됨** — 3주제 60개는 끝(커밋 `2c7c224`), **새 맞추기 76개가 남음** |
| **P2** | 문제 보드 모드(PLAN3.md) — 기존 스테이지 판(C2) 완전 대체 | ⬜ 미착수 |
| **P3** | 업적 시스템(A4) — 테이블 + 판정 로직 + 업적 화면 | ⬜ 미착수 (P2 선행 필요) |
| **P4** | 타워 오르기(C1) — 혼합 출제, 체크포인트, 타워 랭킹 보드 | ⬜ 미착수 (P1·P2 선행 필요) |
| **P5** | 세부 장르 개방(A3) — 문항 차는 대로 자동 활성 | ⬜ 미착수, 상시 진행 항목 |

PLAN2 §5 리스크에 적힌 핵심 판단: **"P1(콘텐츠)이 전체 일정을 지배한다."** 스키마·코드는 하루 단위지만, 세부 장르까지 채우려면 결국 문항이 1,000개 단위로 필요해진다. `build-seed.mjs`가 이미 문항 324개에서 D1 문장 크기 한도에 걸렸던 것도 이 방향과 무관하지 않다(아래 교훈 8번).

## ⚠ 반드시 알아야 할 사고와 교훈 (다시 반복하면 안 됨)
1. **`db/schema.sql`의 DROP TABLE 사고** (`docs/2026-08-13-critical-schema-drop-fix.md`) — 이 파일은 배포마다 무조건 재실행되는데, 한때 여기 `DROP TABLE IF EXISTS users/runs/topic_best/...`가 남아있어서 **진짜 사용자 생긴 뒤의 모든 배포가 그 테이블들을 통째로 비우고 있었다.** 지금은 전부 `CREATE TABLE IF NOT EXISTS`로 고쳐졌지만 — **`db/schema.sql`에는 절대 DROP TABLE을 쓰면 안 된다.**
2. **스키마를 바꿔야 하면 `migrations/` 폴더에 새 파일을 추가한다** (`npx wrangler d1 migrations create quiztrivia <이름>`), `db/schema.sql`은 건드리지 않는다. `db/schema.sql`은 매 배포 재실행되는 멱등 베이스라인이고, `migrations/`는 `d1_migrations` 테이블로 한 번만 적용되는 걸 추적한다.
   - **CHECK 제약을 넓혀야 하면(예: enum 값 추가) `ALTER TABLE`로 안 되고 테이블 재생성이 필요하다.** 이때 D1이 까다롭다 — `PRAGMA foreign_keys=OFF`도 `defer_foreign_keys=ON`도 "참조당하는 테이블 DROP"을 곱게 봐주지 않았다(로컬 테스트로 직접 확인, `migrations/0002_add_text_input_and_images.sql` 참고). **해법**: 그 테이블을 참조하는 자식 테이블들의 FK 선언부터 먼저 없애도록 재생성한 뒤에야, 대상 테이블을 안전하게 재생성할 수 있다. 한번 자식 FK를 없애두면, 그 다음부터 그 테이블 자체를 다시 재생성할 땐 자식을 또 안 건드려도 된다(`migrations/0003_add_difficulty_5.sql`이 실제로 이 덕을 봄 — `questions` 하나만 재생성).
   - 배포 전 반드시 로컬에서 **(a) 기존 데이터 있는 DB에 적용해보고 무결성 확인, (b) 완전히 빈 DB에 `schema.sql`부터 전체 마이그레이션을 순서대로 적용해보고 확인** — 이 두 가지를 다 통과한 뒤 배포한다.
3. **`scripts/build-seed.mjs`가 커뮤니티 문항을 지울 뻔한 사고** (`docs/2026-08-13-image-text-input-and-birds-topic.md`) — 이 스크립트는 공식 JSON 콘텐츠만 알고 있어서, 정리 로직이 "JSON 승인 목록에 없는 문항은 삭제"였는데 **유저 창작마당 문항(uuid라 JSON에 있을 리 없음)도 걸려서 지워질 뻔했다.** `author_uid IS NULL`(공식 문항은 항상 NULL)로 범위를 좁혀 고쳤다. **앞으로 이 스크립트를 건드릴 땐 커뮤니티 문항이 안전한지 반드시 재확인할 것** — 실제로 2026-08-13 `build-seed.mjs` 청크 수정(교훈 8번) 때도 가짜 커뮤니티 문항을 심어 재확인했다.
4. **커밋 단위로 `docs/*.md` 문서 작성**은 사용자의 스탠딩 지시. 큰 작업(커밋)마다 빠짐없이 남길 것 — 한 번 빠뜨렸다가 나중에 발견해서 보충한 적 있음.
5. **로컬 테스트에 playwright가 필요하면 `npm i -D playwright`로 설치했다가, 테스트 끝나면 반드시 `npm uninstall playwright`로 다시 제거한다.** 커밋에 devDependency로 남기지 않는 관례.
6. **이 작업 환경 자체가 Wikimedia 등 외부 이미지 호스트에 요청을 많이 보내면 속도 제한(429)·연결 재설정에 걸린다.** 이미지 URL이 실제로 유효한지 이 환경에서 완벽히 검증하기 어려우니, 이런 경우 사용자에게 직접 확인을 부탁하는 게 낫다.
7. **`db/schema.sql`은 마이그레이션이 나중에 추가하는 컬럼·CHECK 확장을 미리 반영하면 안 된다** (2026-08-13, `docs/2026-08-13-p0-difficulty-5-schema.md`) — `q_count_5` 컬럼을 `schema.sql`에도 직접 추가했다가, 로컬 이중 검증(교훈 2번)에서 "빈 DB → schema → 마이그레이션 순서 적용" 시 마이그레이션의 `ALTER TABLE ADD COLUMN`과 중복 충돌(`duplicate column name`)이 났다. 원래 관례(0001·0002도 `schema.sql`을 안 건드림)대로 되돌렸다 — **`schema.sql`은 항상 "오래된 상태"로 남고, 마이그레이션들이 순서대로 적용되면서 최종 상태를 완성하는 구조**다. 스키마를 바꿀 때 `schema.sql`도 최신화하고 싶은 유혹이 들어도 참을 것.
8. **`scripts/build-seed.mjs`가 D1 SQL 문장 크기 한도(`SQLITE_TOOBIG`)에 걸린 사고** (2026-08-13, `docs/2026-08-13-p1-difficulty-5-content-and-seed-chunking.md`) — `INSERT INTO questions ...`를 문항 전부 한 문장에 몰아넣는 구조였는데, 264개(~96KB)일 때는 아슬아슬하게 통과했지만 324개(~118KB)에서 D1의 문장 크기 한도를 넘어 배포 파이프라인이 실패할 뻔했다(로컬 스모크 테스트에서 미리 발견). **고침**: `questions`/`question_topics` INSERT를 100행 단위로 여러 문장으로 쪼개도록 수정(`CHUNK_SIZE=100`). **문항이 계속 늘어나는 로드맵(PLAN2 P1·P5)이라 이 한계는 또 걸릴 수 있다** — 혹시 다시 문장 크기 에러가 나면 `CHUNK_SIZE`를 줄이거나 청크 로직이 다른 테이블(topics 등)에도 필요한지 확인할 것.
9. **게이트 로직을 바꾸면 이미 `active`인 주제가 배포 즉시 `draft`로 떨어질 수 있다** — 난이도5를 게이트에 포함시키자마자 과학·지리·스포츠(기존에 `active`였던 것들)가 전부 `draft`로 떨어졌다(난이도5 문항이 0개였으므로). `push=배포`인 브랜치에서 이런 변경은 **실사용자에게 노출된 콘텐츠가 갑자기 사라지는 사고**가 될 수 있다. **`npm run validate`로 게이트 판정 결과를 반드시 push 전에 확인하고**, 활성 콘텐츠가 죽는 방향이면 필요한 콘텐츠를 채워서 게이트를 다시 통과시킨 다음에 push할 것(혹은 최소한 사용자에게 미리 확인받을 것 — 이번엔 `AskUserQuestion`으로 확인받고 콘텐츠부터 채운 뒤 push했다).

## 개발/배포 방법
```bash
npm install
npm run dev          # 프론트만 (vite, API 없음)
npm run worker:dev    # wrangler dev — 로컬 D1 포함 풀스택 (포트는 알아서 지정)
npm run db:local      # 로컬 D1에 schema+migrations+seed 전부 적용
npm run validate      # data/*.json 자동 검증 (에러 있으면 배포 파이프라인도 막힘, 게이트 판정 결과도 여기서 보임)
npm run build:seed    # data/*.json → db/seed.generated.sql (gitignore됨, 커밋 안 함)
npm run review        # 로컬 문항 검수 웹페이지
npx tsc -b             # 전체 타입체크 (프론트+워커 동시)
```
배포 파이프라인(`deploy.yml`) 순서: `npm ci` → `validate` → D1 schema 적용 → **D1 migrations 적용** → `build:seed` → D1 seed 적용 → 프론트 빌드 → Worker 배포. `claude/work-handover-file-check-zknp0b`(현재) 또는 `claude/handover-matters-review-gs5597`(이전 세션) 브랜치 push에 자동 트리거.

새 스키마 변경이 필요하면:
```bash
npx wrangler d1 migrations create quiztrivia <설명>   # migrations/000N_설명.sql 생성
# 그 파일에 ALTER TABLE ... 만 쓴다. CHECK 제약을 넓혀야 하면 위 교훈 2번의 재생성 절차를 따른다.
npx wrangler d1 migrations apply quiztrivia --local   # 로컬에서 먼저 검증
```
로컬 이중 검증(교훈 2번) 절차 예시는 `docs/2026-08-13-p0-difficulty-5-schema.md`에 그대로 남아있다 — `rm -rf .wrangler/state/v3/d1` 후 schema→migrations만 먼저 적용해서 "기존 데이터 있는 DB" 상태를 만들고, 테스트 데이터(커뮤니티 문항 포함)를 심은 뒤 새 마이그레이션을 적용해 무결성을 확인하는 방식.

## 파일 구조
```
PLAN.md                  설계 원안 v1 (v1.5, 갱신 안 함 — 참고용)
PLAN2.md                  v2 로드맵 — 난이도5/타워/랭킹개편/업적, 이 문서가 로드맵의 단일 출처
PLAN3.md                  문제 보드(3×3) 모드 상세 설계 — PLAN2의 C2를 대체
PLAN4.md                  커뮤니티 신뢰 신호 + 문항 품질 기준 + 피드백 창구 설계
인수인계.md               이 문서
docs/*.md                 커밋 단위 작업 기록 (날짜-내용.md)
docs/question-authoring-standard.md
                          문항 작성 기준 (P-1 산출물, 날짜 없는 상시 문서).
                          문항을 쓰기 전에 읽는다 — §7이 작성 전 체크리스트.

worker/
  index.ts                메인 엔트리 — 세션/판 진행/랭킹 라우팅, finalizeRun 등 핵심 로직.
                           ⚠ difficultyForStage()·MAX_RUN_SCORE·TOTAL_STAGES는 구 스테이지 판 전용
                           죽은 코드 — P2에서 보드 모드로 교체할 때 통째로 삭제 예정. 지금은 건드리지 말 것.
  auth.ts                  구글 OAuth (Arctic)
  ranking.ts               랭킹 캐시 계산·조회 — PLAN2 C4에 의해 P4에서 타워 전용으로 대체 예정
  community.ts             유저 창작마당 (주제/문제 생성·삭제), 난이도 1~5 대응 완료

src/
  App.tsx                  메인 화면 (주제선택/퀴즈/결과/랭킹 화면 전환)
  Workshop.tsx              창작마당 화면
  api.ts                    EP-5 저장소 계층 (fetch는 여기서만)
  types.ts                  프론트+워커 공유 타입. Difficulty = 1|2|3|4|5

db/schema.sql              멱등 베이스라인 (배포마다 재실행 — DROP 금지! 마이그레이션 추가분 백포트도 금지, 교훈 7번)
migrations/000N_*.sql       스키마 변경 (한 번만 적용, d1_migrations로 추적)
  0001_add_community_workshop.sql   유저 창작마당(topics.source/author_uid, questions.author_uid)
  0002_add_text_input_and_images.sql TEXT_INPUT 타입 + 이미지 URL, questions 재생성(자식 FK 제거)
  0003_add_difficulty_5.sql          난이도 1~5 확장(questions 재생성), topics.q_count_5 추가

data/topics.json            공식 주제 정의서. difficultySpec은 broad 4개(과학/지리/스포츠/새맞추기)에만
                             있음 — narrow 16개는 부모 상속 구조(예전 문서의 "20개 전부"는 부정확했음).
                             매니아(5) 규칙·예시까지 이 4개에 다 채워져 있음.
data/questions/*.json       공식 문항 원본 (JSON이 소스 오브 트루스). science/geography/sports는
                             난이도1~5 각 20개(100개)씩. birds는 난이도1~4만 각 6개(24개).
db/seed.generated.sql        위 JSON → SQL 생성물 (gitignore, npm run build:seed로 재생성)

scripts/
  validate.mjs              자동 검증 (ERROR면 배포 막힘, WARN이면 사람 검수 필요). 난이도 1~5 대응 완료.
  build-seed.mjs             JSON → SQL, 하한 게이트 자동 판정(draft/active). 난이도 1~5 대응 완료.
                             INSERT문을 100행 단위로 청크(교훈 8번) — 이 부분은 함부로 되돌리지 말 것.
  review.mjs / review.html   로컬 문항 검수 웹페이지
  lib.mjs                    공통 유틸 (GATE_PER_DIFFICULTY=20 등 상수)
prompts/generate-questions.md  문항 생성 프롬프트 템플릿 — 난이도3 전용 절차(씨앗 먼저 확보) 포함
```

## 남은 일 / 다음에 이어서 할 수 있는 것

**우선순위 순서 (PLAN2 로드맵 + PLAN4 §7 재조정 반영):**

> ⚠ **이전 판의 우선순위는 PLAN4 §7과 모순이었다.** 이전 판은 PLAN4 신규 항목들을 6번(후순위)에 뒀지만, **PLAN4 §7은 같은 항목들을 `P-1 (신규, 최우선)`으로 올려놓고 "P0보다 먼저, 또는 병행"이라고 못박았다.** 근거도 명확하다 — **문항 작성 기준 문서는 P1(콘텐츠 대량 작성)보다 먼저 있어야 새로 쓰는 문항이 처음부터 기준을 따른다.** 아래 순서는 PLAN4 §7을 정본으로 따른 것이다.

0. **P-1 — 문항 작성 기준 문서화** (`PLAN4.md` §5) — ✅ **문서 작성 완료: `docs/question-authoring-standard.md`.** birds 76문항을 쓸 때 이 문서를 먼저 읽는다(§7 체크리스트).
   - ⬜ **아직 안 한 것 — 이 기준을 실제 작성 경로에 노출하는 일**: `prompts/generate-questions.md`(AI 생성 프롬프트 반영), `scripts/review.mjs`/`review.html`(검수 화면에 체크리스트 노출), `src/Workshop.tsx`(창작마당 폼에 가이드 링크). `PLAN4.md` §8 ③의 지적대로 **커뮤니티는 사람 검수가 없어서 창작마당 폼에 노출되지 않으면 이 문서의 실효가 거의 없다.**
   - **문서를 쓰다 발견한 실제 공백**: `worker/community.ts:21`의 `SUPERLATIVE` 배열이 **선언만 되고 어디에서도 안 쓰인다**(죽은 상수). 즉 커뮤니티 문항은 최상급 표현·문장형 정답·`seedRef` 검사를 **전부 안 받는다**. 공식 문항은 `validate.mjs`가 WARN으로 잡는다. 상수만 보면 검사되는 줄 착각하기 쉽다. 고치면 유저 입력을 400으로 거절하게 되어 실사용자 영향이 있으므로, `PLAN4.md` §5가 그은 범위("자동 검증 규칙 확대는 이번 범위 밖")를 따라 **별도 결정 사항으로 남겼다.**
   - 같은 P-1 묶음의 커뮤니티 추천/비추천(`PLAN4.md` §3)·유저 피드백 화면(§4)은 아직 미착수. 새 테이블 추가뿐이라 위험한 재생성이 없고 P1과 병행 가능.
1. **P1 마무리 — 새 맞추기(birds) 76문항** (24→100, 난이도당 20). **P-1의 기준 문서를 적용해서 쓴다.**
   - `data/topics.json`의 birds `difficultySpec`은 **난이도 1~5가 이미 다 채워져 있다** (이전 판에 "1~4만 있음, 5는 아직 안 씀"이라고 적혀 있었으나 사실이 아니었다 — 확인 완료). 즉 스펙 작성 단계는 불필요하고, 난이도5 문항 20개를 그 스펙대로 쓰면 된다. 부족분은 난이도 1~4에 각 14개 + 난이도 5에 20개 = 76개.
   - **`NUMERIC_INPUT` 비율은 birds에 한해 면제로 확정**(2026-08-13 사용자 결정). "사진 보고 종 맞추기"가 이 주제의 정체성이라, 비율을 맞추려 날개폭·수명 같은 수치 문항을 넣으면 성격이 흐려진다는 판단. **따라서 `birds: NUMERIC_INPUT 비율 0%` WARN은 계속 남는다 — 미처리 할 일이 아니라 수용된 예외다.** 76개도 전부 사진 기반(객관식/단답형)으로 채운다.
   - 기존 24문항에도 품질 경고가 남아있다 — `bird-113`~`bird-118`은 난이도3인데 `seedRef`(씨앗 출처)가 없다. P-1 기준 문서를 만들 때 이 6건도 같이 정리하는 게 자연스럽다.
   - 사진이 필요한 콘텐츠라 이 환경의 네트워크 제약(교훈 6번)이 걸림돌 — Wikimedia 이미지 URL을 대량 검증하려다 429를 만난다(실측: 3장 중 2장 429). **완성 후 이미지가 실제로 뜨는지는 사용자에게 직접 확인을 부탁할 것.** 76개를 만들기 *전에* 기존 URL 방식(`Special:FilePath`)이 실제로 잘 뜨는지 표본 몇 장을 먼저 확인받으면, 같은 실수로 76개를 다시 만드는 일을 막을 수 있다.
2. **P2 — 문제 보드(3×3) 모드 구현** (`PLAN3.md` 상세 설계 있음). 기존 12스테이지 판을 완전히 대체. 이때 `worker/index.ts`의 `difficultyForStage()`·`MAX_RUN_SCORE`·`TOTAL_STAGES`(죽은 코드)와 "시작 난이도 선택" UI를 같이 삭제한다.
3. **P3 — 업적 시스템** (`PLAN2.md` A4). P2(타워의 답안 채점 경로) 완성 후에 판정 로직을 넣게 설계돼 있음.
4. **P4 — 타워 오르기** (`PLAN2.md` C1). 혼합 출제, 체크포인트, 타워 전용 랭킹 보드로 기존 통합 랭킹(전체/주간 2보드) 구조를 대체. `worker/ranking.ts`의 `handleGlobalRanking`/`upsertWeeklyBest`/`handleTopicRanking`, `src/types.ts`의 `GlobalBoardId`/`RankingBoardResponse`/`TopicRankingResponse`가 이때 삭제 대상.
5. **P5 — 세부 장르 개방** (`PLAN2.md` A3). 문항이 하한(20/난이도)을 채우는 대로 자동 활성화되는 구조라 상시 진행. `chemistry`/`physics`/`astronomy`/`anatomy`/`biology`/`units`(과학 하위), `country`/`capital`/`landform`/`flag`/`timezone`(지리 하위), `football`/`baseball`/`ballgame`/`olympics`/`record`(스포츠 하위) 전부 문항이 부족한 상태(`npm run validate` 출력에서 각 주제 현황 확인 가능).
6. **PLAN4.md 신규 항목 중 P-1에 안 들어간 것** — 장르별 대표 배지는 PLAN4 §6에서 **새 단계를 만들지 않고 P3(업적) 범위에 포함**시키기로 확정됐다(업적 테이블·판정 로직이 먼저 있어야 배지가 존재하므로). 위 0번(P-1)으로 올라간 3개 항목은 여기서 뺐다.
   - **"정답 피드백 부족"은 단독 수정 대상이 아니다** — 코드에 실재하는 결함이지만(`src/App.tsx`의 `Quiz`: 5문항을 다 답해야 `제출`이 뜨고 그제서야 `StageResult`가 5문항 정답·해설을 한꺼번에 보여줌. 마지막 문항 버튼이 `isLast ? '제출' : '다음'`), **PLAN4 §2가 스스로 갱신하며 "PLAN3 보드 모드가 이 문제를 그대로 해결하므로 별도 작업 불필요"로 결론냈다.** 지금 이걸 따로 고치면 P2에서 통째로 삭제될 코드를 고치는 낭비가 된다(죽은 코드를 미리 손대지 말라는 것과 같은 논리). **P2 우선순위를 올리는 근거로만 쓸 것.** 타워에도 같은 문제가 재발할 뻔한 것을 "1층 = 1문항, 즉시 채점"으로 미리 막아뒀다(PLAN2 §8 ⑪).
7. **새 맞추기 기존 24장 이미지 육안 확인** — ✅ **완료(2026-08-13).** 24장 전부 눈으로 확인하고 문제 2장을 교체했다. 검증 방법은 `docs/question-authoring-standard.md` §8에 절차로 정리해뒀다 — **76장을 채울 때 그대로 반복하면 된다.**
   - 방법: Wikimedia API 일괄 조회로 존재·학명 확인(요청 1번) → 썸네일 로컬 저장 → 사전 설치 Chromium(`/opt/pw-browsers/chromium`, playwright 패키지 불필요)으로 24칸 시트 렌더링 → 육안 확인. 이 환경의 429 제약을 우회하는 유일한 방법이다.
   - 교체한 것: `bird-108`(왜가리) — 아파트 배경 하천 풍경에 새가 작은 점이라 **답이 불가능한 문항**이었다. 파일명이 이미 `with blue sky in Busan city`로 풍경임을 말하고 있었다. `bird-117`(꾀꼬리) — 잎에 가려 작고 휴대폰 화질에 날짜 스탬프가 박혀 있었다.
   - **썸네일 판단은 신뢰하면 안 된다** — 의심한 5건 중 3건(`bird-102`·`bird-106`·`bird-115`)이 원본에선 좋은 사진이었다. 의심되면 원본 크기로 다시 볼 것.
   - ⚠ **남은 문제 — 저작자 표시**: Commons 사진 대부분이 CC BY/CC BY-SA로 표시 의무가 있는데 **앱은 저작자·출처를 전혀 표시하지 않는다.** 24장 전체 + 앞으로 76장에 해당하고, 공개 서비스라 라이선스 위반 소지가 있다. 판단이 필요한 별도 항목(`docs/question-authoring-standard.md` §8 마지막 참고).
8. **진짜 이미지 파일 업로드** — 지금은 URL 붙여넣기 방식. 기기에서 사진을 직접 올리게 하려면 Cloudflare R2 버킷 프로비저닝이 별도로 필요함(Google OAuth 때처럼 사용자가 콘솔에서 만들어야 할 수도 있음).
9. **문항 정답률 기반 난이도 재검토** — `question_stats` 테이블에 집계만 쌓고 있고 아직 보정 로직은 없음 (`docs/2026-08-12-question-sourcing-research.md` ⑤ 참고).
10. **PLAN.md 자체는 업데이트 안 하고 있음** — 필요하면 이 인수인계 문서 + PLAN2/3/4 + 실제 코드를 기준으로 삼을 것.

## 사용자와의 작업 스타일 (참고)
- 질문은 짧고 명확하게 하는 걸 선호함. 큰 결정(공개 범위, 랭킹 반영 여부, 배포 시점 등)은 `AskUserQuestion`으로 짧은 객관식으로 물어봄. 확인은 보통 "ㄱㄱ", 한 단어 등으로 짧게 옴 — 장황한 설명 없이 진행해도 됨.
- **"질문과 구현의 반복적 프로세스"를 명시적으로 선호함** — 큰 작업을 한 번에 끝까지 밀어붙이지 말고, 자연스러운 체크포인트(배포 여부, 스코프 변경 등)마다 짧게 확인받고 이어가는 방식. 이번 세션에서 실제로 두 번 이렇게 진행함: (1) P0 구현 중 배포하면 기존 활성 주제가 사라진다는 걸 발견하자 push 여부를 먼저 물어봄, (2) P1로 콘텐츠를 채워 문제가 해소된 뒤 "지금 push할지 birds까지 마저 하고 push할지" 다시 물어봄.
- **설계와 구현을 모델로 분리하는 방식이 이번 세션에서 적용됨** — 설계(디자인 문서, PLAN 작성/수정)는 별도로, 실제 코드 구현은 이 세션(Sonnet)에서 진행. 다음 세션에서도 이 구분이 유지되는지는 사용자에게 먼저 확인할 것(대화 맥락이 요약되면서 정확한 지시 문구가 남아있지 않을 수 있음).
- 구현 세부사항(게이트 숫자, 캐시 갱신 방식, 문항 배분 비율 등)은 알아서 합리적으로 정하고 결과를 문서/보고에 명시하는 방식으로 진행해왔음 — 사소한 것까지 전부 물어보지 않음. 다만 **실사용자에게 영향이 가는 배포 판단(활성 콘텐츠가 사라지는 등)은 반드시 먼저 확인**.
- 실제 사용자가 프로덕션을 직접 써보면서 버그를 발견하는 패턴이 많았음(랭킹 미반영, 창작마당 삭제 필요 등). 배포 후에는 사용자가 직접 확인해볼 걸 권하고, 보고 들어오면 프로덕션 데이터를 직접 조회해서 원인을 찾는 방식이 잘 통했음.
- **로컬 검증(교훈 2·8·9번)을 배포 전에 항상 거치는 습관이 이번에 실제로 버그 2건을 사전에 잡았다** — 이 패턴을 계속 유지할 것: `npm run validate` → `npx tsc -b`/`npm run build` → (스키마 변경 시) 로컬 D1 이중 검증 → (필요시) 배포 파이프라인과 동일한 순서로 풀 스모크 테스트.
