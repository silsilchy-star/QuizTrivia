# QuizTrivia 인수인계

다른 작업창(세션)에서 이어받을 때 이 문서 하나로 맥락을 파악할 수 있게 정리했다. 각 항목의 자세한 내용은 `docs/*.md`에 커밋 단위로 남아있으니, 특정 결정의 배경이 궁금하면 거기부터 본다.

## 프로젝트가 뭔가
**랭킹 기반 다주제 학습 퀴즈 게임.** 설계 원안은 저장소 루트의 `PLAN.md`(v1.5) — 다만 Firebase→Cloudflare 전환, Day 5~6 결정 등은 이미 실제로 다르게 진행된 부분이 있어 **PLAN.md는 "왜 이렇게 설계했는지"를 볼 때만 참고하고, "지금 뭐가 실제로 있는지"는 이 문서와 `docs/*.md`, 실제 코드를 기준으로 판단할 것.** PLAN.md 자체는 갱신 안 하고 있다.

- 리포지토리: `silsilchy-star/QuizTrivia`
- 트렁크: **`main`** — 2026-08-13에 GitHub 저장소 설정의 default 브랜치도 `main`으로 전환 완료 확인함(`git ls-remote --symref origin HEAD`로 검증). 그 전에는 `claude/repo-permissions-check-ff2du7`가 default였다.
- 배포 주소: `https://quiztrivia.silsilchy.workers.dev`
- 배포 방식: `main`에 push하면 `.github/workflows/deploy.yml`이 **자동으로** 프로덕션에 배포됨 (별도 승인 절차 없음 — push = 배포이니 주의). 다른 브랜치와 PR에서는 `ci.yml`이 검증만 돌린다.
- 리소스: D1 `quiztrivia`, R2 `quiztrivia-images`
- 배포에 필요한 GitHub Actions 시크릿: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET`(선택 — 없으면 세션 쿠키가 서명 없이 동작, 아래 참고).

## 아키텍처 한눈에
| 계층 | 선택 |
|---|---|
| 프론트 | React 19 + TypeScript + Vite |
| 백엔드 | Cloudflare Workers (`worker/index.ts` 등) |
| DB | Cloudflare D1 (SQLite), 바인딩 이름 `DB` |
| 정적 파일 | Workers Static Assets (`dist/` 빌드 결과를 같은 Worker가 서빙) |
| 인증 | 자체 익명 uid 발급 + Google OAuth(Arctic 라이브러리)로 계정 승계 |
| 이미지 | Cloudflare R2 (바인딩 `IMAGES`) — 워커가 `/images/<해시>`로 직접 서빙 |
| 테스트 | Vitest + `@cloudflare/vitest-pool-workers` (실제 Workers 런타임 + 실제 D1 위에서 돎) |

- `src/api.ts` — **EP-5 저장소 계층**. 화면 코드는 절대 `fetch`를 직접 안 부르고 이 파일만 거친다. 백엔드를 바꿀 일이 생기면 여기만 고치면 된다는 게 설계 의도.
- `src/types.ts` — 프론트/워커가 공유하는 타입 정의. 둘 다 이 파일을 include.
- 서버 채점(PLAN 7.5절 A안) — 정답/해설은 채점 전까지 클라이언트에 안 내려가고, 서버가 출제 시점에 문항 id를 `runs.served_json`에 박아둬서 안 낸 문항 답 제출을 막는다.

## 지금 라이브 상태로 있는 것
- **핵심 게임 루프**: 주제 선택 → 12스테이지(난이도 1→4 구간별 3스테이지씩) → 스테이지당 5문항, 4문항 이상 정답이어야 클리어 → 판 전체 무중복 → 점수 = Σ(정답 문항 난이도×10), 만점 1500.
- **공식 콘텐츠 3개**: 과학·지리·스포츠, 각 80문항(난이도당 20)으로 전부 `active`. **공식 콘텐츠에 이미지·영상 문항은 하나도 없다** — 유일했던 "새 맞추기"를 2026-08-15에 제거하면서 외부 호스트 의존도 같이 사라졌다(`docs/2026-08-15-remove-birds-topic.md`).
- **소셜 로그인 + 계정 승계**: 구글 로그인, 익명 플레이 기록이 로그인 시 그대로 이어짐. 로그인 안 해도 플레이는 가능(랭킹 집계만 안 됨).
- **랭킹**: 통합 2개 보드(전체 기간/주간)만 랭킹 메뉴에 노출, 주제별 순위는 결과 화면 안에서만. 로그인(계정 승계) 안 한 유저는 랭킹 집계 대상이 아님.
- **유저 창작마당**: 로그인 + 닉네임 설정한 유저가 직접 주제·문제를 만들 수 있음. 자동 검증만 통과하면 검수 없이 즉시 공개, 활성화 하한은 공식(20/난이도)보다 훨씬 낮은 5/난이도. **랭킹에는 전혀 반영 안 됨** — 품질 편차·어뷰징 위험을 공식 콘텐츠와 분리하는 경계선.
  - **지금 프로덕션에 남은 커뮤니티 주제는 "동물"(draft, 문항 0개) 하나뿐이다.** 한때 4개였던 기록이 있는데(그중 "칵테일, 주류"는 문항 5개), 3개는 2026-08-13 06:59~14:10 사이에 사라졌다. 그 구간 배포는 전부 다른 세션이 옛 워크플로(백업 단계 없음)로 돌려서 원인은 특정하지 못했다 — 다만 배포 파이프라인이 지웠을 가능성은 백업 대조로 배제했다(`docs/2026-08-13-community-topics-missing-investigation.md`).
  - 문제 유형: 객관식(MULTIPLE_CHOICE) / 숫자입력(NUMERIC_INPUT) / 단답형(TEXT_INPUT). TEXT_INPUT은 정답을 대소문자·공백 차이 무시하고 비교.
  - **문항에 이미지 또는 유튜브 영상을 붙일 수 있다.** 붙여넣기 칸 하나가 셋을 다 받는다 — 기기에서 올린 사진(R2, `worker/images.ts`), 외부 https 이미지 링크, 유튜브 링크. 유튜브면 영상 문항이 되고, 한 문항에 이미지와 영상을 함께 붙일 수는 없다. 공식 콘텐츠(`data/*.json`)는 영상을 지원하지 않는다(아래 15번).
  - ⚠ 사용자 업로드를 같은 오리진에서 서빙하므로 매직 넘버로 형식을 판정하고 SVG를 막고 nosniff·CSP를 붙인다 — 이 방어를 약화시키면 XSS가 된다.
  - 내 주제 삭제 기능 있음(확인 단계 거침, 되돌릴 수 없음).

## ⚠ 반드시 알아야 할 사고와 교훈 (다시 반복하면 안 됨)
1. **`db/schema.sql`의 DROP TABLE 사고** (`docs/2026-08-13-critical-schema-drop-fix.md`) — 이 파일은 배포마다 무조건 재실행되는데, 한때 여기 `DROP TABLE IF EXISTS users/runs/topic_best/...`가 남아있어서 **진짜 사용자 생긴 뒤의 모든 배포가 그 테이블들을 통째로 비우고 있었다.** 지금은 전부 `CREATE TABLE IF NOT EXISTS`로 고쳐졌지만 — **`db/schema.sql`에는 절대 DROP TABLE을 쓰면 안 된다.**
2. **스키마를 바꿔야 하면 `migrations/` 폴더에 새 파일을 추가한다** (`npx wrangler d1 migrations create quiztrivia <이름>`), `db/schema.sql`은 건드리지 않는다. `db/schema.sql`은 매 배포 재실행되는 멱등 베이스라인이고, `migrations/`는 `d1_migrations` 테이블로 한 번만 적용되는 걸 추적한다.
   - **CHECK 제약을 넓혀야 하면(예: enum 값 추가) `ALTER TABLE`로 안 되고 테이블 재생성이 필요하다.** 이때 D1이 까다롭다 — `PRAGMA foreign_keys=OFF`도 `defer_foreign_keys=ON`도 "참조당하는 테이블 DROP"을 곱게 봐주지 않았다(로컬 테스트로 직접 확인, `db/migrations-archive/0002_add_text_input_and_images.sql` 참고 — 흡수되기 전 원본). **해법**: 그 테이블을 참조하는 자식 테이블들의 FK 선언부터 먼저 없애도록 재생성한 뒤에야, 대상 테이블을 안전하게 재생성할 수 있다. 배포 전 반드시 로컬에서 (a) 기존 데이터 있는 DB에 적용해보고 무결성 확인, (b) 완전히 빈 DB에 `schema.sql`부터 전체 마이그레이션을 순서대로 적용해보고 확인 — 이 두 가지를 다 통과한 뒤 배포한다. `npm test`가 (b)를 매번 자동으로 한다(`test/setup.ts`, `test/schema.test.ts`).
3. **`scripts/build-seed.mjs`가 커뮤니티 문항을 지울 뻔한 사고** (`docs/2026-08-13-image-text-input-and-birds-topic.md`) — 이 스크립트는 공식 JSON 콘텐츠만 알고 있어서, 정리 로직이 "JSON 승인 목록에 없는 문항은 삭제"였는데 **유저 창작마당 문항(uuid라 JSON에 있을 리 없음)도 걸려서 지워질 뻔했다.** `author_uid IS NULL`(공식 문항은 항상 NULL)로 범위를 좁혀 고쳤다. **앞으로 이 스크립트를 건드릴 땐 커뮤니티 문항이 안전한지 반드시 재확인할 것.**
4. **커밋 단위로 `docs/*.md` 문서 작성**은 사용자의 스탠딩 지시. 큰 작업(커밋)마다 빠짐없이 남길 것 — 한 번 빠뜨렸다가 나중에 발견해서 보충한 적 있음(`docs/2026-08-13-mobile-header-fix.md` 등 3개를 뒤늦게 채움).
5. **로컬 테스트에 playwright가 필요하면 `npm i -D playwright`로 설치했다가, 테스트 끝나면 반드시 `npm uninstall playwright`로 다시 제거한다.** 커밋에 devDependency로 남기지 않는 관례.
6. **이 작업 환경 자체가 Wikimedia 등 외부 이미지 호스트에 요청을 많이 보내면 속도 제한(429)·연결 재설정에 걸린다.** 이미지 URL이 실제로 유효한지 이 환경에서 완벽히 검증하기 어려우니, 이런 경우 사용자에게 직접 확인을 부탁하는 게 낫다.
7. **배포 파이프라인은 검증이 전부 먼저, DB 변경이 그 다음이다** (`docs/2026-08-13-foundation-tests-and-deploy-safety.md`). 예전엔 순서가 반대여서, 워커에 타입 에러가 있으면 프로덕션 DB만 새 스키마로 바뀌고 워커는 옛 버전으로 남았다. **`deploy.yml`의 단계 순서를 바꿀 땐 이 성질을 깨지 않도록 주의할 것.**
8. **테스트가 통과하지 않으면 배포되지 않는다.** 워커 로직을 고쳤으면 `npm test`로 확인한다. 특히 "커뮤니티 주제는 랭킹에 반영되지 않는다"는 경계선은 `test/community.test.ts`가 DB를 직접 조회해 검증하고 있으니, 랭킹 코드를 건드릴 때 반드시 돌려볼 것.
9. **⚠ 테스트 환경은 D1을 테스트 사이에 되돌려주지 않는다.** `@cloudflare/vitest-pool-workers` v0.21에는 `isolatedStorage` 옵션이 없다(직접 확인). 앞 테스트가 넣은 데이터가 다음 테스트에서 그대로 보인다. 그래서 **테스트는 전역 개수를 단정하면 안 되고**(`SELECT COUNT(*) FROM topics` 같은 것), 자기가 만든 데이터만 확인해야 한다. 레이트리밋 카운터도 누적되므로 `test/helpers.ts`의 `Client`가 클라이언트마다 무작위 IP를 준다 — 이걸 순번으로 바꾸면 파일 간에 겹쳐서 flaky해진다(실제로 겪음, `docs/2026-08-13-rate-limiting.md`).
10. **`db/schema.sql`은 새 테이블·새 인덱스만 추가해도 된다** — `IF NOT EXISTS`가 실제로 만들어주기 때문이다. **기존 테이블의 컬럼·제약 변경**은 반드시 `migrations/`로 해야 한다(`CREATE TABLE`이 그런 DB에서는 실행되지 않으므로).
11. **워크플로(`.github/workflows/*.yml`)는 테스트가 덮지 못한다.** 여기서 두 번 사고가 났다 — step의 `if`에 `secrets` 컨텍스트를 써서 워크플로 전체가 파싱 실패했고(job 레벨 `env`로 받아야 한다), 집계 쿼리에 `UNION ALL`을 8개 쌓았다가 D1의 compound SELECT 항 수 제한에 걸렸다. **워크플로의 SQL·쉘 명령은 푸시 전에 로컬에서 그대로 한 번 돌려볼 것 — 단, 프로덕션과 같은 조건으로.** 세 번째 사고가 여기서 났다: 새로 만든 `error_log`를 `Count rows (before)`에 넣었는데, 그 단계는 `schema.sql` 적용 **전**에 돌아서 프로덕션엔 아직 테이블이 없었다(`no such table`). 로컬은 이미 테이블이 있는 DB로 검증해서 못 잡았다. **`Count rows (before)`에는 프로덕션에 이미 존재하는 테이블만 넣는다 — 새 테이블은 (after)에만.** YAML은 `python3 -c "import yaml;yaml.safe_load(open('.github/workflows/deploy.yml'))"`로 최소한 파싱은 확인한다.
12. **프로덕션 데이터가 이상하면 배포 로그의 `Count rows (before/after)`를 먼저 본다.** 배포마다 커뮤니티 주제·문항·유저·플레이기록 개수가 찍히고, D1 백업이 아티팩트로 30일 보관된다. 백업을 열 때는 `grep`으로 세지 말고 **SQLite에 적재해서 쿼리**할 것 — D1 export의 INSERT 형식을 넘겨짚으면 틀린 숫자가 나온다(실제로 겪음, `docs/2026-08-13-community-topics-missing-investigation.md`).
13. **형식 검증을 통과해도 "문제에 답이 적혀 있는" 문항은 걸러지지 않는다.** 실제로 라이브에 3건 있었다 — 럭비 "(15인제)"(답 15), 축구공 "(32조각 기준)"(답 32), "태양계의 중심"(답 태양). `scripts/fairness.mjs`가 이제 이걸 막는다(숫자입력은 ERROR, 객관식은 WARN). **문항 검증 규칙을 새로 넣을 땐 전체 문항에 먼저 돌려 오탐 수를 확인할 것** — "육지와 바다 중 넓은 쪽은?" 같은 비교 문항은 정답이 본문에 있는 게 정상이라, 순진한 규칙은 멀쩡한 문항을 막는다.
14. **⚠ 유저가 준 URL을 iframe·img의 src에 그대로 넣지 않는다** (`docs/2026-08-15-media-links-youtube.md`). 유튜브 영상은 링크에서 **영상 id만 뽑아** 저장하고(`video_kind`/`video_id`) 임베드 주소는 우리가 조립한다 — 저장값이 11자 URL-safe base64로 강제되니 위험한 문자가 구조적으로 못 들어간다. **파서는 `src/media.ts` 하나뿐이고 프론트·워커가 같이 쓴다. 여기에 규칙을 하나 더 만들면 두 규칙이 어긋나는 날 느슨한 쪽이 뚫린다.** 예전 이미지 검증(`/^https:\/\/.+/`)은 사실상 아무것도 못 걸러서 유튜브 링크가 이미지로 저장되고 화면에 깨진 이미지가 떴었다 — 지금은 URL 파서로 판정한다.
15. **공식 콘텐츠(`data/*.json`)는 영상을 지원하지 않는다.** `scripts/*.mjs`가 평범한 Node ESM이라 TypeScript인 `src/media.ts`를 import할 수 없고, 파서를 JS로 한 벌 더 쓰면 14번 전제가 깨지기 때문이다. `validate.mjs`가 `videoUrl`을 ERROR로 막아 조용히 무시되는 일은 없다. 정말 필요해지면 파서를 두 벌 쓰는 대신 스크립트를 TS로 옮기는 쪽을 먼저 검토할 것.
16. **과거 시점의 데이터를 봐야 하면, 해당 배포 run의 `d1-backup-<sha>` 아티팩트를 내려받아 대조한다.** 특정 브랜치에만 도는 일회용 워크플로(`actions/download-artifact@v4`에 `run-id`를 지정해 다른 run의 아티팩트를 가져옴)를 임시로 만들었다 지우는 방식이 잘 통했다 — 프로덕션에는 전혀 손대지 않고 과거 상태를 확인할 수 있다. 단, 30일 지난 아티팩트는 만료되어 사라지므로 오래된 사고는 이 방법이 안 통한다.

## 개발/배포 방법
```bash
npm install
npm run dev          # 프론트만 (vite, API 없음)
npm run worker:dev    # wrangler dev — 로컬 D1 포함 풀스택 (포트는 알아서 지정)
npm run db:local      # 로컬 D1에 schema+migrations+seed 전부 적용
npm run validate      # data/*.json 자동 검증 (에러 있으면 배포 파이프라인도 막힘)
npm run build:seed    # data/*.json → db/seed.generated.sql (gitignore됨, 커밋 안 함)
npm run review        # 로컬 문항 검수 웹페이지
npm run difficulty:review  # 정답률 기반 난이도 보정 후보 조회 (D1 remote, --local 가능)
npm test              # 테스트 (실제 Workers 런타임 + D1)
npm run test:watch    # 워치 모드
npm run typecheck     # 전체 타입체크 (프론트+워커+테스트 3개 프로젝트)
```
배포 파이프라인(`deploy.yml`, `main` push에 자동 트리거) 순서 — **검증이 전부 먼저, DB 변경은 그 다음**:
```
typecheck → test → validate → build:seed → 프론트 빌드 → 워커 dry-run 번들
  → D1 백업 export (아티팩트 30일 보관)
  → D1 schema → migrations → seed
  → Worker 배포
```
`concurrency: deploy-production`으로 배포가 겹치지 않는다(두 배포가 같은 D1에 동시에 마이그레이션 거는 것을 막음). 모든 브랜치·PR에서는 `ci.yml`이 검증 부분만 돌린다.

새 스키마 변경이 필요하면:
```bash
npx wrangler d1 migrations create quiztrivia <설명>   # migrations/000N_설명.sql 생성
# 그 파일에 ALTER TABLE ... 만 쓴다. CHECK 제약을 넓혀야 하면 위 2번 항목의 재생성 절차를 따른다.
npx wrangler d1 migrations apply quiztrivia --local   # 로컬에서 먼저 검증
```
`migrations/`에는 지금 두 개가 있다 — `0001_add_question_video.sql`(문항 영상 컬럼), `0002_remove_birds_topic.sql`(birds 주제 제거). 2026-08-13에 그 전의 0001·0002를 `db/schema.sql` 베이스라인에 흡수하고 원본은 `db/migrations-archive/`로 옮겼기 때문에 번호가 0001부터 다시 시작한다(10번 항목 참고).

**마이그레이션은 스키마 변경뿐 아니라 일회성 데이터 정리에도 쓴다.** 0002가 그 예다 — `build-seed.mjs`가 주제를 삭제하지 않아서, 공식 주제를 없애려면 JSON에서 빼는 것만으로는 부족하고 프로덕션 행을 명시적으로 지워야 한다. 삭제 순서(자식 FK 먼저)와 범위 좁히기(`author_uid IS NULL` 등)를 그 파일에서 그대로 본뜨면 된다.

## 파일 구조
```
PLAN.md                  설계 원안 (v1.5, 갱신 안 함 — 참고용)
인수인계.md               이 문서
docs/*.md                 커밋 단위 작업 기록 (날짜-내용.md)

worker/
  index.ts                메인 엔트리 — 세션/판 진행/랭킹 라우팅, finalizeRun 등 핵심 로직
  auth.ts                  구글 OAuth (Arctic) + 계정 승계
  session.ts                세션 쿠키 발급·서명·검증 (HMAC-SHA256, SESSION_SECRET)
  ranking.ts               랭킹 캐시 계산·조회
  community.ts             유저 창작마당 (주제/문제 생성·삭제)
  ratelimit.ts              레이트리밋 (D1 고정 창 카운터)
  errorlog.ts               잡히지 않은 예외를 error_log에 기록
  images.ts                 문항 이미지 업로드·서빙 (R2, 매직 넘버 검사)

src/
  App.tsx                  메인 화면 (주제선택/퀴즈/결과/랭킹 화면 전환)
  Workshop.tsx              창작마당 화면
  VideoFrame.tsx            영상 iframe (App이 Workshop을 import하므로 순환 피하려 분리)
  api.ts                    EP-5 저장소 계층 (fetch는 여기서만)
  types.ts                  프론트+워커 공유 타입
  media.ts                  ⚠ 이미지·유튜브 링크 파서 — 프론트/워커 공용, 규칙은 여기 하나뿐

db/schema.sql              베이스라인 (배포마다 재실행 — DROP 금지! 이미 있는 DB엔 무해)
db/migrations-archive/      베이스라인에 흡수된 옛 마이그레이션 (실행 안 됨, 기록용)
migrations/000N_*.sql       스키마 변경·일회성 데이터 정리 (한 번만 적용, d1_migrations로 추적)
                              0001 문항 영상 컬럼 / 0002 birds 주제 제거

data/topics.json            공식 주제 정의서 (과학/지리/스포츠 + D-16 좁은 태그들)
data/questions/*.json       공식 문항 원본 (JSON이 소스 오브 트루스)
db/seed.generated.sql        위 JSON → SQL 생성물 (gitignore, npm run build:seed로 재생성)

scripts/
  validate.mjs              자동 검증 (ERROR면 배포 막힘, WARN이면 사람 검수 필요)
  build-seed.mjs             JSON → SQL, 하한 게이트 자동 판정(draft/active)
  review.mjs / review.html   로컬 문항 검수 웹페이지
  build-image-check.mjs      이미지 문항 검수 페이지 생성 → public/image-check.html
  fairness.mjs               문항 공정성 규칙 (문제에 답이 적혀 있는지)
  difficulty-review.mjs      정답률 기반 난이도 보정 후보 탐지 (D1 조회만, 아무것도 안 고침)
  lib.mjs                    공통 유틸 (GATE_PER_DIFFICULTY=20 등 상수)

test/                       실제 Workers 런타임 + D1 위에서 도는 테스트 (230개)
  setup.ts                  테스트 D1을 프로덕션과 같은 순서로 구성 (schema → migrations)
  helpers.ts                 주제/문항/유저/Client 픽스처 — 테스트는 자기 데이터를 직접 만든다
  scoring.test.ts            채점 규칙, 난이도 매핑, ISO 주차, 창작마당 검증 규칙 (순수 로직)
  schema.test.ts             스키마 드리프트 감시 — 테이블/컬럼/제약이 코드 기대와 일치하는지
  session.test.ts            세션 토큰 서명·검증·수명, 옛 쿠키 전환, 위조 쿠키 차단
  ratelimit.test.ts           레이트리밋 카운터, IP 판별, 엔드포인트별 제한
  errorlog.test.ts            에러 기록·비밀 제거·로깅 실패 격리, 실제 예외 경로
  fairness.test.ts            정답 노출 규칙 — 잡는 것과 안 잡는 것(오탐 방지)
  images.test.ts              업로드 권한·형식 판정·XSS 차단 헤더·키 형식
  run.test.ts                게임 루프 통합 — 세션·출제·채점·12스테이지 완주·통계
  community.test.ts          창작마당 권한·게이트, 랭킹 경계선, 보드, 주제 삭제, 문항 미디어
  media.test.ts               이미지·유튜브 링크 파서 — 특히 막아야 하는 것들(스킴·흉내 호스트·경로 탈출)

.github/workflows/
  ci.yml                    모든 브랜치·PR 검증 (typecheck/test/validate/빌드)
  deploy.yml                 main push → 프로덕션 배포 (검증 먼저, DB 변경 나중)
```

## 남은 일 / 다음에 이어서 할 수 있는 것

### 토대 (건축으로 치면 아래층 — 사실상 끝났다)
- ~~배포 파이프라인 순서 교정 · 백업 · main 트렁크~~ ✅ (`282f18a`)
- ~~테스트 골조~~ ✅ 162개 (`282f18a` 외)
- ~~`db/schema.sql` 드리프트 해소~~ ✅ (`c0fefe3`) — 마이그레이션 0001·0002를 베이스라인에 흡수, 원본은 `db/migrations-archive/`
- ~~세션 토큰 서명화 + OAuth 계정 탈취 경로 수정~~ ✅ (`c0fefe3`) — `SESSION_SECRET` 시크릿 등록 및 배포 완료 확인함 (배포 로그의 "Set session signing secret" 단계가 success로 실행됨)
- ~~어뷰징 방어(레이트리밋)~~ ✅
- ~~GitHub 저장소 default 브랜치를 main으로 전환~~ ✅ 확인 완료
- ~~창작마당 주제 소실 조사~~ ✅ — 이 세션의 작업 이전(06:59~14:10 사이)에 이미 사라져 있었음을 백업 대조로 확정. 원인 자체는 그 구간 백업이 없어 특정 못 함 (`docs/2026-08-13-community-topics-missing-investigation.md`)
- **`worker/session.ts`의 `ACCEPT_LEGACY` 닫기** — 서명 없는 옛 쿠키를 아직 받아주고 있다. 이게 열려 있는 동안은 서명의 보안 효과가 없다. 사용자 대부분이 재접속해 서명된 쿠키로 갈아탄 뒤(`/api/session`이 접속 시 자동 갱신) `false`로 내릴 것. 시간을 좀 두고(예: 1~2주) 판단.
- ~~에러 로깅/관측~~ ✅ — 라우팅 전체를 try/catch로 감싸 잡히지 않은 예외를 `error_log` 테이블에 남긴다(`worker/errorlog.ts`). 배포 로그의 `Count rows`에도 누적 에러 수가 찍힌다. **이제 토대는 `ACCEPT_LEGACY` 닫기만 남았다.**

### 콘텐츠·기능
- ~~"새 맞추기"(birds) 주제~~ **제거함** ✅ 2026-08-15 (`docs/2026-08-15-remove-birds-topic.md`) — 24/80에서 멈춰 세 세션째 미해결이던 주제. 사용자가 "집착하지 말고 없애"로 정리. 이미지·영상 첨부의 목적은 **특정 주제 완성이 아니라 유저 창작의 확장성·개방성**이라는 방향 정리에 따른 것. `migrations/0002`가 프로덕션에서도 지운다(`build-seed.mjs`는 주제를 삭제하지 않으므로 JSON에서 빼는 것만으론 고아 행이 남는다 — 주제를 없앨 일이 또 생기면 이 마이그레이션을 본뜰 것).
- ~~진짜 이미지 파일 업로드~~ ✅ — R2 버킷 `quiztrivia-images`, 워커가 `/images/`로 서빙. 배포 파이프라인이 버킷을 없으면 만든다(권한 부족이면 실패로 드러남).
- ~~유저 문항에 이미지 링크·유튜브 영상 첨부~~ ✅ 2026-08-15 (`docs/2026-08-15-media-links-youtube.md`) — 창작마당 붙여넣기 칸 하나가 업로드 사진·외부 이미지 링크·유튜브 링크를 모두 받는다. 느슨했던 이미지 검증(`/^https:\/\/.+/`)도 URL 파서로 교체. **배포 후 사용자가 실제로 영상 문항을 하나 만들어 재생되는지 확인해줄 것** — 이 작업 환경은 유튜브가 프록시 차단이라 여기서는 확인 불가.
- ~~문항 정답률 기반 난이도 재검토 — 후보 탐지 도구~~ ✅ (`docs/2026-08-14-difficulty-review-tool.md`) — `npm run difficulty:review`(`scripts/difficulty-review.mjs`)가 `question_stats`를 조회해 "너무 쉬움/너무 어려움" 후보를 뽑아 보여준다. **자동 반영 안 함** — 최소 표본 10건, 정답률 90%↑/30%↓ 임계값(사용자 승인받은 기준), 사람이 확인 후 JSON을 손으로 고치는 방식. 프로덕션에 데이터가 더 쌓인 뒤 돌려볼 것.
- **PLAN.md 자체는 업데이트 안 하고 있음** — 필요하면 이 인수인계 문서와 실제 코드를 기준으로 삼을 것.

## 사용자와의 작업 스타일 (참고)
- 질문은 짧고 명확하게 하는 걸 선호함. 큰 결정(공개 범위, 랭킹 반영 여부 등)은 `AskUserQuestion`으로 짧은 객관식으로 물어봄.
- 확인은 보통 "ㄱㄱ", 한 단어 등으로 짧게 옴 — 장황한 설명 없이 진행해도 됨.
- 구현 세부사항(게이트 숫자, 캐시 갱신 방식 등)은 알아서 합리적으로 정하고 결과를 문서/보고에 명시하는 방식으로 진행해왔음 — 사소한 것까지 전부 물어보지 않음.
- 실제 사용자가 프로덕션을 직접 써보면서 버그를 발견하는 패턴이 많았음(랭킹 미반영, 창작마당 삭제 필요 등) — 배포 후에는 사용자가 직접 확인해볼 걸 권하고, 보고 들어오면 프로덕션 데이터를 직접 조회해서 원인을 찾는 방식이 잘 통했음.
