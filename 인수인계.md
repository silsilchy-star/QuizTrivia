# QuizTrivia 인수인계

다른 작업창(세션)에서 이어받을 때 이 문서 하나로 맥락을 파악할 수 있게 정리했다. 각 항목의 자세한 내용은 `docs/*.md`에 커밋 단위로 남아있으니, 특정 결정의 배경이 궁금하면 거기부터 본다.

**2026-08-13 최신화** — v2 로드맵(PLAN2/3/4) 설계 완료 → P0·P1 일부 구현·배포 → **P-1 문항 작성 기준 문서화 + birds 이미지 24장 육안 확인 완료** 시점 기준.

## 새 세션이 가장 먼저 할 일 (읽고 넘기지 말 것)

세션마다 작업 브랜치가 새로 생기는 구조라, **자기 브랜치가 (a) 최신 커밋에서 출발했는지 (b) `deploy.yml` 트리거에 있는지를 다른 어떤 작업보다 먼저 확인해야 한다.**

```bash
git log --oneline -3                      # 내 HEAD가 어디인지
git ls-remote --heads origin              # 원격에 실제로 있는 브랜치들
grep -A4 'branches:' .github/workflows/deploy.yml   # 배포 트리거 목록
```

**왜 이게 1순위인가** — 2026-08-13 세션이 실제로 8커밋 뒤처진 지점에서 시작했다. 그 상태로 트리거만 추가해 push했다면 `build-seed.mjs`가 만드는

```sql
DELETE FROM questions WHERE author_uid IS NULL AND id NOT IN (<JSON의 id 목록>);
```

이 **프로덕션에 살아있는 난이도5 문항 60개를 삭제**했을 것이다. stale JSON에는 그 60개가 없으니까. 게이트도 구 기준으로 재판정돼 주제는 `active`로 남는다 — **에러 없이 콘텐츠만 조용히 롤백되므로 발견이 거의 불가능하다.**

## 프로젝트가 뭔가

**랭킹 기반 다주제 학습 퀴즈 게임.** 설계 원안은 `PLAN.md`(v1.5)지만 Firebase→Cloudflare 전환 등 실제로 다르게 진행된 부분이 많고, 그 위에 `PLAN2/3/4.md`(v2 로드맵)가 얹혀 있다. **"지금 실제로 뭐가 있는지"는 이 문서 + `docs/*.md` + 실제 코드를 기준으로 판단할 것.** `PLAN.md`는 갱신하지 않는다.

- 리포지토리: `silsilchy-star/QuizTrivia`
- 작업 브랜치: **`claude/work-handover-file-check-zknp0b`**
- 배포 주소: `https://quiztrivia.silsilchy.workers.dev`
- **배포 방식: 아래 두 브랜치 중 하나에 push하면 `deploy.yml`이 자동으로 프로덕션에 배포한다. 승인 절차가 없다 — push = 배포.**
  - `claude/work-handover-file-check-zknp0b` (현재)
  - `claude/handover-matters-review-gs5597` (이전 세션 — 그 세션이 살아있으면 **나중에 push한 쪽이 프로덕션을 덮는 배포 경합**이 생긴다)
  - 완전히 폐기된 `claude/repo-permissions-check-ff2du7`은 트리거에서 제거했다.

## 아키텍처 한눈에

| 계층 | 선택 |
|---|---|
| 프론트 | React 19 + TypeScript + Vite |
| 백엔드 | Cloudflare Workers (`worker/index.ts` 등) |
| DB | Cloudflare D1 (SQLite), 바인딩 이름 `DB` |
| 정적 파일 | Workers Static Assets (`dist/` 빌드 결과를 같은 Worker가 서빙) |
| 인증 | 자체 익명 uid 발급 + Google OAuth(Arctic 라이브러리)로 계정 승계 |

- `src/api.ts` — **EP-5 저장소 계층.** 화면 코드는 절대 `fetch`를 직접 안 부르고 이 파일만 거친다. 백엔드를 바꿀 일이 생기면 여기만 고치면 된다는 게 설계 의도.
- `src/types.ts` — 프론트/워커가 공유하는 타입 정의. 둘 다 이 파일을 include. `Difficulty`는 `1|2|3|4|5`.
- 서버 채점(PLAN 7.5절 A안) — 정답/해설은 채점 전까지 클라이언트에 안 내려가고, 서버가 출제 시점에 문항 id를 `runs.served_json`에 박아둬서 안 낸 문항의 답 제출을 막는다.

## 지금 라이브 상태로 있는 것

- **핵심 게임 루프는 아직 구(舊) 방식이다**: 주제 선택 → 12스테이지(난이도 1→4 구간별 3스테이지씩) → 스테이지당 5문항, 4문항 이상 정답이어야 클리어 → 판 전체 무중복 → 점수 = Σ(정답 문항 난이도×10), 만점 1500. **`PLAN3.md`의 "문제 보드(3×3)" 모드가 이걸 완전히 대체할 예정이나 아직 미구현(P2).**
- **DB/백엔드는 난이도 1~5를 전부 지원**(P0 완료). 다만 구 게임 루프(`difficultyForStage()` 등)는 여전히 1~4만 쓴다 — **의도적으로 방치한 죽은 코드다.** P2에서 통째로 교체될 예정이라 지금 고쳤다 다시 지우는 게 낭비이기 때문. **지금 건드리지 말 것.**
- **공식 콘텐츠 3개 — 과학·지리·스포츠**: 각 **100문항**(난이도당 20, 1~5 전부)으로 전부 `active`.
- **새 맞추기(birds)**: **24문항뿐이라 아직 `draft`(비공개).** 활성화에 100개(난이도당 20)가 필요하니 **76개 부족.** 이미지 24장은 육안 확인을 마쳤고 문제 2장을 교체했다(아래 "남은 일" 1번).
- **소셜 로그인 + 계정 승계**: 구글 로그인, 익명 플레이 기록이 로그인 시 그대로 이어짐. 로그인 안 해도 플레이는 가능(랭킹 집계만 안 됨).
- **랭킹**: 통합 2개 보드(전체 기간/주간)만 랭킹 메뉴에 노출, 주제별 순위는 결과 화면 안에서만. **⚠ 이 구조는 PLAN2 C4에 의해 폐기 예정** — P4에서 타워 단일 보드로 대체되고 시즌제 개념도 없앤 상태다. 코드는 아직 안 바뀜.
- **유저 창작마당**: 로그인 + 닉네임 설정한 유저가 직접 주제·문제를 만듦. 자동 검증만 통과하면 검수 없이 즉시 공개, 활성화 하한은 공식(20/난이도)보다 훨씬 낮은 5/난이도. **랭킹에 전혀 반영 안 됨.** 실제 사용자가 만든 주제가 프로덕션에 있음.
  - 문제 유형: 객관식(`MULTIPLE_CHOICE`) / 숫자입력(`NUMERIC_INPUT`) / 단답형(`TEXT_INPUT`, 이미지 URL 선택 첨부 가능, 정답은 대소문자·공백 차이 무시).
  - 난이도 라벨은 PLAN2 공식 명칭("쉬움/보통/어려움/전문가/매니아")으로 통일됨.
  - 내 주제 삭제 기능 있음(확인 단계 거침, 되돌릴 수 없음).

## v2 로드맵 진행 상황 (`PLAN2.md`/`PLAN3.md`/`PLAN4.md`)

이 세 문서가 다음 개발 방향의 설계 원본이다. 읽는 순서: PLAN2(전체 로드맵) → PLAN3(보드 모드 상세) → PLAN4(커뮤니티 신뢰·품질 기준). **PLAN2 §8에 세 문서를 교차검증하며 확정한 결정들이 정리돼 있다.**

**핵심 설계 결정**
- **C1 타워 오르기** — 전 주제 혼합 무한 타워, 유일한 랭킹 보드가 될 예정. 1~5층 난이도1 … 21층 이상은 난이도5 반복. **1층 = 1문항, 즉시 채점**(PLAN2 §8 ⑪).
- **C2 구 스테이지 판(12스테이지)** — **폐지 확정**, PLAN3의 보드 모드로 완전 대체. "시작 난이도 선택" UI도 같이 없어진다.
- **C3 배틀 모드** — 로드맵에서 제외(보류).
- **C4 랭킹** — 통합 2보드+주제별 순위 구조 폐기, 시즌제도 폐기. 타워 단일 보드로 단순화.
- **A1+A2 난이도 5단계** — ✅ 완료.
- **A3 세부 장르 개방** — 공식 하한(20/난이도) 유지, 문항 차는 순서대로 개방. 미착수.
- **A4 업적 시스템** — 미착수. 타워의 답안 채점 경로에서 판정하도록 재설계됨(구 `finalizeRun()` 기반 설계는 폐기).

**로드맵 표** (PLAN2 §4 + PLAN4 §7 재조정 반영)

| 단계 | 내용 | 상태 |
|---|---|---|
| **P-1** | 문항 작성 기준 문서화 + 커뮤니티 추천/비추천 + 유저 피드백 화면 | 🟡 **기준 문서만 완료** (`2be79ac`) — 나머지 2개 미착수 |
| **P0** | 배포 브랜치 정리 / 난이도 1~5 확장 (migration 0003) | ✅ 완료·배포됨 (`d4a5982`) |
| **P1** | 공식 3주제 난이도5 60개 + 새 맞추기 76개 | 🟡 **부분 완료** — 3주제 60개 끝(`2c7c224`), **birds 76개 남음** |
| **P2** | 문제 보드 모드(PLAN3) — 구 스테이지 판 완전 대체 | ⬜ 미착수 |
| **P3** | 업적 시스템(A4) **+ 장르별 대표 배지**(PLAN4 §6) | ⬜ 미착수 (P2 선행) |
| **P4** | 타워 오르기(C1) — 혼합 출제, 체크포인트, 타워 랭킹 보드 | ⬜ 미착수 (P1·P2 선행) |
| **P5** | 세부 장르 개방(A3) | ⬜ 미착수, 상시 진행 |

**PLAN4 §7이 P-1을 P0보다 앞에 둔 이유가 중요하다** — "문항 작성 기준 문서는 **P1(콘텐츠 대량 작성)보다 먼저** 있어야 새로 쓰는 문항이 처음부터 기준을 따른다." 이 순서를 어기면 birds 76문항을 기준 없이 쓰고 재작업하게 된다. (이전 인수인계 판은 PLAN4 항목들을 후순위 6번에 뒀는데 PLAN4 §7과 모순이었다 — 정정했다.)

PLAN2 §5 리스크의 핵심 판단: **"P1(콘텐츠)이 전체 일정을 지배한다."** 스키마·코드는 하루 단위지만 세부 장르까지 채우려면 문항이 1,000개 단위로 필요하다.

## ⚠ 반드시 알아야 할 사고와 교훈 (반복하면 안 됨)

1. **`db/schema.sql`의 DROP TABLE 사고** (`docs/2026-08-13-critical-schema-drop-fix.md`) — 이 파일은 배포마다 무조건 재실행되는데, 한때 `DROP TABLE IF EXISTS users/runs/topic_best/...`가 남아있어서 **진짜 사용자가 생긴 뒤의 모든 배포가 그 테이블들을 통째로 비우고 있었다.** 지금은 전부 `CREATE TABLE IF NOT EXISTS`다 — **`db/schema.sql`에 DROP TABLE은 절대 금지.**

2. **스키마 변경은 `migrations/`에 새 파일로** (`npx wrangler d1 migrations create quiztrivia <이름>`). `db/schema.sql`은 매 배포 재실행되는 멱등 베이스라인이고, `migrations/`는 `d1_migrations` 테이블로 한 번만 적용되는 걸 추적한다.
   - **CHECK 제약을 넓히려면(예: enum 값 추가) `ALTER TABLE`로 안 되고 테이블 재생성이 필요하다.** D1이 까다롭다 — `PRAGMA foreign_keys=OFF`도 `defer_foreign_keys=ON`도 "참조당하는 테이블 DROP"을 봐주지 않았다(`migrations/0002_add_text_input_and_images.sql` 참고). **해법**: 그 테이블을 참조하는 자식 테이블들의 FK 선언부터 없애도록 재생성한 뒤에야 대상 테이블을 안전하게 재생성할 수 있다. 한번 자식 FK를 없애두면 그 다음부터는 자식을 또 안 건드려도 된다(`0003_add_difficulty_5.sql`이 이 덕을 봄 — `questions` 하나만 재생성).
   - 배포 전 반드시 로컬에서 **(a) 기존 데이터 있는 DB에 적용해 무결성 확인, (b) 완전히 빈 DB에 `schema.sql`부터 전체 마이그레이션을 순서대로 적용해 확인** — 둘 다 통과한 뒤 배포한다.

3. **`scripts/build-seed.mjs`가 커뮤니티 문항을 지울 뻔한 사고** (`docs/2026-08-13-image-text-input-and-birds-topic.md`) — 이 스크립트는 공식 JSON만 알고 있어서 정리 로직이 "JSON 승인 목록에 없는 문항은 삭제"였는데 **유저 창작 문항(uuid라 JSON에 있을 리 없음)도 걸려서 지워질 뻔했다.** `author_uid IS NULL`로 범위를 좁혀 고쳤다. **이 스크립트를 건드릴 땐 커뮤니티 문항이 안전한지 반드시 재확인할 것** — 청크 수정(교훈 8) 때도 가짜 커뮤니티 문항을 심어 재확인했다.

4. **커밋 단위로 `docs/*.md` 작성**은 사용자의 스탠딩 지시. 큰 작업(커밋)마다 빠짐없이 남길 것 — 한 번 빠뜨렸다가 나중에 보충한 적 있음.

5. **브라우저가 필요하면 playwright를 설치하기 전에 `/opt/pw-browsers/chromium`을 먼저 본다** — 사전 설치돼 있고 `--headless --disable-gpu --no-sandbox --screenshot=out.png --window-size=W,H file://...`로 바로 렌더링된다(dbus 에러 로그는 무해). 2026-08-13 이미지 검수 시트를 이걸로 만들어서 **설치가 아예 불필요했다.** 그래도 playwright 패키지가 꼭 필요하면 `npm i -D playwright` 후 **끝나면 반드시 `npm uninstall playwright`** — 커밋에 devDependency로 남기지 않는 관례.

6. **외부 이미지 검증을 "URL 하나씩 GET"으로 하면 안 된다** (2026-08-13 원인·해법 모두 정정, `docs/2026-08-13-birds-image-visual-check-and-two-replacements.md`) — 이 환경의 **공용 egress IP**가 Wikimedia 레이트리밋에 걸려 429가 난다. 이전 판은 원인과 해법을 둘 다 틀리게 적어놨었다.
   - **원인은 URL 방식이 아니다.** `Special:FilePath`를 의심했지만, API로 받은 CDN(`upload.wikimedia.org`) 썸네일도 똑같이 429가 났다. **실제 플레이어(각자 다른 IP, 스테이지당 5장)에겐 해당 없다. `Special:FilePath`를 유지한다** — Commons에서 파일명이 바뀌어도 따라가므로 링크가 덜 깨진다.
   - **"사용자에게 확인을 부탁한다"도 해법이 아니었다** — 사용자도 확인할 방법이 없다고 답했다. **양쪽이 막힌 상태는 우리 쪽에서 뚫어야 한다.**
   - **작동하는 절차**: Wikimedia API 일괄 조회(요청 1번에 최대 50개, `User-Agent` 필수 — 기본 curl UA는 막힘) → 썸네일 로컬 저장 → 사전 설치 Chromium으로 시트 렌더링 → 육안 확인. 전체 절차와 명령은 **`docs/question-authoring-standard.md` §8**에 있다.
   - **429는 약 2KB HTML 에러 페이지로 내려온다.** 파일 크기로 성공을 판정하면 에러 페이지를 이미지로 착각한다 — 실제로 2190 bytes 에러 페이지 2개를 성공으로 잘못 셌다. **`file`로 `JPEG|PNG|WebP` 여부를 봐야 한다.**
   - **썸네일 판단을 신뢰하면 안 된다** — 시트(150px)만 보고 문제라고 의심한 5건 중 **3건이 오판**이었다(`bird-102`·`bird-106`·`bird-115` 전부 원본에선 좋은 사진). 의심되면 반드시 원본 크기로 다시 볼 것.

7. **`db/schema.sql`에 마이그레이션이 나중에 추가하는 컬럼·CHECK를 미리 반영하면 안 된다** (`docs/2026-08-13-p0-difficulty-5-schema.md`) — `q_count_5`를 `schema.sql`에도 넣었다가 "빈 DB → schema → 마이그레이션" 순서 적용에서 `duplicate column name` 충돌이 났다. **`schema.sql`은 항상 "오래된 상태"로 남고, 마이그레이션들이 순서대로 최종 상태를 완성하는 구조다.** 최신화하고 싶은 유혹을 참을 것.

8. **`build-seed.mjs`가 D1 SQL 문장 크기 한도(`SQLITE_TOOBIG`)에 걸린 사고** (`docs/2026-08-13-p1-difficulty-5-content-and-seed-chunking.md`) — `INSERT INTO questions`를 한 문장에 몰아넣는 구조여서 264개(~96KB)는 통과했지만 324개(~118KB)에서 한도를 넘었다(로컬 스모크 테스트로 미리 발견). **고침**: `questions`/`question_topics` INSERT를 100행 단위로 청크(`CHUNK_SIZE=100`). **문항이 계속 늘어나는 로드맵이라 또 걸릴 수 있다** — 재발하면 `CHUNK_SIZE`를 줄이거나 청크가 다른 테이블(`topics` 등)에도 필요한지 확인할 것.

9. **게이트 로직을 바꾸면 이미 `active`인 주제가 배포 즉시 `draft`로 떨어질 수 있다** — 난이도5를 게이트에 포함시키자마자 과학·지리·스포츠가 전부 `draft`로 떨어졌다(난이도5 문항이 0개였으므로). `push=배포`인 브랜치에서 이건 **실사용자에게 노출된 콘텐츠가 갑자기 사라지는 사고**다. **`npm run validate`로 게이트 판정을 push 전에 반드시 확인하고**, 활성 콘텐츠가 죽는 방향이면 콘텐츠를 채워 게이트를 다시 통과시킨 뒤 push할 것(최소한 사용자에게 먼저 확인).

10. **커뮤니티 경로에는 의미적 품질 검사가 사실상 없다** (2026-08-13 발견) — `worker/community.ts:21`의 `SUPERLATIVE` 배열이 **선언만 되고 어디에서도 쓰이지 않는다**(죽은 상수). 최상급 검사가 실제로 도는 곳은 `scripts/validate.mjs:101`(공식 JSON)뿐이다. 결과적으로 **커뮤니티 문항은 최상급 표현·문장형 정답·`seedRef` 검사를 전부 안 받는다.** 상수만 보고 "검사되고 있다"고 착각하기 쉽다.
    - 고치면 `validateQuestion()`이 400을 반환하므로 **유저가 "가장 큰 ~는?"을 쓸 때 즉시 거절된다** — 실사용자 영향이 있는 동작 변경이다. `PLAN4.md` §5가 "자동 검증 규칙 확대는 이번 범위 밖"이라 그어놨으므로 **별도 결정 사항으로 남겼다.**
    - 다른 비대칭도 있다: 본문 80자는 공식에서 WARN(초과 허용)인데 커뮤니티는 ERROR(거절)로 **오히려 엄격**하고, 문항 중복은 공식이 전체에서, 커뮤니티는 같은 주제 안에서만 검사한다. 전체 비교표는 `docs/question-authoring-standard.md` §6.

11. **Commons 사진의 저작자 표시 의무를 아직 이행하지 않고 있다** (2026-08-13 발견) — Commons 사진 대부분이 **CC BY / CC BY-SA로 attribution이 요구된다**(예: 이번에 넣은 두 장은 CC BY-SA 4.0 / CC BY 2.0). 그런데 **앱은 사진의 출처·저작자를 어디에도 표시하지 않는다.** 기존 24장 전체 + 앞으로 채울 76장에 해당하고, 공개 서비스라 실제 라이선스 위반 소지가 있다. **P1로 76장을 더 넣기 전에 정하는 게 순서상 맞다** — 나중에 정하면 100장에 소급 적용해야 한다. 지금 `birds.json`은 URL만 저장하므로 저작자·라이선스 필드 추가가 필요하다.

## 개발/배포 방법

```bash
npm install
npm run dev          # 프론트만 (vite, API 없음)
npm run worker:dev    # wrangler dev — 로컬 D1 포함 풀스택
npm run db:local      # 로컬 D1에 schema+migrations+seed 전부 적용
npm run validate      # data/*.json 자동 검증 (ERROR면 배포 파이프라인도 막힘, 게이트 판정도 여기서 보임)
npm run build:seed    # data/*.json → db/seed.generated.sql (gitignore, 커밋 안 함)
npm run review        # 로컬 문항 검수 웹페이지
npx tsc -b             # 전체 타입체크 (프론트+워커 동시)
```

배포 파이프라인(`deploy.yml`) 순서: `npm ci` → `validate` → D1 schema 적용 → **D1 migrations 적용** → `build:seed` → D1 seed 적용 → 프론트 빌드 → Worker 배포.

**push 전 검증 습관** (실제로 버그를 여러 건 사전에 잡았다): `npm run validate` → `npx tsc -b` → (스키마 변경 시) 로컬 D1 이중 검증 → (필요시) 배포 파이프라인과 같은 순서로 풀 스모크 테스트.

새 스키마 변경이 필요하면:

```bash
npx wrangler d1 migrations create quiztrivia <설명>   # migrations/000N_설명.sql 생성
# ALTER TABLE 만 쓴다. CHECK 제약을 넓혀야 하면 교훈 2번의 재생성 절차를 따른다.
npx wrangler d1 migrations apply quiztrivia --local   # 로컬에서 먼저 검증
```

로컬 이중 검증 절차 예시는 `docs/2026-08-13-p0-difficulty-5-schema.md`에 그대로 남아있다 — `rm -rf .wrangler/state/v3/d1` 후 schema→migrations만 적용해 "기존 데이터 있는 DB"를 만들고, 테스트 데이터(커뮤니티 문항 포함)를 심은 뒤 새 마이그레이션을 적용해 무결성을 확인하는 방식.

## 파일 구조

```
PLAN.md                   설계 원안 v1 (v1.5, 갱신 안 함 — 참고용)
PLAN2.md                  v2 로드맵 — 로드맵의 단일 출처. §8에 교차검증 확정 결정들
PLAN3.md                  문제 보드(3×3) 모드 상세 설계 — PLAN2의 C2를 대체
PLAN4.md                  커뮤니티 신뢰 신호 + 문항 품질 기준 + 피드백 창구. §7에 재조정된 로드맵
인수인계.md                이 문서

docs/*.md                  커밋 단위 작업 기록 (날짜-내용.md)
docs/question-authoring-standard.md
                          ★ 문항 작성 기준 (P-1 산출물, 날짜 없는 상시 문서)
                          문항을 쓰기 전에 읽는다. §6 공식/커뮤니티 검증 비대칭표,
                          §7 작성 전 체크리스트, §8 이미지 문항 기준 + 검증 절차

worker/
  index.ts                메인 엔트리 — 세션/판 진행/랭킹 라우팅, finalizeRun 등
                           ⚠ difficultyForStage()·MAX_RUN_SCORE·TOTAL_STAGES는 구 스테이지 판
                           전용 죽은 코드 — P2에서 통째로 삭제 예정. 지금 건드리지 말 것.
  auth.ts                  구글 OAuth (Arctic)
  ranking.ts               랭킹 캐시 계산·조회 — P4에서 타워 전용으로 대체 예정
  community.ts             유저 창작마당 (주제/문제 생성·삭제), 난이도 1~5 대응 완료
                           ⚠ 21행 SUPERLATIVE는 미사용 죽은 상수 (교훈 10번)

src/
  App.tsx                  메인 화면 (주제선택/퀴즈/결과/랭킹 전환)
                           ⚠ Quiz 컴포넌트의 배치 채점(5문항 다 풀어야 정답 공개)은
                           P2 보드 모드가 대체하며 소멸 — 단독 수정 대상 아님
  Workshop.tsx              창작마당 화면
  api.ts                    EP-5 저장소 계층 (fetch는 여기서만)
  types.ts                  프론트+워커 공유 타입. Difficulty = 1|2|3|4|5

db/schema.sql              멱등 베이스라인 (배포마다 재실행 — DROP 금지,
                           마이그레이션 추가분 백포트도 금지. 교훈 1·7번)
migrations/000N_*.sql       스키마 변경 (한 번만 적용, d1_migrations로 추적)
  0001_add_community_workshop.sql    유저 창작마당(topics.source/author_uid 등)
  0002_add_text_input_and_images.sql TEXT_INPUT + 이미지 URL, questions 재생성
  0003_add_difficulty_5.sql          난이도 1~5 확장, topics.q_count_5 추가

data/topics.json            공식 주제 정의서. difficultySpec은 broad 4개(과학/지리/
                             스포츠/새맞추기)에만 있고 narrow 16개는 부모 상속 구조.
                             네 주제 모두 난이도 1~5 규칙·예시가 다 채워져 있다.
data/questions/*.json       공식 문항 원본 (JSON이 소스 오브 트루스).
                             science/geography/sports는 난이도1~5 각 20개(100개).
                             birds는 난이도1~4 각 6개(24개), 난이도5는 0개.
db/seed.generated.sql        위 JSON → SQL 생성물 (gitignore, build:seed로 재생성)

scripts/
  validate.mjs              자동 검증 (ERROR면 배포 막힘, WARN이면 사람 검수)
  build-seed.mjs             JSON → SQL, 하한 게이트 자동 판정(draft/active)
                             INSERT를 100행 단위로 청크 — 되돌리지 말 것 (교훈 8번)
  review.mjs / review.html   로컬 문항 검수 웹페이지
  lib.mjs                    공통 유틸 (GATE_PER_DIFFICULTY=20 등)
prompts/generate-questions.md  문항 생성 프롬프트 — 난이도3 전용 절차(씨앗 먼저) 포함
```

## 남은 일 / 다음에 이어서 할 것

순서는 **PLAN2 로드맵 + PLAN4 §7 재조정**을 따른다. 각 항목에 앞선 단계가 필요한 이유를 붙였다 — 순서를 어기면 재작업이 생긴다.

### 0. 먼저 정할 것 — 사진 저작자 표시 (P1을 막는 결정)

교훈 11번 그대로. **P1로 76장을 더 넣기 전에 정해야 한다** — 나중에 정하면 100장에 소급 적용해야 한다. 최소 대응은 결과 화면이나 해설에 `사진: <저작자> / <라이선스>` 한 줄을 넣는 것이고, 그러려면 `birds.json`에 저작자·라이선스 필드를 추가해야 한다. **76장을 만들 때 이 필드를 같이 채우면 추가 작업이 거의 없다.**

### 1. P1 마무리 — 새 맞추기(birds) 76문항 (24 → 100)

**P-1 기준 문서(`docs/question-authoring-standard.md`)를 먼저 읽고 쓴다.** §7 체크리스트와 §8 이미지 기준이 이 작업용이다.

- 부족분: 난이도 1~4에 각 **14개** + 난이도 5에 **20개** = 76개.
- `data/topics.json`의 birds `difficultySpec`은 **난이도 1~5가 이미 다 채워져 있다** — 스펙 작성 단계는 불필요하고 난이도5 문항 20개를 그 스펙대로 쓰면 된다.
- **`NUMERIC_INPUT` 비율은 birds에 한해 면제로 확정**(2026-08-13 사용자 결정). "사진 보고 종 맞추기"가 이 주제의 정체성이라 수치 문항을 끼우면 성격이 흐려진다는 판단. **`birds: NUMERIC_INPUT 비율 0%` WARN은 계속 남는다 — 미처리 할 일이 아니라 수용된 예외다.** 76개도 전부 사진 기반(객관식/단답형)으로 채운다.
- **이미지 검증은 §8 절차를 그대로 반복한다** (API 일괄 조회 → 썸네일 저장 → Chromium 시트 → 육안). 교훈 6번의 함정(429 에러 페이지, 썸네일 오판)을 다시 밟지 말 것.
- 기존 24문항의 남은 품질 경고: **`bird-113`~`bird-118`은 난이도3인데 `seedRef`가 없다**(6건). 76개를 쓰면서 같이 정리하는 게 자연스럽다.
- ✅ **이미지 24장 육안 확인은 완료**(2026-08-13). `bird-108`(왜가리 — 아파트 배경 하천 풍경에 새가 작은 점이라 답이 불가능했다)과 `bird-117`(꾀꼬리 — 잎에 가려 작고 날짜 스탬프가 박혀 있었다)을 교체했다.

### 2. P2 — 문제 보드(3×3) 모드 (`PLAN3.md`)

구 12스테이지 판을 완전히 대체하고, `worker/index.ts`의 죽은 코드(`difficultyForStage()`·`MAX_RUN_SCORE`·`TOTAL_STAGES`)와 "시작 난이도 선택" UI를 같이 삭제한다.

**P3·P4보다 먼저여야 한다** — 둘 다 P2가 만드는 채점 경로에 의존한다. 건너뛰면 구 `finalizeRun()` 기반으로 만들었다가 다시 만드는 이중 작업이 된다(A4 업적 설계가 실제로 그렇게 한 번 폐기됐다).

**"정답 피드백 부족"이 여기서 자동 해소된다.** 코드에 실재하는 결함이지만(`src/App.tsx`의 `Quiz`는 5문항을 다 답해야 `제출`이 뜨고 그제서야 `StageResult`가 5문항 정답·해설을 한꺼번에 보여준다. 마지막 문항 버튼이 `isLast ? '제출' : '다음'`), **`PLAN4.md` §2가 "PLAN3 보드 모드가 이 문제를 그대로 해결하므로 별도 작업 불필요"로 결론냈다.** 지금 따로 고치면 P2에서 삭제될 코드를 고치는 낭비다 — **P2 우선순위를 올리는 근거로만 쓸 것.**

### 3. P-1 잔여 — 기준 문서를 실제 작성 경로에 노출

기준 문서는 썼지만 **아직 아무 화면·프롬프트에도 연결되지 않았다.** `PLAN4.md` §5가 지정한 대상:
- `prompts/generate-questions.md` — AI 생성 프롬프트에 기준 반영
- `scripts/review.mjs` / `review.html` — 검수 화면에 §7 체크리스트 노출
- `src/Workshop.tsx` — 창작마당 폼에 가이드 링크/툴팁

**`PLAN4.md` §8 ③대로 커뮤니티는 사람 검수가 없으므로, 창작마당 폼 노출이 실효성의 핵심이다.** 세 번째가 가장 중요하다.

같은 P-1 묶음의 **커뮤니티 추천/비추천**(`PLAN4.md` §3)·**유저 피드백 화면**(§4)도 미착수. 새 테이블 추가뿐이라 위험한 재생성이 없고 **P1과 병행 가능**하다.

### 4. P3 — 업적 시스템 + 장르별 대표 배지

`PLAN2.md` A4. P2 완성 후 채점 경로에 판정 로직을 넣는다. 장르별 대표 배지는 `PLAN4.md` §6에서 **새 단계 없이 P3 범위에 포함**시키기로 확정됐다(업적 테이블·판정 로직이 먼저 있어야 배지가 존재하므로). 장르 마스터 임계값은 P3 구현 시점에 정한다.

### 5. P4 — 타워 오르기

`PLAN2.md` C1. 혼합 출제, 체크포인트, 타워 전용 랭킹 보드로 기존 통합 랭킹을 대체. 이때 삭제 대상: `worker/ranking.ts`의 `handleGlobalRanking`/`upsertWeeklyBest`/`handleTopicRanking`, `src/types.ts`의 `GlobalBoardId`/`RankingBoardResponse`/`TopicRankingResponse`.

**P1이 선행인 실질적 이유**: birds가 `draft`인 채로 "전 주제 혼합 출제"를 만들면 출제 풀이 그만큼 빈다.

### 6. P5 — 세부 장르 개방 (상시)

`PLAN2.md` A3. 문항이 하한(20/난이도)을 채우는 대로 자동 활성화되는 구조라 상시 진행. 현재 narrow 16개가 전부 `draft`다 — 구기 32개가 최다, 단위 4개가 최소. 현황은 `npm run validate` 출력에서 볼 수 있다.

### 7. 그 외 (후순위)

- **커뮤니티 최상급 검사 활성화 여부** — 교훈 10번. 유저 입력을 거절하게 되므로 결정이 필요하다.
- **진짜 이미지 파일 업로드** — 지금은 URL 붙여넣기. 기기에서 사진을 올리게 하려면 **Cloudflare R2 버킷 프로비저닝이 필요**하고, Google OAuth 때처럼 사용자가 콘솔에서 만들어야 할 수 있다.
- **문항 정답률 기반 난이도 재검토** — `question_stats`에 집계만 쌓이고 보정 로직은 없다 (`docs/2026-08-12-question-sourcing-research.md` ⑤).
- **PLAN.md는 갱신하지 않는다** — 이 문서 + PLAN2/3/4 + 실제 코드를 기준으로 삼을 것.

## 사용자와의 작업 스타일 (참고)

- **질문은 짧고 명확하게.** 큰 결정(공개 범위, 랭킹 반영, 배포 시점 등)은 `AskUserQuestion`으로 짧은 객관식으로 물어본다. 확인은 보통 "ㄱㄱ", 한 단어 등으로 짧게 온다 — 장황한 설명 없이 진행해도 된다.
- **"질문과 구현의 반복적 프로세스"를 명시적으로 선호한다** — 큰 작업을 한 번에 끝까지 밀지 말고, 자연스러운 체크포인트(배포 여부, 스코프 변경)마다 짧게 확인받고 이어간다.
- 구현 세부(게이트 숫자, 캐시 갱신 방식, 문항 배분 비율 등)는 **알아서 합리적으로 정하고 결과를 문서·보고에 명시**하는 방식으로 진행해왔다 — 사소한 것까지 다 묻지 않는다. 다만 **실사용자에게 영향이 가는 배포 판단(활성 콘텐츠가 사라지는 등)은 반드시 먼저 확인.**
- 실제 사용자가 프로덕션을 직접 써보며 버그를 발견하는 패턴이 많았다(랭킹 미반영, 창작마당 삭제 필요 등). **배포 후에는 사용자가 직접 확인해볼 것을 권하고**, 보고가 오면 프로덕션 데이터를 직접 조회해 원인을 찾는 방식이 잘 통했다.
- **사용자가 확인해줄 수 없는 경우가 있다** — 2026-08-13에 이미지 확인을 부탁했더니 "확인할 방법이 없다"는 답이 왔다. 사용자 확인을 전제로 한 계획은 막힐 수 있으니, **우리 쪽에서 검증 경로를 뚫는 방법을 먼저 찾아볼 것**(교훈 6번이 그 사례다).
- 설계(PLAN 문서 작성·수정)와 구현을 다른 세션으로 분리해 진행한 적이 있다. 다음 세션에서도 이 구분이 유지되는지는 사용자에게 먼저 확인할 것.
